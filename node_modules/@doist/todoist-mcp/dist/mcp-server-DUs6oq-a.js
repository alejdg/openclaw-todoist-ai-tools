import { ResourceTemplate as cs, McpServer as ds } from "@modelcontextprotocol/sdk/server/mcp.js";
import { createHash as ls, randomUUID as Ne } from "node:crypto";
import { readFileSync as us } from "node:fs";
import { dirname as xt, join as Le } from "node:path";
import { fileURLToPath as ps } from "node:url";
import { registerAppResource as ms, RESOURCE_MIME_TYPE as Et, registerAppTool as hs } from "@modelcontextprotocol/ext-apps/server";
import Q, { ZodError as fs, z as s } from "zod";
import { AsyncLocalStorage as bs } from "node:async_hooks";
import { createRequire as gs } from "node:module";
import { TodoistApi as ys, isWorkspaceProject as ke, isPersonalProject as ie, colors as Me, LOCATION_TRIGGERS as Ot, REMINDER_TYPES as ks, createCommand as q, REMINDER_DELIVERY_SERVICES as Ts, HEALTH_STATUSES as Ve, getTaskUrl as Ut, getProjectUrl as Pt, GOAL_OWNER_TYPES as ws, WORKSPACE_ROLES as vs, WORKSPACE_PLANS as Is } from "@doist/todoist-sdk";
import { addDays as Fe, formatISO as ct } from "date-fns";
const dt = xt(ps(import.meta.url)), js = [
  Le(dt, "task-list", "index.html"),
  Le(dt, "mcp-apps", "index.html")
], Ss = `<!doctype html>
<html>
  <body>
    <p>Task list app is missing. Run "npm run build" to generate it.</p>
  </body>
</html>`;
function Cs() {
  const e = [];
  for (const t of js)
    try {
      return us(t, "utf-8");
    } catch (o) {
      e.push(o);
    }
  return console.error("Failed to load task list app HTML from any known path:", e), Ss;
}
const _t = Cs(), $s = ls("sha256").update(_t).digest("hex").slice(0, 12), He = `ui://todoist/task-list@${$s}`, Ds = "ui://todoist/task-list@{hash}", We = "Interactive task list widget", As = "https://ai.todoist.net", Ge = {
  ui: {
    prefersBorder: !0,
    csp: {
      // The widget is bundled into one HTML file and does not fetch or load external assets.
      connectDomains: [],
      resourceDomains: []
    }
  },
  "openai/widgetDescription": We,
  "openai/widgetPrefersBorder": !0,
  "openai/widgetCSP": {
    connect_domains: [],
    resource_domains: []
  },
  "openai/widgetDomain": As
};
function lt(e) {
  return {
    contents: [
      {
        uri: e,
        mimeType: Et,
        text: _t,
        _meta: Ge
      }
    ]
  };
}
function xs(e) {
  ms(
    e,
    "todoist-task-list",
    He,
    {
      description: We,
      _meta: Ge
    },
    async () => lt(He)
  ), e.registerResource(
    "todoist-task-list-hash-fallback",
    new cs(Ds, { list: void 0 }),
    {
      description: `${We} compatibility fallback`,
      mimeType: Et,
      _meta: Ge
    },
    async (t) => lt(t.toString())
  );
}
function z(e) {
  return typeof e == "object" && e !== null;
}
function Be(...e) {
  return e.find(z);
}
function N(...e) {
  return e.find((t) => t !== void 0);
}
function O(e) {
  if (typeof e == "number" && Number.isFinite(e))
    return e;
  if (typeof e == "string" && /^\d+$/.test(e.trim()))
    return Number(e.trim());
}
function T(e) {
  if (typeof e == "string") {
    const t = e.trim();
    return t.length > 0 ? t : void 0;
  }
  if (typeof e == "number" || typeof e == "boolean")
    return String(e);
}
function ee(e) {
  const t = O(e);
  return t !== void 0 ? t : T(e);
}
function Es(e) {
  return e.replace(/\b(Bearer)\s+[A-Za-z0-9._~+/=-]+/gi, "$1 [REDACTED]").replace(
    /\b(token|api[_-]?key|authorization)\s*[:=]\s*["']?[A-Za-z0-9._~+/=-]{6,}["']?/gi,
    "$1: [REDACTED]"
  ).replace(/([?&](?:token|api[_-]?key|authorization)=)[^&\s]+/gi, "$1[REDACTED]").replace(/\beyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\b/g, "[REDACTED]");
}
function P(e, t = 220) {
  const o = Es(e).replace(/\s+/g, " ").trim();
  return o.length <= t ? o : `${o.slice(0, t - 3)}...`;
}
function ut(e) {
  const t = e.trim();
  return /\bHTTP\s*\d{3}\b/i.test(t) || /\bstatus code \d{3}\b/i.test(t) || /^bad request$/i.test(t) || /^unauthorized$/i.test(t) || /^forbidden$/i.test(t) || /^not found$/i.test(t);
}
function Ee(e) {
  if (!e)
    return;
  const t = e.match(/\b(?:HTTP|status code)\s*[:#-]?\s*(\d{3})\b/i);
  if (t?.[1])
    return O(t[1]);
}
function Y(e) {
  const t = T(e);
  if (t)
    return P(t);
  if (Array.isArray(e)) {
    const n = e.map((a) => z(a) ? T(a.message) || T(a.error) : T(a)).filter((a) => !!a);
    return n.length > 0 ? P(n.slice(0, 2).join("; ")) : void 0;
  }
  if (!z(e))
    return;
  const o = N(
    T(e.detail),
    T(e.details),
    T(e.message),
    T(e.error),
    T(e.description)
  );
  if (o)
    return P(o);
  const r = [];
  for (const [n, a] of Object.entries(e)) {
    const i = T(a);
    if (i && (r.push(`${n}: ${i}`), r.length >= 2))
      break;
  }
  return r.length > 0 ? P(r.join("; ")) : void 0;
}
function Os(e) {
  if (!e)
    return [];
  const t = /* @__PURE__ */ new Set(), o = (i) => {
    i && t.add(P(i, 120));
  }, r = N(
    T(e.field),
    T(e.parameter),
    T(e.param),
    T(e.path)
  );
  o(r);
  const n = Be(
    e.details,
    e.errorDetails,
    e.errorExtra,
    e.error_extra
  );
  if (n) {
    const i = N(
      T(n.field),
      T(n.parameter),
      T(n.param),
      T(n.path),
      T(n.argument)
    );
    o(i);
  }
  const a = e.errors;
  if (Array.isArray(a))
    for (const i of a) {
      if (!z(i)) {
        o(T(i));
        continue;
      }
      const c = N(
        T(i.field),
        T(i.parameter),
        T(i.param),
        T(i.path),
        T(i.name)
      ), d = N(
        T(i.message),
        T(i.error),
        T(i.detail),
        T(i.description)
      );
      o(c && d ? `${c}: ${d}` : c || d);
    }
  else if (z(a))
    for (const [i, c] of Object.entries(a)) {
      const d = T(c);
      if (d) {
        o(`${i}: ${d}`);
        continue;
      }
      if (Array.isArray(c)) {
        const l = c.map((p) => T(p)).filter((p) => !!p).join(", ");
        o(l ? `${i}: ${l}` : i);
      }
    }
  return Array.from(t).slice(0, 3);
}
const Us = [
  "error",
  "error_code",
  "error_tag",
  "http_code",
  "error_extra",
  "errorCode",
  "errorTag",
  "httpCode",
  "errorExtra"
];
function Ps(e) {
  return e ? Us.some((t) => e[t] !== void 0) : !1;
}
function _s(e, t) {
  return e === 401 || e === 403 ? "Verify your API token and access permissions, then retry." : e === 404 ? "Confirm the referenced IDs exist and are accessible, then retry." : e === 429 ? "Rate limit reached. Wait briefly and retry." : e !== void 0 && e >= 500 ? "Todoist API may be temporarily unavailable. Retry shortly." : t ? "Fix the field hints above and retry." : e === 400 || e === 422 ? "Check parameter values and formats, then retry." : "Check the request parameters and retry.";
}
function Rs(e) {
  const t = z(e) ? e : void 0, o = e instanceof Error && z(e.cause) ? e.cause : void 0, r = Be(t?.response, o?.response), n = Be(
    t?.responseData,
    r?.data,
    t?.data,
    o?.responseData,
    o?.data
  ), a = N(
    O(t?.httpStatusCode),
    O(t?.statusCode),
    O(t?.status),
    O(r?.status),
    O(n?.httpStatusCode),
    O(n?.statusCode),
    O(n?.status),
    O(n?.httpCode),
    O(n?.http_code),
    Ee(T(t?.message)),
    Ee(T(o?.message)),
    Ee(typeof e == "string" ? e : void 0)
  ), i = N(
    ee(n?.errorCode),
    ee(n?.error_code),
    ee(n?.code),
    ee(t?.errorCode),
    ee(t?.error_code),
    ee(t?.code)
  ), c = N(
    T(n?.errorTag),
    T(n?.error_tag),
    T(n?.tag),
    T(t?.errorTag),
    T(t?.error_tag),
    T(t?.tag)
  ), d = [
    T(n?.error),
    T(n?.message),
    Y(n?.message),
    T(t?.message),
    T(o?.message),
    e instanceof Error ? e.message : T(e)
  ].filter((h) => !!h), l = d.find((h) => !ut(h)) || d[0], p = N(
    Y(n?.errorExtra),
    Y(n?.error_extra),
    Y(n?.details),
    Y(n?.errorDetails),
    Y(n?.errors),
    Y(t?.details)
  ), u = Os(n);
  return a !== void 0 || Ps(n) || c !== void 0 || i !== void 0 || (l ? ut(l) : !1) ? {
    statusCode: a,
    code: i,
    tag: c ? P(c, 80) : void 0,
    message: l ? P(l) : void 0,
    details: p ? P(p) : void 0,
    fieldHints: u
  } : null;
}
function Ns(e) {
  const t = [];
  e.statusCode !== void 0 && t.push(`HTTP ${e.statusCode}`), e.code !== void 0 && t.push(`code ${e.code}`), e.tag && t.push(`tag ${e.tag}`);
  const o = [
    t.length > 0 ? `Todoist API request failed (${t.join(", ")}).` : "Todoist API request failed."
  ];
  return e.message && o.push(`Message: ${e.message}`), e.details && e.details !== e.message && o.push(`Details: ${e.details}`), e.fieldHints.length > 0 && o.push(`Field hints: ${e.fieldHints.join("; ")}`), o.push(`Try next: ${_s(e.statusCode, e.fieldHints.length > 0)}`), o.join(`
`);
}
function Ls(e) {
  return e instanceof Error ? P(e.message) : typeof e == "string" ? P(e) : "An unknown error occurred";
}
function Ms(e) {
  if (e instanceof fs)
    return e.message;
  const t = Rs(e);
  return t ? Ns(t) : Ls(e);
}
const Fs = "10.4.0", Hs = {
  version: Fs
}, Ws = "todoist-mcp", Je = Hs.version, Rt = new bs(), pt = gs(import.meta.url);
let ye;
const Gs = async () => {
  const e = pt.resolve("@doist/todoist-sdk"), t = Le(xt(e), "transport", "http-dispatcher.js");
  return pt(t);
};
let Bs = Gs;
function Ys() {
  return `${Ws}/${Je}`;
}
function Nt(e) {
  return e.trim().toLowerCase();
}
function qs(e, t) {
  return Rt.run(Nt(e), t);
}
function zs({
  label: e,
  platform: t = "mcp",
  sessionId: o = Ne()
} = {}) {
  const r = e ? Nt(e) : Rt.getStore();
  return {
    "User-Agent": Ys(),
    "doist-platform": t,
    "doist-version": Je,
    "request-id": Ne(),
    "session-id": o,
    "mcp-tool": r ?? "unknown"
  };
}
function Ks(e, t) {
  const o = new Headers(e);
  for (const [r, n] of Object.entries(zs(t)))
    o.set(r, n);
  return Object.fromEntries(o.entries());
}
function mt() {
  return new DOMException("The operation was aborted due to timeout", "TimeoutError");
}
function Vs(e, t) {
  if (!e && t === void 0)
    return {
      signal: void 0,
      cleanup: () => {
      }
    };
  if (!e && t !== void 0) {
    const i = new AbortController(), c = setTimeout(() => i.abort(mt()), t);
    return {
      signal: i.signal,
      cleanup: () => clearTimeout(c)
    };
  }
  if (e && t === void 0)
    return {
      signal: e,
      cleanup: () => {
      }
    };
  const o = new AbortController(), r = [], n = (i) => {
    o.signal.aborted || o.abort(i);
  };
  if (e?.aborted)
    n(e.reason);
  else if (e) {
    const i = () => n(e.reason);
    e.addEventListener("abort", i, { once: !0 }), r.push(() => e.removeEventListener("abort", i));
  }
  const a = setTimeout(() => n(mt()), t);
  return r.push(() => clearTimeout(a)), {
    signal: o.signal,
    cleanup: () => {
      for (const i of r)
        i();
    }
  };
}
async function Js() {
  if (Xs())
    return ye || (ye = Bs().then((e) => e.getDefaultDispatcher()).catch((e) => {
      throw ye = void 0, e;
    })), ye;
}
function Xs() {
  return typeof process < "u" && !!process.versions?.node;
}
async function Zs(e) {
  const t = await Js();
  t !== void 0 && (e.dispatcher = t);
}
function Qs(e) {
  return {
    ok: e.ok,
    status: e.status,
    statusText: e.statusText,
    headers: Object.fromEntries(e.headers.entries()),
    text: () => e.text(),
    json: () => e.json(),
    arrayBuffer: () => e.arrayBuffer()
  };
}
function eo(e = globalThis.fetch, t = {}) {
  const o = e === globalThis.fetch, { enabled: r = !0, platform: n = "mcp", sessionId: a = Ne() } = t;
  return async (i, c = {}) => {
    const { timeout: d, headers: l, signal: p, ...u } = c, { signal: m, cleanup: h } = Vs(p, d);
    try {
      const k = {
        ...u,
        signal: m,
        ...r ? { headers: Ks(l, { platform: n, sessionId: a }) } : l !== void 0 ? { headers: l } : {}
      };
      o && await Zs(k);
      const b = await e(i, k);
      return Qs(b);
    } finally {
      h();
    }
  };
}
function to(e, {
  baseUrl: t,
  tracking: o
} = {}) {
  return new ys(e, {
    customFetch: eo(globalThis.fetch, o),
    ...t ? { baseUrl: t } : {}
  });
}
const so = 2, oo = 500, ro = 2e3, no = /* @__PURE__ */ new Set([502, 503, 504]);
function ao(e) {
  if (e == null || typeof e != "object")
    return;
  const t = e;
  if (typeof t.httpStatusCode == "number")
    return t.httpStatusCode;
  if (typeof t.statusCode == "number")
    return t.statusCode;
  if (typeof t.status == "number")
    return t.status;
  if (e instanceof Error) {
    const o = e.message.match(/\bHTTP\s+(\d{3})\b/i);
    if (o?.[1])
      return Number(o[1]);
  }
}
function io(e) {
  const t = ao(e);
  return t !== void 0 && no.has(t);
}
function co({
  attempt: e,
  baseDelayMs: t,
  maxDelayMs: o
}) {
  const r = t * 2 ** e;
  return Math.min(r, o);
}
function lo(e) {
  return new Promise((t) => setTimeout(t, e));
}
async function uo(e, t = {}) {
  const o = t.maxRetries ?? so, r = t.baseDelayMs ?? oo, n = t.maxDelayMs ?? ro;
  let a;
  for (let i = 0; i <= o; i++)
    try {
      return await e();
    } catch (c) {
      if (a = c, i < o && io(c)) {
        const d = co({ attempt: i, baseDelayMs: r, maxDelayMs: n });
        await lo(d);
        continue;
      }
      throw c;
    }
  throw a;
}
function Ye(e) {
  if (e == null)
    return e;
  if (Array.isArray(e))
    return e.map((t) => Ye(t));
  if (typeof e == "object") {
    const t = {};
    for (const [o, r] of Object.entries(e))
      if (r !== null) {
        const n = Ye(r);
        if (n !== null && typeof n == "object" && !Array.isArray(n) && Object.keys(n).length === 0)
          continue;
        t[o] = n;
      }
    return t;
  }
  return e;
}
const f = {
  // Task management tools
  ADD_TASKS: "add-tasks",
  COMPLETE_TASKS: "complete-tasks",
  UNCOMPLETE_TASKS: "uncomplete-tasks",
  UPDATE_TASKS: "update-tasks",
  FIND_TASKS: "find-tasks",
  FIND_TASKS_BY_DATE: "find-tasks-by-date",
  FIND_COMPLETED_TASKS: "find-completed-tasks",
  RESCHEDULE_TASKS: "reschedule-tasks",
  // Project management tools
  ADD_PROJECTS: "add-projects",
  UPDATE_PROJECTS: "update-projects",
  FIND_PROJECTS: "find-projects",
  PROJECT_MANAGEMENT: "project-management",
  PROJECT_MOVE: "project-move",
  // Section management tools
  ADD_SECTIONS: "add-sections",
  UPDATE_SECTIONS: "update-sections",
  FIND_SECTIONS: "find-sections",
  // Goal management tools
  FIND_GOALS: "find-goals",
  ADD_GOALS: "add-goals",
  UPDATE_GOALS: "update-goals",
  COMPLETE_GOALS: "complete-goals",
  LINK_GOAL_TASKS: "link-goal-tasks",
  // Comment management tools
  ADD_COMMENTS: "add-comments",
  UPDATE_COMMENTS: "update-comments",
  FIND_COMMENTS: "find-comments",
  // Reminder management tools
  ADD_REMINDERS: "add-reminders",
  UPDATE_REMINDERS: "update-reminders",
  FIND_REMINDERS: "find-reminders",
  // Attachment tools
  VIEW_ATTACHMENT: "view-attachment",
  // Assignment and collaboration tools
  FIND_PROJECT_COLLABORATORS: "find-project-collaborators",
  MANAGE_ASSIGNMENTS: "manage-assignments",
  // Activity and audit tools
  FIND_ACTIVITY: "find-activity",
  GET_PRODUCTIVITY_STATS: "get-productivity-stats",
  // Health and insights tools
  GET_PROJECT_HEALTH: "get-project-health",
  GET_PROJECT_ACTIVITY_STATS: "get-project-activity-stats",
  ANALYZE_PROJECT_HEALTH: "analyze-project-health",
  GET_WORKSPACE_INSIGHTS: "get-workspace-insights",
  // General tools
  GET_OVERVIEW: "get-overview",
  DELETE_OBJECT: "delete-object",
  FETCH_OBJECT: "fetch-object",
  REORDER_OBJECTS: "reorder-objects",
  USER_INFO: "user-info",
  // Label management tools
  ADD_LABELS: "add-labels",
  UPDATE_LABELS: "update-labels",
  FIND_LABELS: "find-labels",
  // Filter management tools
  FIND_FILTERS: "find-filters",
  ADD_FILTERS: "add-filters",
  UPDATE_FILTERS: "update-filters",
  // Workspace tools
  LIST_WORKSPACES: "list-workspaces",
  // OpenAI MCP tools
  SEARCH: "search",
  FETCH: "fetch"
}, rd = {
  /**
   * Strips email addresses from tool outputs that expose user data.
   * Affects: find-project-collaborators, find-completed-tasks
   */
  STRIP_EMAILS: "strip_emails"
}, po = process.env.USE_STRUCTURED_CONTENT === "true" || process.env.NODE_ENV === "test";
function mo({
  textContent: e,
  structuredContent: t,
  contentItems: o
}) {
  const r = Ye(t), n = {}, a = [];
  return o && a.push(...o), !po && t && a.push({
    type: "text",
    text: JSON.stringify(r)
  }), e && a.push({ type: "text", text: e }), a.length > 0 && (n.content = a), t && (n.structuredContent = r), n;
}
function ho(e) {
  return {
    content: [{ type: "text", text: e }],
    isError: !0
  };
}
function fo(e) {
  return { ...{
    title: `Todoist: ${bo(e.name)}`,
    openWorldHint: !1
  }, ...e.annotations };
}
function bo(e) {
  return e.split("-").filter(Boolean).map((t) => `${t.charAt(0).toUpperCase()}${t.slice(1)}`).join(" ");
}
function go(e) {
  return e ? typeof e["ui/resourceUri"] == "string" ? !0 : typeof e.ui == "object" && e.ui !== null : !1;
}
const yo = [
  f.FIND_PROJECT_COLLABORATORS,
  f.FIND_COMPLETED_TASKS
];
function qe(e) {
  if (e == null)
    return e;
  if (Array.isArray(e))
    return e.map((t) => qe(t));
  if (typeof e == "object") {
    const t = {};
    for (const [o, r] of Object.entries(e))
      o !== "email" && (t[o] = qe(r));
    return t;
  }
  return e;
}
function ko(e) {
  const t = /\s*\([^)]*@[^)]+\)/g, o = /\S+@\S+\.\S+/g;
  return e.replace(t, "").replace(o, "[email hidden]");
}
function g({
  tool: e,
  server: t,
  client: o,
  features: r = []
}) {
  const n = r.some((c) => c.name === "strip_emails") && yo.includes(e.name), a = async (c, d) => {
    try {
      let { textContent: l, structuredContent: p, contentItems: u } = await qs(
        e.name,
        () => uo(
          () => e.execute(c, o)
        )
      );
      return n && (l && (l = ko(l)), p && (p = qe(p))), mo({ textContent: l, structuredContent: p, contentItems: u });
    } catch (l) {
      return console.error(`Error executing tool ${e.name}:`, { args: c, error: l }), ho(Ms(l));
    }
  }, i = {
    description: e.description,
    inputSchema: e.parameters,
    ...e.outputSchema ? { outputSchema: e.outputSchema } : {},
    annotations: fo(e),
    ...e._meta ? { _meta: e._meta } : {}
  };
  if (go(e._meta)) {
    hs(
      t,
      e.name,
      {
        ...i,
        _meta: e._meta
      },
      a
    );
    return;
  }
  t.registerTool(e.name, i, a);
}
const To = {
  period: s.enum(["today", "7d", "14d", "30d", "this-week", "this-month"]).default("7d").describe(
    'Time period for the analysis. "today" analyzes the current day, "7d"/"14d"/"30d" analyze the last N days, "this-week" and "this-month" analyze the current week or month.'
  ),
  focus: s.enum(["overall", "goals", "projects", "trends", "recommendations"]).default("overall").describe(
    'Focus area for the analysis. "overall" provides a comprehensive report covering all areas. Other values focus on a specific aspect.'
  ),
  projectId: s.string().optional().describe("Optional project ID to scope the analysis to a specific project.")
};
function wo(e, t = /* @__PURE__ */ new Date()) {
  const o = (n) => n.toISOString().slice(0, 10), r = o(t);
  switch (e) {
    case "today":
      return { since: r, until: r, periodDescription: "today" };
    case "7d": {
      const n = new Date(t);
      return n.setDate(n.getDate() - 6), {
        since: o(n),
        until: r,
        periodDescription: "the last 7 days"
      };
    }
    case "14d": {
      const n = new Date(t);
      return n.setDate(n.getDate() - 13), {
        since: o(n),
        until: r,
        periodDescription: "the last 14 days"
      };
    }
    case "30d": {
      const n = new Date(t);
      return n.setDate(n.getDate() - 29), {
        since: o(n),
        until: r,
        periodDescription: "the last 30 days"
      };
    }
    case "this-week": {
      const n = t.getUTCDay(), a = new Date(t);
      return a.setDate(t.getDate() - (n + 6) % 7), {
        since: o(a),
        until: r,
        periodDescription: "this week (Monday to today)"
      };
    }
    case "this-month": {
      const n = new Date(Date.UTC(t.getUTCFullYear(), t.getUTCMonth(), 1));
      return {
        since: o(n),
        until: r,
        periodDescription: "this month"
      };
    }
  }
}
function vo(e) {
  const t = {
    goals: `### Goal Tracking
- Compare tasks completed today vs the daily goal
- Calculate progress toward the weekly goal (using daysItems to sum completions for the current week)
- Report on the current daily and weekly streaks — how long they've been going, and how they compare to the user's best streaks
- Flag if the user is at risk of breaking a streak`,
    trends: `### Completion Trends
- Show a day-by-day breakdown of tasks completed over the period
- Identify peak productivity days and low-activity days
- Note any patterns (e.g., weekday vs weekend, specific days that are consistently high or low)
- Compare weekly totals if multiple weeks are in the period`,
    projects: `### Project Distribution
- Break down completions by project (using per-project data from daysItems/weekItems)
- Identify which projects received the most and least attention
- Highlight any projects with zero completions in the period
- If a specific project was requested, focus the analysis on that project's performance`,
    recommendations: `### Karma & Momentum
- Report the current karma score and trend
- Summarize recent karma changes and their reasons

### Recommendations
- Based on all the data gathered, provide 3-5 specific, actionable suggestions
- Address any concerning patterns (declining completions, broken streaks, neglected projects)
- Suggest concrete next steps (e.g., "Focus on Project X which has had no completions this week")
- If goals seem too easy or too hard based on actual completion rates, suggest adjustments`
  };
  return e === "overall" ? Object.values(t).join(`

`) : t[e] ?? "";
}
function Io(e) {
  const { since: t, until: o, periodDescription: r } = wo(e.period), n = e.projectId ? `
Scope this analysis to project ID: ${e.projectId}. Pass this projectId when calling find-completed-tasks and get-overview.` : "";
  return `Analyze my Todoist productivity for ${r} (${t} to ${o}).${n}

## Instructions

Follow these steps to gather data and produce the analysis:

### Step 1: Gather Data

Call the following tools to collect the necessary data:

1. **user-info** — Get my timezone, daily/weekly goals, tasks completed today, and user ID (you'll need the user ID for the responsibleUser filter)
2. **get-productivity-stats** — Get streak data, karma score/trend, daily and weekly completion breakdowns, and karma history
3. **find-completed-tasks** — Get completed tasks for the period (since: "${t}", until: "${o}"). Set responsibleUser to my user ID (from user-info) to see only my completions${e.projectId ? `. Set projectId to "${e.projectId}"` : ""}
${e.focus === "overall" || e.focus === "projects" ? `4. **get-overview** — Get the project structure${e.projectId ? ` for project "${e.projectId}"` : ""} to provide context for the analysis` : ""}

### Step 2: Analyze

Using the collected data, produce an analysis covering the following sections:

${vo(e.focus)}

### Step 3: Format

Present the analysis as a clear, well-structured markdown report with:
- A summary header with the period and key headline metrics
- Each analytical section with its own header
- Specific numbers and comparisons (not vague statements)
- Actionable takeaways highlighted clearly`;
}
function jo(e) {
  return {
    messages: [
      {
        role: "user",
        content: {
          type: "text",
          text: Io(e)
        }
      }
    ]
  };
}
const me = {
  name: "productivity-analysis",
  title: "Productivity Analysis",
  description: "Analyze your Todoist productivity with insights on completion trends, goal streaks, project distribution, and actionable recommendations. Gathers data from multiple tools and synthesizes a comprehensive report.",
  argsSchema: To,
  callback: jo
}, j = {
  /** Default limit for task listings */
  TASKS_DEFAULT: 10,
  /** Maximum limit for task search and list operations */
  TASKS_MAX: 100,
  /** Default limit for completed tasks */
  COMPLETED_TASKS_DEFAULT: 50,
  /** Maximum limit for completed tasks */
  COMPLETED_TASKS_MAX: 200,
  /** Default limit for project listings */
  PROJECTS_DEFAULT: 50,
  /** Maximum limit for project listings */
  PROJECTS_MAX: 200,
  /** Maximum limit for section listings */
  SECTIONS_MAX: 200,
  /** Batch size for fetching all tasks in a project */
  TASKS_BATCH_SIZE: 50,
  /** Default limit for comment listings */
  COMMENTS_DEFAULT: 10,
  /** Maximum limit for comment search and list operations */
  COMMENTS_MAX: 10,
  /** Default limit for activity log listings */
  ACTIVITY_DEFAULT: 20,
  /** Maximum limit for activity log search and list operations */
  ACTIVITY_MAX: 100,
  /** Default limit for label listings */
  LABELS_DEFAULT: 50,
  /** Maximum limit for label listings */
  LABELS_MAX: 200,
  /** Default limit for goal listings */
  GOALS_DEFAULT: 50,
  /** Maximum limit for goal search and list operations */
  GOALS_MAX: 200
}, Oe = {
  /** Maximum number of failures to show in detailed error messages */
  MAX_FAILURES_SHOWN: 3
};
class U extends Error {
  constructor(t, o) {
    super(`Invalid duration format "${t}": ${o}`), this.name = "DurationParseError";
  }
}
function Lt(e) {
  if (!e || typeof e != "string")
    throw new U(e, "Duration must be a non-empty string");
  const t = e.trim().toLowerCase().replace(/\s+/g, "");
  if (!t)
    throw new U(e, "Duration must be a non-empty string");
  const o = t.match(/^(?:(\d+(?:\.\d+)?)h)?(?:(\d+(?:\.\d+)?)m)?$/);
  if (!o || !o[1] && !o[2])
    throw new U(e, 'Use format like "2h", "30m", "2h30m", or "1.5h"');
  let r = 0;
  const [, n, a] = o;
  if (n) {
    const i = Number.parseFloat(n);
    if (Number.isNaN(i) || i < 0)
      throw new U(e, "Hours must be a positive number");
    r += i * 60;
  }
  if (a) {
    const i = Number.parseFloat(a);
    if (Number.isNaN(i) || i < 0)
      throw new U(e, "Minutes must be a positive number");
    if (i % 1 !== 0)
      throw new U(
        e,
        "Minutes must be a whole number (use decimal hours instead)"
      );
    r += i;
  }
  if (r = Math.round(r), r === 0)
    throw new U(e, "Duration must be greater than 0 minutes");
  if (r > 1440)
    throw new U(e, "Duration cannot exceed 24 hours (1440 minutes)");
  return { minutes: r };
}
function So(e) {
  if (e <= 0) return "0m";
  const t = Math.floor(e / 60), o = e % 60;
  return t === 0 ? `${o}m` : o === 0 ? `${t}h` : `${t}h${o}m`;
}
const Co = ["p1", "p2", "p3", "p4"], Xe = 'Task priority as a string: "p1" (highest), "p2" (high), "p3" (medium), or "p4" (lowest/default). Integers like 1, 2, 3, or 4 are not accepted.', Ze = s.enum(Co).describe(Xe);
function Mt(e) {
  return { p1: 4, p2: 3, p3: 2, p4: 1 }[e];
}
function $o(e) {
  return { 4: "p1", 3: "p2", 2: "p3", 1: "p4" }[e];
}
const E = /* @__PURE__ */ new Map(), te = /* @__PURE__ */ new Map(), Ue = 300 * 1e3, Do = "me";
class Ao {
  /**
   * Resolve a user name or ID to a user ID by looking up collaborators across all shared projects.
   * Supports exact name matches, partial matches, email matches, and the "me" keyword.
   */
  async resolveUser(t, o) {
    if (!o || o.trim().length === 0)
      return null;
    const r = o.trim(), n = E.get(r);
    if (n && Date.now() - n.timestamp < Ue)
      return n.result;
    if (r.toLowerCase() === Do)
      try {
        const a = await t.getUser();
        return {
          userId: a.id,
          displayName: a.fullName,
          email: a.email
        };
      } catch {
        return null;
      }
    if (/^[0-9]+$/.test(r) || /^[a-f0-9-]{8,}$/i.test(r) && r.includes("-") || /^[a-z0-9_]{6,}$/i.test(r) && !/^[a-z]+[\s-]/.test(r) && /[0-9_]/.test(r)) {
      const a = { userId: r, displayName: r, email: r };
      return E.set(r, { result: a, timestamp: Date.now() }), a;
    }
    try {
      let a = await this.getAllCollaborators(t);
      try {
        const l = await t.getUser();
        if (l) {
          const p = {
            id: l.id,
            name: l.fullName,
            email: l.email
          };
          a.some((u) => u.id === l.id) || (a = [p, ...a]);
        }
      } catch {
      }
      if (a.length === 0)
        return E.set(r, { result: null, timestamp: Date.now() }), null;
      const i = o.toLowerCase().trim();
      let c = a.find((l) => l.id === r);
      if (c) {
        const l = { userId: c.id, displayName: c.name, email: c.email };
        return E.set(r, { result: l, timestamp: Date.now() }), l;
      }
      if (c = a.find((l) => l.name.toLowerCase() === i), c) {
        const l = { userId: c.id, displayName: c.name, email: c.email };
        return E.set(r, { result: l, timestamp: Date.now() }), l;
      }
      if (c = a.find((l) => l.email.toLowerCase() === i), c) {
        const l = { userId: c.id, displayName: c.name, email: c.email };
        return E.set(r, { result: l, timestamp: Date.now() }), l;
      }
      if (c = a.find((l) => l.name.toLowerCase().includes(i)), c) {
        const l = { userId: c.id, displayName: c.name, email: c.email };
        return E.set(r, { result: l, timestamp: Date.now() }), l;
      }
      if (c = a.find((l) => l.email.toLowerCase().includes(i)), c) {
        const l = { userId: c.id, displayName: c.name, email: c.email };
        return E.set(r, { result: l, timestamp: Date.now() }), l;
      }
      const d = null;
      return E.set(r, { result: d, timestamp: Date.now() }), d;
    } catch {
      return E.set(r, { result: null, timestamp: Date.now() }), null;
    }
  }
  /**
   * Validate that a user is a collaborator on a specific project
   */
  async validateProjectCollaborator(t, o, r) {
    try {
      return (await this.getProjectCollaborators(t, o)).some((a) => a.id === r);
    } catch {
      return !1;
    }
  }
  /**
   * Get collaborators for a specific project
   */
  async getProjectCollaborators(t, o) {
    const r = `project_${o}`, n = te.get(r);
    if (n && Date.now() - n.timestamp < Ue)
      return n.result;
    try {
      const a = await t.getProjectCollaborators(o), c = (Array.isArray(a) ? a : a.results || []).filter((d) => d?.id && d.name && d.email);
      return te.set(r, {
        result: c,
        timestamp: Date.now()
      }), c;
    } catch {
      return [];
    }
  }
  /**
   * Get all collaborators from all shared projects, deduplicated by user ID.
   */
  async getAllCollaborators(t) {
    const o = "all_collaborators", r = te.get(o);
    if (r && Date.now() - r.timestamp < Ue)
      return r.result;
    try {
      const a = (await L({
        apiMethod: t.getProjects.bind(t),
        args: {}
      })).filter((p) => p.isShared);
      if (a.length === 0) {
        const p = [];
        return te.set(o, { result: p, timestamp: Date.now() }), p;
      }
      const i = [], c = /* @__PURE__ */ new Set(), d = a.map(
        (p) => this.getProjectCollaborators(t, p.id)
      ), l = await Promise.allSettled(d);
      for (const p of l)
        if (p.status === "fulfilled")
          for (const u of p.value)
            u && !c.has(u.id) && (i.push(u), c.add(u.id));
      return te.set(o, {
        result: i,
        timestamp: Date.now()
      }), i;
    } catch {
      return [];
    }
  }
  /**
   * Clear all caches - useful for testing
   */
  clearCache() {
    E.clear(), te.clear();
  }
}
const G = new Ao();
async function xo(e, t) {
  return G.resolveUser(e, t);
}
const Ft = ["assigned", "unassignedOrMe", "all"];
async function Qe(e, t) {
  if (!t)
    return;
  const o = await xo(e, t);
  if (!o)
    throw new Error(
      `Could not find user: "${t}". Make sure the user is a collaborator on a shared project.`
    );
  return { userId: o.userId, email: o.email };
}
function ae(e, t) {
  return t.length === 0 ? e : e.length === 0 ? t : `${e} & ${t}`;
}
function Ht({
  resolvedAssigneeId: e,
  assigneeEmail: t,
  responsibleUserFiltering: o = "unassignedOrMe"
}) {
  return e && t ? `assigned to: ${t}` : o === "unassignedOrMe" ? "!assigned to: others" : o === "assigned" ? "assigned to: others" : "";
}
function Eo({
  tasks: e,
  resolvedAssigneeId: t,
  currentUserId: o,
  responsibleUserFiltering: r = "unassignedOrMe"
}) {
  return t ? e.filter((n) => n.responsibleUid === t) : r === "unassignedOrMe" ? e.filter((n) => !n.responsibleUid || n.responsibleUid === o) : r === "assigned" ? e.filter((n) => n.responsibleUid && n.responsibleUid !== o) : e;
}
function fe(e) {
  return e?.toLowerCase() === "inbox";
}
async function K(e) {
  const { projectId: t, user: o, client: r } = e;
  if (!fe(t))
    return t;
  const n = o || (r ? await r.getUser() : null);
  if (!n)
    throw new Error("Either user or client must be provided when resolving inbox project ID");
  return n.inboxProjectId;
}
async function L(e) {
  const { apiMethod: t, args: o, limit: r = 100 } = e, n = [];
  let a = null;
  do {
    const i = await t({
      ...o,
      cursor: a,
      limit: r
    });
    n.push(...i.results), a = i.nextCursor ?? null;
  } while (a !== null);
  return n;
}
function et(e) {
  return /(?<!\\)(?:\\\\)*\*/.test(e) ? e : `*${e.replaceAll(/\\(?!\*)/g, "\\\\")}*`;
}
async function Wt(e, t) {
  return L({
    apiMethod: e.searchProjects.bind(e),
    args: { query: et(t) },
    limit: j.PROJECTS_MAX
  });
}
async function Oo(e) {
  return L({
    apiMethod: e.getProjects.bind(e),
    args: {},
    limit: j.PROJECTS_MAX
  });
}
async function Uo(e) {
  return L({
    apiMethod: e.getArchivedProjects.bind(e),
    args: {},
    limit: j.PROJECTS_MAX
  });
}
function Po(e) {
  const t = (a) => a.replaceAll(/[.*+?^${}()|[\]\\]/g, "\\$&"), o = /(?<!\\)(?:\\\\)*\*/.test(e), r = [...e];
  let n = "";
  for (let a = 0; a < r.length; a++) {
    const i = r[a];
    i === "\\" && r[a + 1] === "\\" ? (n += "\\\\", a++) : i === "\\" && r[a + 1] === "*" ? (n += "\\*", a++) : i === "*" ? n += ".*" : n += t(i);
  }
  return new RegExp(o ? `^${n}$` : n, "iu");
}
async function _o(e, t) {
  return L({
    apiMethod: e.searchLabels.bind(e),
    args: { query: et(t) },
    limit: j.LABELS_MAX
  });
}
async function Ro(e) {
  return L({
    apiMethod: e.getSharedLabels.bind(e),
    args: {},
    limit: j.LABELS_MAX
  });
}
async function No(e, t, o) {
  const r = et(t);
  return L({
    apiMethod: e.searchSections.bind(e),
    args: o ? { query: r, projectId: o } : { query: r },
    limit: j.SECTIONS_MAX
  });
}
function Lo(e, t, o, r) {
  const n = [t, o, r].filter(Boolean);
  if (n.length > 1)
    throw new Error(
      `Task ${e}: Only one of projectId, sectionId, or parentId can be specified at a time. The Todoist API requires exactly one destination for move operations.`
    );
  if (n.length === 0)
    throw new Error(
      `Task ${e}: At least one of projectId, sectionId, or parentId must be provided for move operations.`
    );
  if (t) return { projectId: t };
  if (o) return { sectionId: o };
  if (r) return { parentId: r };
  throw new Error("Unexpected error: No valid move parameter found");
}
function _(e) {
  return {
    id: e.id,
    content: e.content,
    description: e.description,
    dueDate: e.due?.date,
    recurring: e.due?.isRecurring && e.due.string ? e.due.string : !1,
    deadlineDate: e.deadline?.date,
    priority: $o(e.priority) ?? "p4",
    projectId: e.projectId,
    sectionId: e.sectionId ?? void 0,
    parentId: e.parentId ?? void 0,
    labels: e.labels,
    duration: e.duration ? So(e.duration.amount) : void 0,
    responsibleUid: e.responsibleUid ?? void 0,
    assignedByUid: e.assignedByUid ?? void 0,
    checked: e.checked,
    completedAt: e.completedAt?.toISOString() ?? void 0
  };
}
function V(e) {
  return {
    id: e.id,
    name: e.name,
    description: e.description,
    color: e.color,
    isFavorite: e.isFavorite,
    isShared: e.isShared,
    parentId: ie(e) ? e.parentId ?? void 0 : void 0,
    inboxProject: ie(e) ? e.inboxProject ?? !1 : !1,
    viewStyle: e.viewStyle,
    workspaceId: ke(e) ? e.workspaceId : void 0,
    folderId: ke(e) ? e.folderId ?? void 0 : void 0,
    childOrder: e.childOrder,
    isArchived: e.isArchived
  };
}
function we(e) {
  return {
    id: e.id,
    taskId: e.taskId ?? void 0,
    projectId: e.projectId ?? void 0,
    content: e.content,
    postedAt: e.postedAt.toISOString(),
    postedUid: e.postedUid ?? void 0,
    fileAttachment: e.fileAttachment ? {
      resourceType: e.fileAttachment.resourceType,
      fileName: e.fileAttachment.fileName ?? void 0,
      fileSize: e.fileAttachment.fileSize ?? void 0,
      fileType: e.fileAttachment.fileType ?? void 0,
      fileUrl: e.fileAttachment.fileUrl ?? void 0,
      fileDuration: e.fileAttachment.fileDuration ?? void 0,
      uploadState: e.fileAttachment.uploadState ?? void 0,
      url: e.fileAttachment.url ?? void 0,
      title: e.fileAttachment.title ?? void 0,
      image: e.fileAttachment.image ?? void 0,
      imageWidth: e.fileAttachment.imageWidth ?? void 0,
      imageHeight: e.fileAttachment.imageHeight ?? void 0
    } : void 0
  };
}
function ve(e) {
  return {
    id: e.id,
    name: e.name,
    ownerType: e.ownerType,
    ownerId: e.ownerId,
    description: e.description ?? void 0,
    deadline: e.deadline ?? void 0,
    responsibleUid: e.responsibleUid ?? void 0,
    isCompleted: e.isCompleted,
    progress: e.progress
  };
}
function Mo(e) {
  return {
    id: e.id ?? void 0,
    objectType: e.objectType,
    objectId: e.objectId,
    eventType: e.eventType,
    eventDate: e.eventDate.toISOString(),
    parentProjectId: e.parentProjectId ?? void 0,
    parentItemId: e.parentItemId ?? void 0,
    initiatorId: e.initiatorId ?? void 0,
    extraData: e.extraData ?? void 0
  };
}
const Fo = Q.object({
  httpStatusCode: Q.number(),
  responseData: Q.object({
    error: Q.string(),
    errorCode: Q.number(),
    errorTag: Q.string()
  })
});
async function tt({
  client: e,
  query: t,
  limit: o,
  cursor: r
}) {
  try {
    const { results: n, nextCursor: a } = await e.getTasksByFilter({ query: t, cursor: r, limit: o });
    return { tasks: n.map(_), nextCursor: a };
  } catch (n) {
    const a = Fo.safeParse(n);
    if (!a.success)
      throw n;
    const { responseData: i } = a.data;
    throw i.errorTag === "INVALID_SEARCH_QUERY" ? new Error(`Invalid filter query: ${t}`) : new Error(
      `${i.error} (tag: ${i.errorTag}, code: ${i.errorCode})`
    );
  }
}
function ht(e) {
  return {
    isRecurring: e.isRecurring,
    string: e.string,
    date: e.date,
    datetime: e.datetime ?? void 0,
    timezone: e.timezone ?? void 0
  };
}
function he(e) {
  const t = {
    id: e.id,
    taskId: e.itemId,
    type: e.type
  };
  switch (e.type) {
    case "relative":
      return {
        ...t,
        minuteOffset: e.minuteOffset,
        due: e.due ? ht(e.due) : void 0,
        isUrgent: e.isUrgent
      };
    case "absolute":
      return {
        ...t,
        due: ht(e.due),
        isUrgent: e.isUrgent
      };
    case "location":
      return {
        ...t,
        name: e.name,
        locLat: e.locLat,
        locLong: e.locLong,
        locTrigger: e.locTrigger,
        radius: e.radius
      };
  }
}
function Gt(e) {
  const t = e.filter(
    (r) => r.type === "relative" || r.type === "absolute"
  ).length, o = e.filter((r) => r.type === "location").length;
  return { timeBasedCount: t, locationCount: o };
}
const Ie = Me.map((e) => e.key);
function Ho(e) {
  if (typeof e != "string") return;
  const t = e.toLowerCase();
  return (Me.find((r) => r.key === t) ?? Me.find((r) => r.displayName.toLowerCase() === t))?.key;
}
const Wo = `Color for the entity. Accepts a color key (e.g. "berry_red") or display name (e.g. "Berry Red"). Valid colors: ${Ie.join(", ")}. Unrecognized colors are omitted and charcoal will be used as the default.`, de = s.preprocess(Ho, s.enum(Ie).optional()).describe(Wo);
s.enum(Ie).describe("The color key of the entity.");
const le = s.enum(Ie).optional().catch(void 0).describe("The color key of the entity."), J = s.object({
  id: s.string().describe("The unique ID of the task."),
  content: s.string().describe("The task title/content."),
  description: s.string().describe("The task description."),
  dueDate: s.string().optional().describe("The due date of the task (ISO 8601 format)."),
  recurring: s.union([s.boolean(), s.string()]).describe("Whether the task is recurring, or the recurrence string."),
  deadlineDate: s.string().optional().describe("The deadline date of the task (ISO 8601 format)."),
  priority: Ze.describe(
    "The priority level: p1 (highest), p2 (high), p3 (medium), p4 (lowest)."
  ),
  projectId: s.string().describe("The ID of the project this task belongs to."),
  sectionId: s.string().optional().describe("The ID of the section this task belongs to."),
  parentId: s.string().optional().describe("The ID of the parent task (for subtasks)."),
  labels: s.array(s.string()).optional().describe("The labels attached to this task."),
  duration: s.string().optional().describe('The duration of the task (e.g., "2h30m").'),
  responsibleUid: s.string().optional().describe("The UID of the user responsible for this task."),
  isUncompletable: s.boolean().optional().describe("Whether the task is uncompletable (organizational header)."),
  assignedByUid: s.string().optional().describe("The UID of the user who assigned this task."),
  checked: s.boolean().describe("Whether the task is checked/completed."),
  completedAt: s.string().optional().describe("When the task was completed (ISO 8601 format).")
}), ue = s.object({
  id: s.string().describe("The unique ID of the project."),
  name: s.string().describe("The name of the project."),
  description: s.string().describe("The description of the project (empty string if none)."),
  color: le,
  isFavorite: s.boolean().describe("Whether the project is marked as favorite."),
  isShared: s.boolean().describe("Whether the project is shared."),
  parentId: s.string().optional().describe("The ID of the parent project (for sub-projects)."),
  inboxProject: s.boolean().describe("Whether this is the inbox project."),
  viewStyle: s.string().describe("The view style of the project (list, board, calendar)."),
  workspaceId: s.string().optional().describe(
    "The ID of the workspace this project belongs to (undefined for personal projects)."
  ),
  folderId: s.string().optional().describe("The ID of the folder this project belongs to (workspace projects only)."),
  childOrder: s.number().describe("The ordering index of the project among its siblings."),
  isArchived: s.boolean().describe("Whether the project is archived.")
}), ce = s.object({
  id: s.string().describe("The unique ID of the section."),
  name: s.string().describe("The name of the section."),
  description: s.string().optional().describe("The description of the section. Supports Markdown.")
});
function X({ id: e, name: t, description: o }) {
  return { id: e, name: t, description: o ?? void 0 };
}
const Go = s.object({
  resourceType: s.string().describe("The type of resource (file, url, image, etc)."),
  fileName: s.string().optional().describe("The name of the file."),
  fileSize: s.number().optional().describe("The size of the file in bytes."),
  fileType: s.string().optional().describe("The MIME type of the file."),
  fileUrl: s.string().optional().describe("The URL to access the file."),
  fileDuration: s.number().optional().describe("The duration in milliseconds (for audio/video files)."),
  uploadState: s.enum(["pending", "completed"]).optional().describe("The upload state of the file."),
  url: s.string().optional().describe("The URL for link/url resource types."),
  title: s.string().optional().describe("The title for link/url resource types."),
  image: s.string().optional().describe("The image URL for image resource types."),
  imageWidth: s.number().optional().describe("The width of the image in pixels."),
  imageHeight: s.number().optional().describe("The height of the image in pixels.")
}), je = s.object({
  id: s.string().describe("The unique ID of the comment."),
  taskId: s.string().optional().describe("The ID of the task this comment belongs to."),
  projectId: s.string().optional().describe("The ID of the project this comment belongs to."),
  content: s.string().describe("The content of the comment."),
  postedAt: s.string().describe("When the comment was posted (ISO 8601 format)."),
  postedUid: s.string().optional().describe("The UID of the user who posted this comment."),
  fileAttachment: Go.optional().describe("File attachment information, if any.")
}), Bo = s.object({
  id: s.string().optional().describe("The unique ID of the activity event."),
  objectType: s.string().describe("The type of object this event relates to (task, project, etc)."),
  objectId: s.string().describe("The ID of the object this event relates to."),
  eventType: s.string().describe("The type of event (added, updated, deleted, completed, etc)."),
  eventDate: s.string().describe("When the event occurred (ISO 8601 format)."),
  parentProjectId: s.string().optional().describe("The ID of the parent project."),
  parentItemId: s.string().optional().describe("The ID of the parent item."),
  initiatorId: s.string().optional().describe("The ID of the user who initiated this event."),
  extraData: s.record(s.string(), s.unknown()).optional().describe("Additional event data.")
}), Yo = s.object({
  id: s.string().describe("The unique ID of the user."),
  name: s.string().describe("The full name of the user."),
  email: s.string().describe("The email address of the user.")
}), pe = s.object({
  id: s.string().describe("The unique ID of the label."),
  name: s.string().describe("The name of the label."),
  color: le,
  order: s.number().optional().catch(void 0).describe("The display order of the label."),
  isFavorite: s.boolean().describe("Whether the label is marked as favorite.")
}), qo = s.object({
  isRecurring: s.boolean().describe("Whether this is a recurring reminder."),
  string: s.string().describe("Human-readable due string."),
  date: s.string().describe("Due date in ISO format."),
  datetime: s.string().optional().describe("Due datetime in ISO format."),
  timezone: s.string().optional().describe("Timezone of the reminder.")
}), st = s.object({
  id: s.string().describe("The unique ID of the reminder."),
  taskId: s.string().describe("The task ID this reminder belongs to."),
  type: s.enum(ks).describe("The type of reminder: relative, absolute, or location."),
  minuteOffset: s.number().optional().describe("Minutes before due time to trigger (relative reminders only)."),
  due: qo.optional().describe(
    "Due date info (absolute and sometimes relative reminders)."
  ),
  name: s.string().optional().describe("Location name (location reminders only)."),
  locLat: s.string().optional().describe("Latitude (location reminders only)."),
  locLong: s.string().optional().describe("Longitude (location reminders only)."),
  locTrigger: s.enum(Ot).optional().describe("Trigger type: on_enter or on_leave (location reminders only)."),
  radius: s.number().optional().describe("Geofence radius in meters (location reminders only)."),
  isUrgent: s.boolean().optional().describe("Whether this is an urgent reminder (relative and absolute reminders only).")
}), Se = s.object({
  id: s.string().describe("The unique ID of the goal."),
  name: s.string().describe("The name of the goal."),
  ownerType: s.string().describe("The owner type: USER or WORKSPACE."),
  ownerId: s.string().describe("The owner ID (user ID or workspace ID)."),
  description: s.string().optional().describe("The description of the goal."),
  deadline: s.string().optional().describe("The deadline (YYYY-MM-DD)."),
  responsibleUid: s.string().optional().describe("The user ID responsible for this goal."),
  isCompleted: s.boolean().describe("Whether the goal is completed."),
  progress: s.object({
    totalTaskCount: s.number(),
    completedTaskCount: s.number(),
    percentage: s.number()
  }).optional().describe("Progress of linked tasks.")
}), be = s.object({
  item: s.string().describe("The item that failed (usually an ID or identifier)."),
  error: s.string().describe("The error message."),
  code: s.string().optional().describe("The error code, if available.")
}), zo = s.object({
  taskId: s.string().optional().describe("The ID of the task to comment on."),
  projectId: s.string().optional().describe(
    'The ID of the project to comment on. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  content: s.string().min(1).describe("The content of the comment.")
}), Ko = {
  comments: s.array(zo).min(1).describe("The array of comments to add.")
}, Vo = {
  comments: s.array(je).describe("The created comments."),
  totalCount: s.number().describe("The total number of comments created."),
  addedCommentIds: s.array(s.string()).describe("The IDs of the added comments.")
}, Jo = {
  name: f.ADD_COMMENTS,
  description: "Add multiple comments to tasks or projects. Each comment must specify either taskId or projectId.",
  parameters: Ko,
  outputSchema: Vo,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const { comments: o } = e;
    for (const [l, p] of o.entries()) {
      if (!p.taskId && !p.projectId)
        throw new Error(
          `Comment ${l + 1}: Either taskId or projectId must be provided.`
        );
      if (p.taskId && p.projectId)
        throw new Error(
          `Comment ${l + 1}: Cannot provide both taskId and projectId. Choose one.`
        );
    }
    const n = o.some((l) => fe(l.projectId)) ? await t.getUser() : void 0, a = o.map(async ({ content: l, taskId: p, projectId: u }) => {
      const m = await K({
        projectId: u,
        user: n,
        client: n ? void 0 : t
      });
      return await t.addComment({
        content: l,
        ...p ? { taskId: p } : { projectId: m }
      });
    }), c = (await Promise.all(a)).map(we);
    return {
      textContent: Xo({ comments: c }),
      structuredContent: {
        comments: c,
        totalCount: c.length,
        addedCommentIds: c.map((l) => l.id)
      }
    };
  }
};
function Xo({ comments: e }) {
  const t = e.filter((a) => a.taskId).length, o = e.filter((a) => a.projectId).length, r = [];
  if (t > 0) {
    const a = t > 1 ? "comments" : "comment";
    r.push(`${t} task ${a}`);
  }
  if (o > 0) {
    const a = o > 1 ? "comments" : "comment";
    r.push(`${o} project ${a}`);
  }
  return r.length > 0 ? `Added ${r.join(" and ")}` : "No comments added";
}
const Zo = {
  search: s.string().optional().describe(
    "Search for a filter by name (partial and case insensitive match). If omitted, all filters are returned."
  )
}, ot = s.object({
  id: s.string().describe("The unique ID of the filter."),
  name: s.string().describe("The name of the filter."),
  query: s.string().describe('The filter query string (e.g. "today & p1", "#Work & overdue").'),
  color: le,
  isFavorite: s.boolean().describe("Whether the filter is marked as favorite."),
  itemOrder: s.number().describe("The display order of the filter.")
}), Qo = {
  filters: s.array(ot).describe("The found filters."),
  totalCount: s.number().describe("The total number of filters returned.")
}, er = {
  name: f.FIND_FILTERS,
  description: 'List all personal filters or search for filters by name. Filters are saved custom views that use query syntax to organize tasks (e.g. "today & p1", "#Work & overdue").',
  parameters: Zo,
  outputSchema: Qo,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    let r = ((await t.sync({ resourceTypes: ["filters"], syncToken: "*" })).filters ?? []).filter((i) => !i.isDeleted);
    if (e.search) {
      const i = e.search.toLowerCase();
      r = r.filter((c) => c.name.toLowerCase().includes(i));
    }
    r.sort((i, c) => i.itemOrder - c.itemOrder);
    const n = r.map((i) => ({
      id: i.id,
      name: i.name,
      query: i.query,
      color: le.parse(i.color),
      isFavorite: i.isFavorite,
      itemOrder: i.itemOrder
    }));
    return {
      textContent: tr({ filters: n, search: e.search }),
      structuredContent: {
        filters: n,
        totalCount: n.length
      }
    };
  }
};
function tr({
  filters: e,
  search: t
}) {
  const o = t ? `Filters matching "${t}"` : "Filters";
  if (e.length === 0) {
    const n = t ? ["Try broader search terms", "Check spelling", "Remove search to see all filters"] : ["No filters created yet", `Use ${f.ADD_FILTERS} to create a filter`];
    return `${o}: 0 found

Suggestions:
${n.map((a) => `- ${a}`).join(`
`)}`;
  }
  const r = [`${o}: ${e.length} found`, ""];
  for (const n of e) {
    const a = n.isFavorite ? " ★" : "";
    r.push(`• ${n.name}${a} (id=${n.id})`), r.push(`  Query: ${n.query}`);
  }
  return r.join(`
`);
}
const sr = s.object({
  name: s.string().min(1).describe("The name of the filter."),
  query: s.string().min(1).describe(
    'The filter query string. Examples: "today & p1", "#Work & overdue", "@email & today", "(p1 | p2) & !assigned". Operators: | (OR), & (AND), ! (NOT), () grouping, , (multiple queries).'
  ),
  color: de,
  isFavorite: s.boolean().optional().describe("Whether to mark the filter as a favorite. Defaults to false.")
}), or = {
  filters: s.array(sr).min(1).describe("The array of filters to add.")
}, rr = {
  filters: s.array(ot).describe("The created filters."),
  totalCount: s.number().describe("The total number of filters created.")
}, nr = {
  name: f.ADD_FILTERS,
  description: "Add one or more new personal filters. Filters are saved custom views using query syntax to organize tasks.",
  parameters: or,
  outputSchema: rr,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ filters: e }, t) {
    const o = Date.now(), r = e.map((u, m) => `tempFilterAdd${m}${o}`), n = e.map((u, m) => q(
      "filter_add",
      {
        name: u.name,
        query: u.query,
        ...u.color !== void 0 ? { color: u.color } : {},
        ...u.isFavorite !== void 0 ? { isFavorite: u.isFavorite } : {}
      },
      r[m]
    )), i = (await t.sync({ commands: n })).tempIdMapping ?? {}, c = e.map((u, m) => {
      const h = r[m], k = h !== void 0 ? i[h] : void 0;
      if (!k) return null;
      const b = u.color !== void 0 ? le.parse(u.color) : void 0;
      return {
        id: k,
        name: u.name,
        query: u.query,
        color: b,
        isFavorite: u.isFavorite ?? !1,
        itemOrder: 0
      };
    }).filter((u) => u !== null), d = c.length, l = c.map((u) => `• ${u.name} (id=${u.id})`).join(`
`);
    return {
      textContent: `Added ${d} filter${d === 1 ? "" : "s"}:
${l}`,
      structuredContent: {
        filters: c,
        totalCount: c.length
      }
    };
  }
}, ft = 25, ar = s.object({
  name: s.string().min(1).describe("The name of the goal."),
  workspaceId: s.string().optional().describe(
    "Workspace ID. If provided, creates a workspace goal. If omitted, creates a personal goal."
  ),
  description: s.string().optional().describe("The description of the goal."),
  deadline: s.string().optional().describe("The target date (YYYY-MM-DD)."),
  responsibleUid: s.string().optional().describe("The user ID responsible for this goal.")
}), ir = {
  goals: s.array(ar).min(1).max(ft).describe(`The array of goals to create (max ${ft}).`)
}, cr = {
  goals: s.array(Se).describe("The created goals."),
  totalCount: s.number().describe("The total number of goals created.")
}, dr = {
  name: f.ADD_GOALS,
  description: "Create one or more goals. Omit workspaceId for personal goals.",
  parameters: ir,
  outputSchema: cr,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ goals: e }, t) {
    const r = (await Promise.all(e.map((c) => t.addGoal(c)))).map(ve), n = r.length, a = r.map((c) => `• ${c.name} (id=${c.id}, owner=${c.ownerType})`).join(`
`);
    return {
      textContent: `Added ${n} goal${n === 1 ? "" : "s"}:
${a}`,
      structuredContent: {
        goals: r,
        totalCount: n
      }
    };
  }
}, lr = s.object({
  name: s.string().min(1).max(128).describe("The name of the label."),
  color: de,
  order: s.number().int().optional().describe("The position of the label in the label list."),
  isFavorite: s.boolean().optional().describe("Whether the label is a favorite. Defaults to false.")
}), ur = {
  labels: s.array(lr).min(1).describe("The array of labels to add.")
}, pr = {
  labels: s.array(pe).describe("The created labels."),
  totalCount: s.number().describe("The total number of labels created.")
}, mr = {
  name: f.ADD_LABELS,
  description: "Add one or more new personal labels.",
  parameters: ur,
  outputSchema: pr,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ labels: e }, t) {
    const o = await Promise.all(e.map((n) => t.addLabel(n)));
    return {
      textContent: hr({ labels: o }),
      structuredContent: {
        labels: o.map((n) => pe.parse(n)),
        totalCount: o.length
      }
    };
  }
};
function hr({ labels: e }) {
  const t = e.length, o = e.map((r) => `• ${r.name} (id=${r.id})`).join(`
`);
  return `Added ${t} label${t === 1 ? "" : "s"}:
${o}`;
}
const fr = 300 * 1e3;
function br(e) {
  return /^\d+$/.test(e);
}
class gr {
  constructor() {
    this.cache = null;
  }
  async getWorkspaces(t) {
    if (this.cache && Date.now() - this.cache.timestamp < fr)
      return this.cache.workspaces;
    const o = await t.getWorkspaces();
    return this.cache = { workspaces: o, timestamp: Date.now() }, o;
  }
  /**
   * Resolve a workspace name or ID to a workspace ID and name.
   *
   * Resolution order:
   * 1. Exact ID match (always tried first for any input)
   * 2. If input looks like an ID (numeric) but wasn't found, pass through as-is (API validates)
   * 3. Exact case-insensitive name match
   * 4. Unique partial case-insensitive name match
   * 5. Multiple partial matches → throw ambiguous error
   * 6. No match → throw not-found error
   */
  async resolveWorkspace(t, o) {
    const r = o.trim();
    if (!r)
      throw new Error("Workspace reference cannot be empty");
    const n = await this.getWorkspaces(t), a = n.find((p) => p.id === r);
    if (a)
      return { workspaceId: a.id, workspaceName: a.name };
    if (br(r))
      return { workspaceId: r, workspaceName: r };
    const i = r.toLowerCase(), c = n.find((p) => p.name.toLowerCase() === i);
    if (c)
      return { workspaceId: c.id, workspaceName: c.name };
    const d = n.filter((p) => p.name.toLowerCase().includes(i)), l = d.length === 1 ? d[0] : void 0;
    if (l)
      return { workspaceId: l.id, workspaceName: l.name };
    if (d.length > 1) {
      const p = d.slice(0, 5).map((u) => `  - "${u.name}" (id: ${u.id})`).join(`
`);
      throw new Error(
        `Ambiguous workspace reference "${r}". Multiple workspaces match:
${p}` + (d.length > 5 ? `
  ... and ${d.length - 5} more` : "")
      );
    }
    throw new Error(
      `Workspace "${r}" not found. Use list-workspaces to see available workspaces.`
    );
  }
  /**
   * Clear the workspace cache — useful for testing.
   */
  clearCache() {
    this.cache = null;
  }
}
const Bt = new gr(), yr = s.object({
  name: s.string().min(1).describe("The name of the project."),
  parentId: s.string().optional().describe("The ID of the parent project. If provided, creates this as a sub-project."),
  isFavorite: s.boolean().optional().describe("Whether the project is a favorite. Defaults to false."),
  viewStyle: s.enum(["list", "board", "calendar"]).optional().describe('The project view style. Defaults to "list".'),
  description: s.string().optional().describe("The description of the project. Supports Markdown."),
  color: de,
  workspace: s.string().trim().min(1).optional().describe(
    "The workspace to create the project in. Accepts a workspace name or workspace ID. If not provided, creates a personal project. Use list-workspaces to see available workspaces."
  )
}), kr = {
  projects: s.array(yr).min(1).describe("The array of projects to add.")
}, Tr = {
  projects: s.array(ue).describe("The created projects."),
  totalCount: s.number().describe("The total number of projects created.")
}, wr = {
  name: f.ADD_PROJECTS,
  description: "Add one or more new projects.",
  parameters: kr,
  outputSchema: Tr,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ projects: e }, t) {
    const o = [
      ...new Set(e.map((c) => c.workspace).filter(Boolean))
    ], r = /* @__PURE__ */ new Map();
    for (const c of o) {
      const d = await Bt.resolveWorkspace(t, c);
      r.set(c, d.workspaceId);
    }
    const n = await Promise.all(
      e.map(({ workspace: c, ...d }) => {
        const l = c ? r.get(c) : void 0;
        return t.addProject({ ...d, ...l ? { workspaceId: l } : {} });
      })
    ), a = vr({ projects: n }), i = n.map(V);
    return {
      textContent: a,
      structuredContent: {
        projects: i,
        totalCount: i.length
      }
    };
  }
};
function vr({ projects: e }) {
  const t = e.length, o = e.map((n) => `• ${n.name} (id=${n.id})`).join(`
`);
  return `Added ${t} project${t === 1 ? "" : "s"}:
${o}`;
}
const Te = 25, Ce = s.enum(Ts), Yt = s.object({
  date: s.string().optional().describe("Due date in YYYY-MM-DD format."),
  string: s.string().optional().describe('Natural language due string, e.g. "tomorrow at 3pm".'),
  timezone: s.string().optional().describe('Timezone for the reminder, e.g. "America/New_York".'),
  lang: s.string().optional().describe('Language for parsing the due string, e.g. "en".')
}), qt = s.enum(Ot), $e = s.boolean().optional().describe("Whether this is an urgent reminder. Applies to relative and absolute reminders."), Ir = s.object({
  type: s.literal("relative"),
  taskId: s.string().min(1).describe("The ID of the task to set a reminder for."),
  minuteOffset: s.number().int().min(0).describe(
    "Minutes before the task due time to trigger the reminder. E.g., 30 for 30 minutes before, 60 for 1 hour before, 1440 for 1 day before."
  ),
  service: Ce.optional().describe(
    'Delivery method: "email" or "push" notification. Defaults to push.'
  ),
  isUrgent: $e
}), jr = s.object({
  type: s.literal("absolute"),
  taskId: s.string().min(1).describe("The ID of the task to set a reminder for."),
  due: Yt.describe("The specific date/time for the reminder."),
  service: Ce.optional().describe(
    'Delivery method: "email" or "push" notification. Defaults to push.'
  ),
  isUrgent: $e
}), Sr = s.object({
  type: s.literal("location"),
  taskId: s.string().min(1).describe("The ID of the task to set a reminder for."),
  name: s.string().min(1).describe('Name of the location, e.g. "Office", "Home".'),
  locLat: s.string().describe('Latitude of the location as a string, e.g. "37.7749".'),
  locLong: s.string().describe('Longitude of the location as a string, e.g. "-122.4194".'),
  locTrigger: qt.describe(
    'When to trigger: "on_enter" (arriving) or "on_leave" (departing).'
  ),
  radius: s.number().int().optional().describe("Radius in meters for the geofence. Defaults to server default.")
}), Cr = s.discriminatedUnion("type", [
  Ir,
  jr,
  Sr
]), $r = {
  reminders: s.array(Cr).min(1).max(Te).describe(
    `Array of reminders to create (max ${Te}). Each reminder must specify a type: "relative" (minutes before due), "absolute" (specific date/time), or "location" (geofence trigger).`
  )
}, Dr = {
  reminders: s.array(st).describe("The created reminders."),
  totalCount: s.number().describe("Total number of reminders created."),
  addedReminderIds: s.array(s.string()).describe("IDs of the created reminders.")
}, Ar = {
  name: f.ADD_REMINDERS,
  description: 'Add reminders to tasks. Supports three types: "relative" (minutes before due), "absolute" (specific date/time), or "location" (geofence-triggered). Each reminder must specify a taskId.',
  parameters: $r,
  outputSchema: Dr,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const { reminders: o } = e, r = o.map(async (c) => {
      switch (c.type) {
        case "relative":
          return await t.addReminder({
            taskId: c.taskId,
            reminderType: "relative",
            minuteOffset: c.minuteOffset,
            service: c.service,
            isUrgent: c.isUrgent
          });
        case "absolute":
          return await t.addReminder({
            taskId: c.taskId,
            reminderType: "absolute",
            due: c.due,
            service: c.service,
            isUrgent: c.isUrgent
          });
        case "location":
          return await t.addLocationReminder({
            taskId: c.taskId,
            name: c.name,
            locLat: c.locLat,
            locLong: c.locLong,
            locTrigger: c.locTrigger,
            radius: c.radius
          });
      }
    }), a = (await Promise.all(r)).map(he);
    return {
      textContent: xr(a),
      structuredContent: {
        reminders: a,
        totalCount: a.length,
        addedReminderIds: a.map((c) => c.id)
      }
    };
  }
};
function xr(e) {
  const { timeBasedCount: t, locationCount: o } = Gt(e), r = [];
  if (t > 0) {
    const n = t > 1 ? "reminders" : "reminder";
    r.push(`${t} time-based ${n}`);
  }
  if (o > 0) {
    const n = o > 1 ? "reminders" : "reminder";
    r.push(`${o} location ${n}`);
  }
  return r.length > 0 ? `Added ${r.join(" and ")}` : "No reminders added";
}
const Er = s.object({
  name: s.string().min(1).describe("The name of the section."),
  projectId: s.string().min(1).describe(
    'The ID of the project to add the section to. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  description: s.string().optional().describe("The description of the section. Supports Markdown.")
}), Or = {
  sections: s.array(Er).min(1).describe("The array of sections to add.")
}, Ur = {
  sections: s.array(ce).describe("The created sections."),
  totalCount: s.number().describe("The total number of sections created.")
}, Pr = {
  name: f.ADD_SECTIONS,
  description: "Add one or more new sections to projects.",
  parameters: Or,
  outputSchema: Ur,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute({ sections: e }, t) {
    const r = e.some((c) => fe(c.projectId)) ? await t.getUser() : void 0, n = await Promise.all(
      e.map(async (c) => ({
        ...c,
        projectId: await K({
          projectId: c.projectId,
          user: r,
          client: r ? void 0 : t
        }) ?? c.projectId
      }))
    ), a = await Promise.all(
      n.map((c) => t.addSection(c))
    );
    return {
      textContent: _r({ sections: a }),
      structuredContent: {
        sections: a.map(X),
        totalCount: a.length
      }
    };
  }
};
function _r({ sections: e }) {
  const t = e.length, o = e.map((n) => `• ${n.name} (id=${n.id}, projectId=${n.projectId})`).join(`
`);
  return `Added ${t} section${t === 1 ? "" : "s"}:
${o}`;
}
const se = {
  USER_NOT_FOUND: "USER_NOT_FOUND",
  USER_NOT_COLLABORATOR: "USER_NOT_COLLABORATOR",
  PROJECT_NOT_SHARED: "PROJECT_NOT_SHARED",
  TASK_NOT_ACCESSIBLE: "TASK_NOT_ACCESSIBLE",
  PERMISSION_DENIED: "PERMISSION_DENIED",
  PROJECT_NOT_FOUND: "PROJECT_NOT_FOUND",
  TASK_NOT_FOUND: "TASK_NOT_FOUND"
};
class Rr {
  /**
   * Validate a single assignment operation
   */
  async validateAssignment(t, o) {
    const { taskId: r, projectId: n, responsibleUid: a } = o, i = await G.resolveUser(t, a);
    if (!i)
      return {
        isValid: !1,
        taskId: r,
        projectId: n,
        error: {
          type: se.USER_NOT_FOUND,
          message: `User "${a}" not found`,
          suggestions: [
            "Check the spelling of the user name or email",
            "Ensure the user is a collaborator on at least one shared project",
            "Try using the user's full email address"
          ]
        }
      };
    try {
      const c = await t.getProject(n);
      if (!c.isShared)
        return {
          isValid: !1,
          taskId: r,
          projectId: n,
          resolvedUser: i,
          error: {
            type: se.PROJECT_NOT_SHARED,
            message: `Project "${c.name}" is not shared. Task assignments require a shared project with collaborators. Share the project with at least one other person to enable assignments.`,
            suggestions: [
              "Share the project with at least one collaborator to enable assignments",
              "Use find-project-collaborators to check project sharing status"
            ]
          }
        };
      if (!await G.validateProjectCollaborator(
        t,
        n,
        i.userId
      ))
        return {
          isValid: !1,
          taskId: r,
          projectId: n,
          resolvedUser: i,
          error: {
            type: se.USER_NOT_COLLABORATOR,
            message: `User "${i.displayName}" is not a collaborator on project "${c.name}"`,
            suggestions: [
              "Invite the user to collaborate on this project first",
              "Check if the user has been removed from the project",
              "Verify you have permission to see project collaborators"
            ]
          }
        };
      if (r)
        try {
          await t.getTask(r);
        } catch {
          return {
            isValid: !1,
            taskId: r,
            projectId: n,
            resolvedUser: i,
            error: {
              type: se.TASK_NOT_FOUND,
              message: `Task "${r}" not found or not accessible`,
              suggestions: [
                "Verify the task ID is correct",
                "Check if the task has been deleted",
                "Ensure you have access to the task"
              ]
            }
          };
        }
      return {
        isValid: !0,
        taskId: r,
        projectId: n,
        resolvedUser: i
      };
    } catch {
      return {
        isValid: !1,
        taskId: r,
        projectId: n,
        resolvedUser: i,
        error: {
          type: se.PERMISSION_DENIED,
          message: "Permission denied or API error occurred",
          suggestions: [
            "Check your API permissions",
            "Verify you have access to the project",
            "Try again later if this is a temporary API issue"
          ]
        }
      };
    }
  }
  /**
   * Validate multiple assignment operations in bulk
   */
  async validateBulkAssignment(t, o) {
    const r = o.map(
      (n) => this.validateAssignment(t, n)
    );
    return Promise.all(r);
  }
  /**
   * Validate assignment for task creation (no taskId required)
   */
  async validateTaskCreationAssignment(t, o, r) {
    return this.validateAssignment(t, {
      projectId: o,
      responsibleUid: r
    });
  }
  /**
   * Validate assignment for task update
   */
  async validateTaskUpdateAssignment(t, o, r) {
    if (r === null)
      return {
        isValid: !0,
        taskId: o
      };
    try {
      const n = await t.getTask(o);
      return this.validateAssignment(t, {
        taskId: o,
        projectId: n.projectId,
        responsibleUid: r
      });
    } catch {
      return {
        isValid: !1,
        taskId: o,
        error: {
          type: se.TASK_NOT_FOUND,
          message: `Task "${o}" not found or not accessible`,
          suggestions: [
            "Verify the task ID is correct",
            "Check if the task has been deleted",
            "Ensure you have access to the task"
          ]
        }
      };
    }
  }
  /**
   * Get detailed assignment eligibility information for troubleshooting
   */
  async getAssignmentEligibility(t, o, r, n) {
    const a = [];
    let i = !1, c;
    try {
      c = await t.getProject(o);
    } catch {
      return {
        canAssign: !1,
        projectInfo: {
          name: "Unknown",
          isShared: !1,
          collaboratorCount: 0
        },
        recommendations: ["Project not found or not accessible"]
      };
    }
    const d = await G.getProjectCollaborators(t, o), l = {
      name: c.name,
      isShared: c.isShared,
      collaboratorCount: d.length
    };
    if (!c.isShared)
      return a.push("Share this project to enable task assignments"), { canAssign: !1, projectInfo: l, recommendations: a };
    const p = await G.resolveUser(t, r);
    if (!p)
      return a.push("User not found - check spelling or invite to a shared project"), { canAssign: !1, projectInfo: l, recommendations: a };
    const u = d.some((k) => k.id === p.userId), m = {
      resolvedName: p.displayName,
      isCollaborator: u
    };
    if (!u)
      return a.push(
        `Invite ${p.displayName} to collaborate on project "${c.name}"`
      ), { canAssign: !1, projectInfo: l, userInfo: m, recommendations: a };
    let h;
    if (n && n.length > 0) {
      let k = 0, b = 0;
      for (const C of n)
        try {
          await t.getTask(C), k++;
        } catch {
          b++;
        }
      h = { accessibleTasks: k, inaccessibleTasks: b }, b > 0 && a.push(`${b} task(s) are not accessible`);
    }
    return i = !0, a.push(`Ready to assign tasks to ${p.displayName}`), {
      canAssign: i,
      projectInfo: l,
      userInfo: m,
      taskInfo: h,
      recommendations: a
    };
  }
}
const rt = new Rr();
function re(e = /* @__PURE__ */ new Date()) {
  const t = e.getFullYear(), o = String(e.getMonth() + 1).padStart(2, "0"), r = String(e.getDate()).padStart(2, "0");
  return `${t}-${o}-${r}`;
}
function nt(e, t, o = {}) {
  const { context: r, showDetails: n = !1 } = o, a = t.length, i = [], d = `${e} ${a} ${a === 1 ? "task" : "tasks"}${r ? ` ${r}` : ""}.`;
  i.push(d);
  const l = 5;
  if (n || a <= l) {
    const p = De(t, l);
    if (p.length > 0) {
      const u = a > l ? `, +${a - l} more` : "";
      i.push(`Tasks:
${p}${u}.`);
    }
  }
  return i.join(`
`);
}
function ge(e) {
  const { action: t, success: o, total: r, successItems: n, successLabel: a = "Completed", failures: i } = e, c = [], d = `${t}: ${o}/${r} successful.`;
  if (c.push(d), n?.length && n.length <= 5 && c.push(`${a}:
${n.map((l) => `    ${l}`).join(`
`)}.`), i?.length) {
    const l = i.length, p = `Failed (${l}):
${i.slice(0, Oe.MAX_FAILURES_SHOWN).map((u) => `    ${u.item} (Error: ${u.error}${u.code ? ` [${u.code}]` : ""})`).join(
      `
`
    )}${l > Oe.MAX_FAILURES_SHOWN ? `, +${l - Oe.MAX_FAILURES_SHOWN} more` : ""}.`;
    c.push(p);
  }
  return c.join(`
`);
}
function Nr(e) {
  const t = e.content || e.title || "Untitled", o = e.dueDate ? ` • due ${e.dueDate}` : "", r = e.priority ? ` • ${e.priority.toUpperCase()}` : "", n = e.projectName ? ` • ${e.projectName}` : "", a = e.id ? ` • id=${e.id}` : "";
  return `    ${t}${o}${r}${n}${a}`;
}
function Lr(e) {
  const t = ` (${e.color})`, o = e.isFavorite ? " • ⭐" : "", r = ` • id=${e.id}`;
  return `    ${e.name}${t}${o}${r}`;
}
function Mr(e) {
  const t = e.inboxProject ? " • Inbox" : "", o = e.isFavorite ? " • ⭐" : "", r = e.isShared ? " • Shared" : "", n = e.isArchived ? " • Archived" : "", a = e.viewStyle && e.viewStyle !== "list" ? ` • ${e.viewStyle}` : "", i = ` • id=${e.id}`;
  return `    ${e.name}${t}${o}${r}${n}${a}${i}`;
}
function De(e, t = 5) {
  const r = e.slice(0, t).map(Nr).join(`
`);
  if (e.length > t) {
    const n = e.length - t;
    return `${r}
    ... and ${n} more task${n === 1 ? "" : "s"}`;
  }
  return r;
}
function M({
  subject: e,
  count: t,
  limit: o,
  nextCursor: r,
  filterHints: n,
  previewLines: a,
  zeroReasonHints: i,
  nextSteps: c
}) {
  const d = [], l = `${e}: ${t}${typeof o == "number" ? ` (limit ${o})` : ""}${r ? ", more available" : ""}.`;
  return d.push(l), n?.length && d.push(`Filter: ${n.join("; ")}.`), a?.length && d.push(`Preview:
${a}`), !t && i?.length && d.push(`No results. ${i.join("; ")}.`), (c?.length || r) && d.push(zt(c || [], r)), d.join(`
`);
}
function zt(e, t) {
  const o = [...e];
  return t && o.push(`Pass cursor '${t}' to fetch more results.`), `${o.length === 1 ? "Possible suggested next step:" : "Possible suggested next steps:"}
${o.map((n) => `- ${n}`).join(`
`)}`;
}
function W(e) {
  return s.string().transform((t) => t === "" ? void 0 : t).optional().describe(e);
}
const bt = 25, Fr = s.object({
  content: s.string().min(1).describe(
    'The task name/title. Should be concise and actionable (e.g., "Review PR #123", "Call dentist"). For longer content, use the description field instead. Supports Markdown.'
  ),
  description: W(
    "Additional details, notes, or context for the task. Use this for longer content rather than putting it in the task name. Supports Markdown."
  ),
  priority: Ze.optional().describe(Xe),
  dueString: W("The due date for the task, in natural language."),
  deadlineDate: W(
    'The deadline date for the task in ISO 8601 format (YYYY-MM-DD, e.g., "2025-12-31"). Deadlines are immovable constraints shown with a different indicator than due dates.'
  ),
  duration: W(
    'The duration of the task. Use format: "2h" (hours), "90m" (minutes), "2h30m" (combined), or "1.5h" (decimal hours). Max 24h.'
  ),
  labels: s.array(s.string()).optional().describe("The labels to attach to the task."),
  projectId: W(
    'The project ID to add this task to. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: W("The section ID to add this task to."),
  parentId: W("The parent task ID (for subtasks)."),
  order: s.number().optional().describe("Position of the task among sibling tasks under the same parent/section."),
  responsibleUser: W(
    'Assign task to this user. Can be "me" (assigns to current user), a user ID, name, or email address. User must be a collaborator on the target project.'
  ),
  isUncompletable: s.boolean().optional().describe(
    "Whether this task should be uncompletable (organizational header). Tasks with isUncompletable: true appear as organizational headers and cannot be completed."
  )
}), gt = {
  tasks: s.array(Fr).min(1).max(bt).describe(`The array of tasks to add (max ${bt}).`)
}, Hr = {
  tasks: s.array(J).describe("The created tasks."),
  totalCount: s.number().describe("The total number of tasks created."),
  failures: s.array(be).describe("Failed task creations with error details."),
  totalRequested: s.number().describe("The total number of tasks requested."),
  successCount: s.number().describe("The number of successfully created tasks."),
  failureCount: s.number().describe("The number of failed task creations.")
}, Wr = {
  name: f.ADD_TASKS,
  description: "Add one or more tasks to a project, section, or parent. Supports assignment to project collaborators.",
  parameters: gt,
  outputSchema: Hr,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const { tasks: o } = s.object(gt).parse(e), r = /* @__PURE__ */ new Map();
    o.forEach((u, m) => {
      const h = Gr(u), k = r.get(h);
      k ? k.push({ task: u, index: m }) : r.set(h, [{ task: u, index: m }]);
    });
    const n = /* @__PURE__ */ new Map(), i = (await Promise.all(
      [...r.values()].map(async (u) => {
        const m = [];
        for (const { task: h, index: k } of u)
          try {
            const b = await Br(h, t, n);
            m.push({ index: k, result: { status: "fulfilled", value: b } });
          } catch (b) {
            m.push({
              index: k,
              result: { status: "rejected", reason: b }
            });
          }
        return m;
      })
    )).flat().sort((u, m) => u.index - m.index), c = [], d = [];
    for (const { index: u, result: m } of i)
      m.status === "fulfilled" ? c.push(m.value) : d.push({
        item: o[u]?.content ?? `Task ${u + 1}`,
        error: m.reason instanceof Error ? m.reason.message : String(m.reason)
      });
    if (c.length === 0 && d.length > 0) {
      const u = d.map((m) => `"${m.item}": ${m.error}`).join("; ");
      throw new Error(`All ${d.length} task(s) failed to create: ${u}`);
    }
    const l = c.map(_);
    return {
      textContent: Yr({
        tasks: l,
        failures: d,
        args: { tasks: o }
      }),
      structuredContent: {
        tasks: l,
        totalCount: l.length,
        failures: d,
        totalRequested: o.length,
        successCount: c.length,
        failureCount: d.length
      }
    };
  }
};
function Gr(e) {
  return `${e.projectId ?? ""}|${e.sectionId ?? ""}|${e.parentId ?? ""}`;
}
async function Br(e, t, o) {
  const {
    duration: r,
    projectId: n,
    sectionId: a,
    parentId: i,
    order: c,
    responsibleUser: d,
    priority: l,
    labels: p,
    deadlineDate: u,
    ...m
  } = e, h = fe(n) ? void 0 : n;
  if (h) {
    const b = await t.getProject(h);
    if (b.isArchived)
      throw new Error(
        `Task "${e.content}": Cannot create task in archived project "${b.name}"`
      );
  }
  let k = {
    ...m,
    projectId: h,
    sectionId: a,
    parentId: i,
    order: c,
    labels: p,
    deadlineDate: u
  };
  if (l && (k.priority = Mt(l)), d && !h && !a && !i)
    throw new Error(
      `Task "${e.content}": Cannot assign tasks without specifying project context. Please specify a projectId, sectionId, or parentId.`
    );
  if (r)
    try {
      const { minutes: b } = Lt(r);
      k = {
        ...k,
        duration: b,
        durationUnit: "minute"
      };
    } catch (b) {
      throw b instanceof U ? new Error(`Task "${e.content}": ${b.message}`) : b;
    }
  if (d) {
    let b = h;
    if (!b && i)
      try {
        b = (await t.getTask(i)).projectId;
      } catch {
        throw new Error(`Task "${e.content}": Parent task "${i}" not found`);
      }
    else if (!b && a && (b = o.get(a), !b)) {
      const I = await t.getSection(a);
      if (!I)
        throw new Error(`Task "${e.content}": Section "${a}" not found`);
      b = I.projectId, o.set(a, I.projectId);
    }
    if (!b)
      throw new Error(
        `Task "${e.content}": Cannot determine target project for assignment validation`
      );
    const C = await rt.validateTaskCreationAssignment(
      t,
      b,
      d
    );
    if (!C.isValid) {
      const I = C.error?.message || "Assignment validation failed", x = C.error?.suggestions?.join(". ") || "";
      throw new Error(
        `Task "${e.content}": ${I}${x ? `. ${x}` : ""}`
      );
    }
    k.assigneeId = C.resolvedUser?.userId;
  }
  return await t.addTask(k);
}
function Yr({
  tasks: e,
  failures: t,
  args: o
}) {
  const r = /* @__PURE__ */ new Set();
  for (const a of o.tasks)
    a.projectId && !fe(a.projectId) ? r.add("projects") : a.sectionId ? r.add("sections") : a.parentId ? r.add("subtasks") : r.add("inbox");
  let n = "";
  if (r.size === 1) {
    const a = Array.from(r)[0];
    n = a === "inbox" ? "" : `to ${a}`;
  } else r.size > 1 && (n = "to multiple contexts");
  return t.length > 0 ? ge({
    action: `Added tasks${n ? ` ${n}` : ""}`,
    success: e.length,
    total: o.tasks.length,
    successItems: e.map((a) => a.content ?? "Untitled"),
    successLabel: "Created",
    failures: t
  }) : nt("Added", e, {
    context: n,
    showDetails: !0
  });
}
const qr = {
  projectId: s.string().min(1).describe(
    "The ID of the project to analyze. This triggers a new health analysis which may take some time to complete."
  )
}, zr = {
  projectId: s.string().describe("The project ID."),
  health: s.object({
    status: s.enum(Ve).describe("The health status after triggering analysis."),
    isStale: s.boolean().describe("Whether the health data is still stale after the request."),
    updateInProgress: s.boolean().describe("Whether an analysis update is currently in progress.")
  }).describe("The health response returned after triggering analysis."),
  message: s.string().describe("A human-readable message about the analysis status.")
}, Kr = {
  name: f.ANALYZE_PROJECT_HEALTH,
  description: "Trigger a new health analysis for a project. Use this when the health data is stale or you want a fresh assessment. The analysis may take time to complete — use get-project-health afterward to see updated results.",
  parameters: qr,
  outputSchema: zr,
  annotations: {
    readOnlyHint: !1,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { projectId: o } = e, r = await t.analyzeProjectHealth(o), n = r.updateInProgress ? "Health analysis is in progress. Use get-project-health shortly to see updated results." : `Health analysis complete. Current status: ${r.status}.`;
    return {
      textContent: `# Analyze Project Health

${n}`,
      structuredContent: {
        projectId: o,
        health: {
          status: r.status,
          isStale: r.isStale,
          updateInProgress: r.updateInProgress
        },
        message: n
      }
    };
  }
}, yt = 25, Vr = {
  ids: s.array(s.string().min(1)).min(1).max(yt).describe(
    `The IDs of the goals to complete or uncomplete (max ${yt}).`
  ),
  action: s.enum(["complete", "uncomplete"]).describe("Whether to complete or uncomplete the goals.")
}, Jr = {
  processed: s.array(s.string()).describe("The IDs of successfully processed goals."),
  failures: s.array(be).describe("Failed operations with error details."),
  totalRequested: s.number().describe("The total number of goals requested."),
  successCount: s.number().describe("The number of successfully processed goals."),
  failureCount: s.number().describe("The number of failed operations.")
}, Xr = {
  name: f.COMPLETE_GOALS,
  description: "Complete or uncomplete one or more goals by their IDs.",
  parameters: Vr,
  outputSchema: Jr,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const o = await Promise.allSettled(
      e.ids.map(async (c) => (e.action === "complete" ? await t.completeGoal(c) : await t.uncompleteGoal(c), c))
    ), r = [], n = [];
    o.forEach((c, d) => {
      const l = e.ids[d];
      if (c.status === "fulfilled")
        r.push(l);
      else {
        const p = c.reason instanceof Error ? c.reason.message : "Unknown error";
        n.push({ item: l, error: p });
      }
    });
    const a = e.action === "complete" ? "Completed goals" : "Reopened goals";
    return {
      textContent: ge({
        action: a,
        success: r.length,
        total: e.ids.length,
        successItems: r,
        failures: n
      }),
      structuredContent: {
        processed: r,
        failures: n,
        totalRequested: e.ids.length,
        successCount: r.length,
        failureCount: n.length
      }
    };
  }
}, Zr = {
  ids: s.array(s.string().min(1)).min(1).describe("The IDs of the tasks to complete.")
}, Qr = {
  completed: s.array(s.string()).describe("The IDs of successfully completed tasks."),
  failures: s.array(be).describe("Failed task completions with error details."),
  totalRequested: s.number().describe("The total number of tasks requested to complete."),
  successCount: s.number().describe("The number of successfully completed tasks."),
  failureCount: s.number().describe("The number of failed task completions.")
}, en = {
  name: f.COMPLETE_TASKS,
  description: "Complete one or more tasks by their IDs.",
  parameters: Zr,
  outputSchema: Qr,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const o = [], r = [];
    for (const a of e.ids)
      try {
        await t.closeTask(a), o.push(a);
      } catch (i) {
        const c = i instanceof Error ? i.message : "Unknown error";
        r.push({
          item: a,
          error: c
        });
      }
    return {
      textContent: tn({
        completed: o,
        failures: r,
        args: e
      }),
      structuredContent: {
        completed: o,
        failures: r,
        totalRequested: e.ids.length,
        successCount: o.length,
        failureCount: r.length
      }
    };
  }
};
function tn({
  completed: e,
  failures: t,
  args: o
}) {
  return ge({
    action: "Completed tasks",
    success: e.length,
    total: o.ids.length,
    successItems: e,
    failures: t
  });
}
const Kt = [
  "project",
  "section",
  "task",
  "comment",
  "label",
  "filter",
  "goal",
  "reminder",
  "location_reminder"
], sn = {
  type: s.enum(Kt).describe("The type of entity to delete."),
  id: s.string().min(1).describe("The ID of the entity to delete.")
}, on = {
  deletedEntity: s.object({
    type: s.enum(Kt).describe("The type of deleted entity."),
    id: s.string().describe("The ID of the deleted entity.")
  }).describe("Information about the deleted entity."),
  success: s.boolean().describe("Whether the deletion was successful.")
}, rn = {
  name: f.DELETE_OBJECT,
  description: "Delete a project, section, task, comment, label, filter, goal, reminder, or location_reminder by its ID. Projects can be deleted whether active or archived (find archived ones via find-projects with archivedStatus); note a workspace project must be archived before it can be deleted, while personal projects can be deleted regardless.",
  parameters: sn,
  outputSchema: on,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !0 },
  async execute(e, t) {
    switch (e.type) {
      case "project": {
        const o = await t.getProject(e.id);
        if (ke(o) && !o.isArchived)
          throw new Error(
            `Workspace project "${o.name}" must be archived before it can be deleted. Archive the project first, then delete it.`
          );
        await t.deleteProject(e.id);
        break;
      }
      case "section":
        await t.deleteSection(e.id);
        break;
      case "task":
        await t.deleteTask(e.id);
        break;
      case "comment":
        await t.deleteComment(e.id);
        break;
      case "label":
        await t.deleteLabel(e.id);
        break;
      case "filter":
        await t.sync({
          commands: [q("filter_delete", { id: e.id })]
        });
        break;
      case "goal":
        await t.deleteGoal(e.id);
        break;
      case "reminder":
        await t.deleteReminder(e.id);
        break;
      case "location_reminder":
        await t.deleteLocationReminder(e.id);
        break;
    }
    return {
      textContent: `Deleted ${e.type}: id=${e.id}`,
      structuredContent: {
        deletedEntity: { type: e.type, id: e.id },
        success: !0
      }
    };
  }
}, Vt = ["task", "project", "comment", "section", "goal"], nn = {
  type: s.enum(Vt).describe("The type of object to fetch."),
  id: s.string().min(1).describe("The unique ID of the object to fetch.")
}, an = {
  type: s.enum(Vt).describe("The type of object fetched."),
  id: s.string().describe("The ID of the fetched object."),
  object: s.union([J, ue, je, ce, Se]).describe("The fetched object data.")
}, cn = {
  name: f.FETCH_OBJECT,
  description: "Fetch a single task, project, comment, section, or goal by its ID. Use this when you have a specific object ID and want to retrieve its full details.",
  parameters: nn,
  outputSchema: an,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { type: o, id: r } = e;
    try {
      switch (o) {
        case "task": {
          const n = await t.getTask(r), a = _(n);
          return {
            textContent: `Found task: ${a.content} • id=${a.id} • priority=${a.priority} • project=${a.projectId}`,
            structuredContent: {
              type: o,
              id: r,
              object: a
            }
          };
        }
        case "project": {
          const n = await t.getProject(r), a = V(n);
          return {
            textContent: `Found project: ${a.name} • id=${a.id} • color=${a.color} • viewStyle=${a.viewStyle}`,
            structuredContent: {
              type: o,
              id: r,
              object: a
            }
          };
        }
        case "comment": {
          const n = await t.getComment(r), a = we(n), i = a.content.length > 50 ? `${a.content.substring(0, 50)}...` : a.content;
          return {
            textContent: `Found comment • id=${a.id} • content="${i}" • posted=${a.postedAt}`,
            structuredContent: {
              type: o,
              id: r,
              object: a
            }
          };
        }
        case "section": {
          const n = await t.getSection(r);
          if (!n)
            throw new Error(`Section ${r} not found.`);
          const a = X(n);
          return {
            textContent: `Found section: ${a.name} • id=${a.id}`,
            structuredContent: {
              type: o,
              id: r,
              object: a
            }
          };
        }
        case "goal": {
          const n = await t.getGoal(r), a = ve(n);
          return {
            textContent: `Found goal: ${a.name} • id=${a.id} • progress=${a.progress?.percentage ?? 0}%`,
            structuredContent: {
              type: o,
              id: r,
              object: a
            }
          };
        }
      }
    } catch (n) {
      throw new Error(
        `Failed to fetch ${o} with id ${r}: ${n instanceof Error ? n.message : String(n)}`
      );
    }
  }
}, dn = {
  id: s.string().min(1).describe(
    'A unique identifier for the document in the format "task:{id}" or "project:{id}".'
  )
}, ln = {
  id: s.string().describe("The ID of the fetched document."),
  title: s.string().describe("The title of the document."),
  text: s.string().describe("The text content of the document."),
  url: s.string().describe("The URL of the document."),
  metadata: s.record(s.string(), s.unknown()).optional().describe("Additional metadata about the document.")
}, un = {
  name: f.FETCH,
  description: 'Fetch the full contents of a task or project by its ID. The ID should be in the format "task:{id}" or "project:{id}".',
  parameters: dn,
  outputSchema: ln,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { id: o } = e, [r, n] = o.split(":", 2);
    if (!n || r !== "task" && r !== "project")
      throw new Error(
        'Invalid ID format. Expected "task:{id}" or "project:{id}". Example: "task:8485093748" or "project:6cfCcrrCFg2xP94Q"'
      );
    let a;
    if (r === "task") {
      const i = await t.getTask(n), c = _(i), d = [c.content];
      c.description && d.push(`

Description: ${c.description}`), c.dueDate && d.push(`
Due: ${c.dueDate}`), c.labels.length > 0 && d.push(`
Labels: ${c.labels.join(", ")}`), a = {
        id: `task:${c.id}`,
        title: c.content,
        text: d.join(""),
        url: Ut(c.id),
        metadata: {
          priority: c.priority,
          projectId: c.projectId,
          sectionId: c.sectionId,
          parentId: c.parentId,
          recurring: c.recurring,
          duration: c.duration,
          responsibleUid: c.responsibleUid,
          assignedByUid: c.assignedByUid,
          checked: c.checked,
          completedAt: c.completedAt
        }
      };
    } else {
      const i = await t.getProject(n), c = V(i), d = [c.name];
      c.description && d.push(`

Description: ${c.description}`), c.isShared && d.push(`

Shared project`), c.isFavorite && d.push(`
Favorite: Yes`), a = {
        id: `project:${c.id}`,
        title: c.name,
        text: d.join(""),
        url: Pt(c.id),
        metadata: {
          color: c.color,
          isFavorite: c.isFavorite,
          isShared: c.isShared,
          parentId: c.parentId,
          inboxProject: c.inboxProject,
          viewStyle: c.viewStyle
        }
      };
    }
    return {
      textContent: JSON.stringify(a),
      structuredContent: a
    };
  }
}, pn = {
  objectType: s.enum(["task", "project", "comment"]).optional().describe("Type of object to filter by."),
  objectId: s.string().optional().describe("Filter by specific object ID (task, project, or comment)."),
  eventType: s.enum([
    "added",
    "updated",
    "deleted",
    "completed",
    "uncompleted",
    "archived",
    "unarchived",
    "shared",
    "left"
  ]).optional().describe("Type of event to filter by."),
  projectId: s.string().optional().describe("Filter events by parent project ID."),
  taskId: s.string().optional().describe("Filter events by parent task ID (for subtask events)."),
  initiatorId: s.string().optional().describe("Filter by the user ID who initiated the event."),
  limit: s.number().int().min(1).max(j.ACTIVITY_MAX).default(j.ACTIVITY_DEFAULT).describe("Maximum number of activity events to return."),
  cursor: s.string().optional().describe("Pagination cursor for retrieving the next page of results.")
}, mn = {
  events: s.array(Bo).describe("The activity events."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of events in this page."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, hn = {
  name: f.FIND_ACTIVITY,
  description: "Retrieve recent activity logs to monitor and audit changes in Todoist. Shows events from all users by default (use initiatorId to filter by specific user). Track task completions, updates, deletions, project changes, and more with flexible filtering. Note: Date-based filtering is not supported by the Todoist API.",
  parameters: pn,
  outputSchema: mn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { objectType: o, objectId: r, eventType: n, projectId: a, taskId: i, initiatorId: c, limit: d, cursor: l } = e, p = {
      limit: d,
      cursor: l ?? null
    };
    o && n ? p.objectEventTypes = `${o}:${n}` : o ? p.objectEventTypes = `${o}:` : n && (p.objectEventTypes = `:${n}`), r && r !== "remove" && (p.objectId = r), a && (p.parentProjectId = a), i && (p.parentItemId = i), c && (p.initiatorId = c);
    const { results: u, nextCursor: m } = await t.getActivityLogs(p), h = u.map(Mo);
    return {
      textContent: fn({ events: h, args: e, nextCursor: m }),
      structuredContent: {
        events: h,
        nextCursor: m ?? void 0,
        totalCount: h.length,
        hasMore: !!m,
        appliedFilters: e
      }
    };
  }
};
function fn({
  events: e,
  args: t,
  nextCursor: o
}) {
  let r = "Activity events";
  const n = [];
  if (t.eventType && n.push(`${t.eventType}`), t.objectType) {
    const c = t.objectType === "task" ? "tasks" : `${t.objectType}s`;
    n.push(c);
  }
  n.length > 0 && (r = `Activity: ${n.join(" ")}`);
  const a = [];
  t.objectId && a.push(`object ID: ${t.objectId}`), t.projectId && a.push(`project: ${t.projectId}`), t.taskId && a.push(`task: ${t.taskId}`), t.initiatorId && a.push(`initiator: ${t.initiatorId}`);
  const i = [];
  return e.length === 0 && (i.push("No activity events match the specified filters"), i.push("Note: Activity logs only show recent events"), t.eventType && i.push(`Try removing the eventType filter (${t.eventType})`), t.objectType && i.push(`Try removing the objectType filter (${t.objectType})`), (t.objectId || t.projectId || t.taskId) && i.push("Verify the object ID is correct")), M({
    subject: r,
    count: e.length,
    limit: t.limit,
    nextCursor: o ?? void 0,
    filterHints: a,
    previewLines: bn(e, Math.min(e.length, t.limit)),
    zeroReasonHints: i
  });
}
function bn(e, t = 10) {
  const r = e.slice(0, t).map(gn).join(`
`);
  if (e.length > t) {
    const n = e.length - t;
    return `${r}
    ... and ${n} more event${n === 1 ? "" : "s"}`;
  }
  return r;
}
function gn(e) {
  const t = yn(e.eventDate), o = `${e.eventType} ${e.objectType}`;
  let r = "";
  if (e.extraData) {
    const c = e.extraData.content || e.extraData.name || e.extraData.last_content;
    c && typeof c == "string" && (r = ` • "${c.length > 50 ? `${c.substring(0, 47)}...` : c}"`);
  }
  const n = e.objectId ? ` • id=${e.objectId}` : "", a = e.initiatorId ? ` • by=${e.initiatorId}` : " • system", i = e.parentProjectId ? ` • project=${e.parentProjectId}` : "";
  return `    [${t}] ${o}${r}${n}${a}${i}`;
}
function yn(e) {
  try {
    const t = new Date(e), o = t.toLocaleDateString("en-US", { month: "short", timeZone: "UTC" }), r = t.toLocaleDateString("en-US", { day: "numeric", timeZone: "UTC" }), n = t.toLocaleTimeString("en-US", {
      hour: "2-digit",
      minute: "2-digit",
      hour12: !1,
      timeZone: "UTC"
    });
    return `${o} ${r}, ${n}`;
  } catch {
    return e;
  }
}
const kn = {
  taskId: s.string().optional().describe("Find comments for a specific task."),
  projectId: s.string().optional().describe(
    'Find comments for a specific project. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  commentId: s.string().optional().describe("Get a specific comment by ID."),
  cursor: s.string().optional().describe("Pagination cursor for retrieving more results."),
  limit: s.number().int().min(1).max(j.COMMENTS_MAX).optional().describe("Maximum number of comments to return")
}, Tn = {
  comments: s.array(je).describe("The found comments."),
  searchType: s.string().describe(
    'The type of search performed: "single" (comment ID), "task" (task ID), or "project" (project ID).'
  ),
  searchId: s.string().describe("The ID that was searched for (comment, task, or project ID)."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of comments in this page.")
}, wn = {
  name: f.FIND_COMMENTS,
  description: "Find comments by task, project, or get a specific comment by ID. Exactly one of taskId, projectId, or commentId must be provided.",
  parameters: kn,
  outputSchema: Tn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = [e.taskId, e.projectId, e.commentId].filter(Boolean);
    if (o.length === 0)
      throw new Error("Must provide exactly one of: taskId, projectId, or commentId.");
    if (o.length > 1)
      throw new Error(
        "Cannot provide multiple search parameters. Choose one of: taskId, projectId, or commentId."
      );
    const r = await K({
      projectId: e.projectId,
      client: t
    });
    let n = !1, a = null, i;
    if (e.commentId)
      i = [await t.getComment(e.commentId)];
    else if (e.taskId) {
      const l = await t.getComments({
        taskId: e.taskId,
        cursor: e.cursor || null,
        limit: e.limit || j.COMMENTS_DEFAULT
      });
      i = l.results, n = l.nextCursor !== null, a = l.nextCursor;
    } else if (r) {
      const l = await t.getComments({
        projectId: r,
        cursor: e.cursor || null,
        limit: e.limit || j.COMMENTS_DEFAULT
      });
      i = l.results, n = l.nextCursor !== null, a = l.nextCursor;
    } else
      throw new Error("Invalid state: no search parameter provided");
    const c = i.map(we);
    return {
      textContent: vn({
        comments: c,
        searchType: e.commentId ? "single" : e.taskId ? "task" : "project",
        searchId: e.commentId || e.taskId || e.projectId || "",
        hasMore: n,
        nextCursor: a
      }),
      structuredContent: {
        comments: c,
        searchType: e.commentId ? "single" : e.taskId ? "task" : "project",
        searchId: e.commentId || e.taskId || e.projectId || "",
        hasMore: n,
        nextCursor: a ?? void 0,
        totalCount: c.length
      }
    };
  }
};
function vn({
  comments: e,
  searchType: t,
  searchId: o,
  hasMore: r,
  nextCursor: n
}) {
  if (e.length === 0)
    return `No comments found for ${t}${t !== "single" ? ` ${o}` : ""}`;
  let a;
  if (t === "single") {
    const i = e[0];
    if (!i)
      return "Comment not found";
    a = `Found comment${i.fileAttachment !== void 0 ? ` • Has attachment: ${i.fileAttachment?.fileName || "file"}` : ""} • id=${i.id}`;
  } else {
    const i = e.filter((l) => l.fileAttachment !== void 0).length, c = i > 0 ? ` (${i} with attachments)` : "", d = e.length === 1 ? "comment" : "comments";
    a = `Found ${e.length} ${d} for ${t} ${o}${c}`, r && (a += " • More available");
  }
  if (n) {
    const i = zt([], n);
    return `${a}
${i}`;
  }
  return a;
}
function Jt(e) {
  const t = e.getUTCFullYear(), o = String(e.getUTCMonth() + 1).padStart(2, "0"), r = String(e.getUTCDate()).padStart(2, "0");
  return `${t}-${o}-${r}`;
}
function In(e) {
  const t = /^([+-])(\d{2}):(\d{2})$/.exec(e);
  if (!t)
    return 0;
  const [, o, r, n] = t, a = Number(r) * 60 + Number(n);
  return o === "-" ? -a : a;
}
function jn(e, t) {
  const o = new Date(e.getTime() + t * 60 * 1e3);
  return Jt(o);
}
function kt(e, t) {
  const [o, r, n] = e.split("-"), a = Number(o), i = Number(r), c = Number(n);
  if (!Number.isFinite(a) || !Number.isFinite(i) || !Number.isFinite(c))
    throw new Error(`Invalid date format: ${e}. Expected YYYY-MM-DD.`);
  const d = new Date(Date.UTC(a, i - 1, c));
  return d.setUTCDate(d.getUTCDate() + t), Jt(d);
}
const Sn = ["and", "or"], at = {
  labels: s.string().array().optional().describe("The labels to filter the tasks by"),
  labelsOperator: s.enum(Sn).optional().describe(
    'The operator to use when filtering by labels. This will dictate whether a task has all labels, or some of them. Default is "or".'
  )
};
function it(e = [], t = "or") {
  if (e.length === 0) return "";
  const o = t === "and" ? " & " : " | ";
  return `(${e.map((a) => a.startsWith("@") ? a : `@${a}`).join(` ${o} `)})`;
}
const Cn = 7;
function $n({
  since: e,
  until: t,
  userGmtOffset: o,
  now: r = /* @__PURE__ */ new Date()
}) {
  const n = In(o), a = jn(r, n), i = Cn - 1, c = t ?? (e ? kt(e, i) : a);
  return {
    since: e ?? kt(c, -i),
    until: c
  };
}
const Dn = {
  getBy: s.enum(["completion", "due"]).default("completion").describe(
    'The method to use to get the tasks: "completion" to get tasks by completion date (ie, when the task was actually completed), "due" to get tasks by due date (ie, when the task was due to be completed by).'
  ),
  since: s.string().date().regex(/^\d{4}-\d{2}-\d{2}$/).optional().describe(
    "Optional start date for completed tasks. Format: YYYY-MM-DD. Defaults to 6 days before until (or today if until is omitted), resulting in a 7-day window."
  ),
  until: s.string().date().regex(/^\d{4}-\d{2}-\d{2}$/).optional().describe(
    "Optional end date for completed tasks. Format: YYYY-MM-DD. Defaults to 6 days after since (or today if since is omitted), resulting in a 7-day window."
  ),
  workspaceId: s.string().optional().describe("The ID of the workspace to get the tasks for."),
  projectId: s.string().optional().describe(
    'The ID of the project to get the tasks for. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: s.string().optional().describe("The ID of the section to get the tasks for."),
  parentId: s.string().optional().describe("The ID of the parent task to get the tasks for."),
  responsibleUser: s.string().optional().describe(
    "Filter completed tasks assigned to this user. User ID, name, or email. For personal queries (summaries, plans, reports), set to current user from user-info to exclude collaborators. Defaults to all collaborators."
  ),
  limit: s.number().int().min(1).max(j.COMPLETED_TASKS_MAX).default(j.COMPLETED_TASKS_DEFAULT).describe("The maximum number of tasks to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of tasks (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  ...at
}, An = {
  tasks: s.array(J).describe("The found completed tasks."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of tasks in this page."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, xn = {
  name: f.FIND_COMPLETED_TASKS,
  description: "Get completed tasks. since/until are optional and default to a 7-day window when omitted. Includes all collaborators by default. Person-specific queries (summaries, plans, reports) require responsibleUser.",
  parameters: Dn,
  outputSchema: An,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { getBy: o, labels: r, labelsOperator: n, since: a, until: i, responsibleUser: c, projectId: d, ...l } = e;
    if (e.cursor && (!a || !i))
      throw new Error(
        "Cursor pagination requires explicit since and until. Reuse structuredContent.appliedFilters.since and structuredContent.appliedFilters.until from the previous page."
      );
    const p = await Qe(t, c), u = p?.email;
    let h = it(r, n);
    p && u && (h = ae(h, `assigned to: ${u}`));
    const k = await t.getUser(), b = k.tzInfo?.gmtString || "+00:00", C = await K({
      projectId: d,
      user: k
    }), I = $n({
      since: a,
      until: i,
      userGmtOffset: b
    }), x = `${I.since}T00:00:00${b}`, w = `${I.until}T23:59:59${b}`, y = new Date(x).toISOString(), D = new Date(w).toISOString(), R = {
      ...e,
      since: I.since,
      until: I.until
    }, { items: v, nextCursor: $ } = o === "completion" ? await t.getCompletedTasksByCompletionDate({
      ...l,
      projectId: C,
      since: y,
      until: D,
      ...h ? { filterQuery: h, filterLang: "en" } : {}
    }) : await t.getCompletedTasksByDueDate({
      ...l,
      projectId: C,
      since: y,
      until: D,
      ...h ? { filterQuery: h, filterLang: "en" } : {}
    }), S = v.map(_);
    return {
      textContent: En({
        tasks: S,
        args: R,
        nextCursor: $,
        assigneeEmail: u
      }),
      structuredContent: {
        tasks: S,
        nextCursor: $ ?? void 0,
        totalCount: S.length,
        hasMore: !!$,
        appliedFilters: R
      }
    };
  }
};
function En({
  tasks: e,
  args: t,
  nextCursor: o,
  assigneeEmail: r
}) {
  const n = t.getBy === "completion" ? "completed" : "due", a = `Completed tasks (by ${n} date)`, i = [];
  if (i.push(`${n} date: ${t.since} to ${t.until}`), t.projectId && i.push(`project: ${t.projectId}`), t.sectionId && i.push(`section: ${t.sectionId}`), t.parentId && i.push(`parent: ${t.parentId}`), t.workspaceId && i.push(`workspace: ${t.workspaceId}`), t.labels && t.labels.length > 0) {
    const d = t.labels.map((l) => `@${l}`).join(t.labelsOperator === "and" ? " & " : " | ");
    i.push(`labels: ${d}`);
  }
  if (t.responsibleUser) {
    const d = r || t.responsibleUser;
    i.push(`assigned to: ${d}`);
  }
  const c = [];
  return e.length === 0 && (c.push("No tasks completed in this date range"), c.push("Try expanding the date range"), (t.projectId || t.sectionId || t.parentId) && c.push("Try removing project/section/parent filters"), t.getBy === "due" && c.push('Try switching to "completion" date instead')), M({
    subject: a,
    count: e.length,
    limit: t.limit,
    nextCursor: o ?? void 0,
    filterHints: i,
    previewLines: De(e, Math.min(e.length, t.limit)),
    zeroReasonHints: c
  });
}
const On = {
  searchText: s.string().optional().describe(
    'Search for a goal by name (partial and case insensitive match). Supports wildcards (e.g. "ship*"). If omitted, all goals are returned.'
  ),
  ownerType: s.enum(ws).optional().describe("Filter by ownership type. Omit for all accessible goals."),
  limit: s.number().int().min(1).max(j.GOALS_MAX).default(j.GOALS_DEFAULT).describe("The maximum number of goals to return per page."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of goals (cursor is obtained from the previous call to this tool, with the same parameters)."
  )
}, Un = {
  goals: s.array(Se).describe("The found goals."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  totalCount: s.number().describe("The total number of goals returned in this page."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, Pn = {
  name: f.FIND_GOALS,
  description: "Search for goals by name or list all accessible goals. Results are paginated — use the returned `nextCursor` to fetch subsequent pages.",
  parameters: On,
  outputSchema: Un,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = e.searchText ? await t.searchGoals({
      query: e.searchText,
      ownerType: e.ownerType,
      cursor: e.cursor ?? null,
      limit: e.limit
    }) : await t.getGoals({
      ownerType: e.ownerType,
      cursor: e.cursor ?? null,
      limit: e.limit
    }), r = o.results.map(ve), n = o.nextCursor ?? void 0, a = e.searchText ? `Goals matching "${e.searchText}"` : "All accessible goals", i = r.length > 0 ? r.map(
      (d) => `    ${d.name} • id=${d.id} • progress=${d.progress?.percentage ?? 0}%`
    ).join(`
`) : void 0;
    return {
      textContent: M({
        subject: a,
        count: r.length,
        limit: e.limit,
        nextCursor: n,
        previewLines: i,
        zeroReasonHints: e.searchText ? [
          "Try broader search terms",
          "Check spelling",
          "Remove searchText to see all goals"
        ] : ["No goals exist yet", `Use ${f.ADD_GOALS} to create goals`]
      }),
      structuredContent: {
        goals: r,
        nextCursor: n,
        hasMore: !!n,
        totalCount: r.length,
        appliedFilters: e
      }
    };
  }
}, _n = {
  searchText: s.string().optional().describe(
    'Search for a label by name (partial and case insensitive match). Supports wildcards (e.g. "work*" for prefix match). Use "\\*" for a literal asterisk. If omitted, all labels are returned.'
  ),
  limit: s.number().int().min(1).max(j.LABELS_MAX).default(j.LABELS_DEFAULT).describe("The maximum number of labels to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of labels (cursor is obtained from the previous call to this tool, with the same parameters). Ignored when searchText is provided."
  )
}, Rn = {
  labels: s.array(pe).describe("The found personal labels."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of labels in this page."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  sharedLabels: s.array(s.string()).describe(
    "Names of all shared labels visible to you. These have no IDs or metadata — use their names directly when filtering tasks."
  ),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, Nn = {
  name: f.FIND_LABELS,
  description: "List personal labels and shared labels. Personal labels have full metadata (id, name, color, order, isFavorite) and support pagination and name search (partial, case insensitive). Shared labels are labels used on tasks shared with you — they are returned as names only (no IDs or metadata). When searching, all matching personal labels are fetched across all pages and returned as a single result set (limit and cursor are ignored). When not searching, personal labels are returned with pagination.",
  parameters: _n,
  outputSchema: Rn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const [o, r] = await Promise.all([
      e.searchText ? _o(t, e.searchText).then((c) => ({
        results: c,
        nextCursor: null
      })) : t.getLabels({ limit: e.limit, cursor: e.cursor ?? null }),
      Ro(t)
    ]), { results: n, nextCursor: a } = o, i = e.searchText ? { searchText: e.searchText } : { limit: e.limit, cursor: e.cursor };
    return {
      textContent: Ln({ labels: n, args: e, nextCursor: a, sharedLabels: r }),
      structuredContent: {
        labels: n.map((c) => pe.parse(c)),
        nextCursor: a ?? void 0,
        totalCount: n.length,
        hasMore: !!a,
        sharedLabels: r,
        appliedFilters: i
      }
    };
  }
};
function Ln({
  labels: e,
  args: t,
  nextCursor: o,
  sharedLabels: r
}) {
  const n = t.searchText ? `All labels matching "${t.searchText}"` : "Labels", a = [];
  t.searchText && a.push(`searchText: "${t.searchText}"`);
  const i = 10, d = e.slice(0, i).map(Lr).join(`
`), l = e.length - i, p = l > 0 ? `${d}
    …and ${l} more` : d, u = [];
  e.length === 0 && (t.searchText ? (u.push("Try broader search terms"), u.push("Check spelling"), u.push("Remove searchText to see all labels")) : u.push("No personal labels created yet"));
  const m = r.length > 0 ? `
Shared labels (${r.length}): ${r.join(", ")}` : `
No shared labels.`;
  return M({
    subject: n,
    count: e.length,
    limit: t.searchText ? void 0 : t.limit,
    nextCursor: o ?? void 0,
    filterHints: a,
    previewLines: p,
    zeroReasonHints: u
  }) + m;
}
const { FIND_PROJECTS: Xt, ADD_TASKS: Zt, UPDATE_TASKS: Qt } = f, Mn = {
  projectId: s.string().min(1).optional().describe(
    'Optional. If provided, searches only collaborators of this project. If omitted, searches across the collaborators of all shared projects the authenticated user can access (plus the authenticated user themselves) — use this for general "find a user" / "who is X" lookups.'
  ),
  searchTerm: s.string().optional().describe(
    "Search for a user by name or email (partial and case insensitive match). If omitted, all users are returned."
  )
}, Fn = {
  collaborators: s.array(Yo).describe("The found users."),
  projectInfo: s.object({
    id: s.string().describe("The project ID."),
    name: s.string().describe("The project name."),
    isShared: s.boolean().describe("Whether the project is shared.")
  }).optional().describe("Information about the project (only present when projectId was provided)."),
  totalCount: s.number().describe("The total number of users found."),
  totalAvailable: s.number().optional().describe("The total number of available users before the search filter was applied."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, Hn = {
  name: f.FIND_PROJECT_COLLABORATORS,
  description: `Find Todoist users (collaborators, teammates) by name or email to look up their user ID. Use this whenever the user asks to find, look up, or identify a person — e.g. "find Carrie's user ID", "who is Ernesto", "look up a user". When projectId is omitted, searches across the collaborators of every shared project the authenticated user has access to, plus the authenticated user themselves — an empty result means the person is not a collaborator on any project you share with them, not necessarily that they do not exist in Todoist. When projectId is provided, searches only that project. Partial, case-insensitive match on name and email.`,
  parameters: Mn,
  outputSchema: Fn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { projectId: o, searchTerm: r } = e;
    if (!o)
      return Wn({ searchTerm: r, client: t, appliedFilters: e });
    let n = o, a;
    try {
      if (a = await t.getProject(o), !a)
        throw new Error(`Project with ID "${o}" not found or not accessible`);
      if (n = a.name, !a.isShared)
        return {
          textContent: `Project "${n}" is not shared and has no collaborators.

**Next steps:**
• Share the project to enable collaboration
• Use ${Zt} and ${Qt} for assignment features once shared`,
          structuredContent: {
            collaborators: [],
            projectInfo: {
              id: o,
              name: n,
              isShared: !1
            },
            totalCount: 0,
            appliedFilters: e
          }
        };
    } catch (l) {
      throw new Error(
        `Failed to access project "${o}": ${l instanceof Error ? l.message : "Unknown error"}`
      );
    }
    const i = await G.getProjectCollaborators(t, o);
    if (i.length === 0)
      return {
        textContent: `Project "${n}" has no collaborators or collaborator data is not accessible.

**Next steps:**
• Check project sharing settings
• Ensure you have permission to view collaborators
• Try refreshing or re-sharing the project`,
        structuredContent: {
          collaborators: [],
          projectInfo: {
            id: o,
            name: n,
            isShared: !0
          },
          totalCount: 0,
          appliedFilters: e
        }
      };
    const c = es(i, r);
    return {
      textContent: ts({
        collaborators: c,
        projectName: n,
        searchTerm: r,
        totalAvailable: i.length
      }),
      structuredContent: {
        collaborators: c,
        projectInfo: {
          id: o,
          name: n,
          isShared: !0
        },
        totalCount: c.length,
        totalAvailable: i.length,
        appliedFilters: e
      }
    };
  }
};
async function Wn({
  searchTerm: e,
  client: t,
  appliedFilters: o
}) {
  const r = await G.getAllCollaborators(t), n = await Gn(t, r);
  if (n.length === 0)
    return {
      textContent: `No users found. You may have no shared projects, or collaborator data is not accessible.

**Next steps:**
• Use ${Xt} to find shared projects
• Share a project to add collaborators
• Ensure you have permission to view collaborators`,
      structuredContent: {
        collaborators: [],
        projectInfo: void 0,
        totalCount: 0,
        totalAvailable: 0,
        appliedFilters: o
      }
    };
  const a = es(n, e);
  return {
    textContent: ts({
      collaborators: a,
      projectName: void 0,
      searchTerm: e,
      totalAvailable: n.length
    }),
    structuredContent: {
      collaborators: a,
      projectInfo: void 0,
      totalCount: a.length,
      totalAvailable: n.length,
      appliedFilters: o
    }
  };
}
async function Gn(e, t) {
  try {
    const o = await e.getUser();
    return !o || t.some((r) => r.id === o.id) ? t : [
      { id: o.id, name: o.fullName, email: o.email },
      ...t
    ];
  } catch {
    return t;
  }
}
function es(e, t) {
  if (!t)
    return e;
  const o = t.toLowerCase().trim();
  return e.filter(
    (r) => r.name.toLowerCase().includes(o) || r.email.toLowerCase().includes(o)
  );
}
function ts({
  collaborators: e,
  projectName: t,
  searchTerm: o,
  totalAvailable: r
}) {
  const n = t ? `project "${t}"` : "workspace", a = t ? "Project collaborators" : "Workspace users", i = o ? `${a} matching "${o}"` : a, c = [];
  o && c.push(`matching "${o}"`), c.push(`in ${n}`);
  let d = [];
  e.length > 0 && (d = e.slice(0, 10).map((u) => {
    const m = u.name || "Unknown Name", h = u.email || "No email";
    return `• ${m} (${h}) - ID: ${u.id}`;
  }), e.length > 10 && d.push(`... and ${e.length - 10} more`));
  const l = [];
  e.length === 0 && o && (l.push(`No users match "${o}"`), l.push("Try a broader search term or check spelling"), r > 0 && l.push(`${r} users available without filter`));
  const p = [];
  return e.length > 0 ? (p.push(`Use ${Zt} with responsibleUser to assign new tasks`), p.push(`Use ${Qt} with responsibleUser to reassign existing tasks`), p.push("Use user names, emails, or IDs for assignments")) : (p.push(`Use ${Xt} to find other projects`), o && r > 0 && p.push("Try searching without filters to see all users")), M({
    subject: i,
    count: e.length,
    filterHints: c,
    previewLines: d.join(`
`),
    zeroReasonHints: l,
    nextSteps: p
  });
}
const { ADD_PROJECTS: Tt } = f, Bn = {
  active: "projects",
  archived: "archived projects",
  all: "projects (active and archived)"
}, Yn = {
  searchText: s.string().optional().describe(
    'Search for a project by name (partial and case insensitive match). Supports wildcards (e.g. "work*" for prefix match). Use "\\*" for a literal asterisk. If omitted, all projects are returned.'
  ),
  limit: s.number().int().min(1).max(j.PROJECTS_MAX).default(j.PROJECTS_DEFAULT).describe("The maximum number of projects to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of projects (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  archivedStatus: s.enum(["active", "archived", "all"]).optional().describe(
    "Which projects to return by archive status: 'active' (default, non-archived only), 'archived' (archived only), or 'all' (both active and archived). Each project includes an isArchived field. Archived projects can be deleted via the delete-object tool (type: 'project')."
  )
}, qn = {
  projects: s.array(ue).describe("The found projects."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of projects in this page."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, zn = {
  name: f.FIND_PROJECTS,
  description: "List all projects or search for projects by name. By default only active projects are returned; use archivedStatus ('archived' or 'all') to include archived projects. When searching or when archivedStatus is 'all', all matching projects are returned (pagination is ignored). Otherwise projects are returned with pagination.",
  parameters: Yn,
  outputSchema: qn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { searchText: o } = e, r = e.archivedStatus ?? "active";
    let n, a = null;
    if (o || r === "all") {
      const c = Promise.resolve([]), [d, l] = await Promise.all([
        r === "archived" ? c : o ? Wt(t, o) : Oo(t),
        r === "active" ? c : Uo(t)
      ]), p = o ? Po(o) : null, u = p ? l.filter((m) => p.test(m.name)) : l;
      n = [...d, ...u];
    } else {
      const c = r === "archived" ? await t.getArchivedProjects({
        limit: e.limit,
        cursor: e.cursor ?? null
      }) : await t.getProjects({ limit: e.limit, cursor: e.cursor ?? null });
      n = c.results, a = c.nextCursor;
    }
    const i = n.map(V);
    return {
      textContent: Kn({ projects: i, args: e, nextCursor: a }),
      structuredContent: {
        projects: i,
        nextCursor: a ?? void 0,
        totalCount: i.length,
        hasMore: !!a,
        appliedFilters: e
      }
    };
  }
};
function Kn({
  projects: e,
  args: t,
  nextCursor: o
}) {
  const r = Bn[t.archivedStatus ?? "active"], n = t.searchText ? `All ${r} matching "${t.searchText}"` : `${r.charAt(0).toUpperCase()}${r.slice(1)}`, a = [];
  t.searchText && a.push(`searchText: "${t.searchText}"`), t.archivedStatus && t.archivedStatus !== "active" && a.push(`archivedStatus: "${t.archivedStatus}"`);
  const i = 10, d = e.slice(0, i).map(Mr).join(`
`), l = e.length - i, p = l > 0 ? `${d}
    …and ${l} more` : d, u = [];
  return e.length === 0 && (t.searchText ? (u.push("Try broader search terms"), u.push("Check spelling"), u.push("Remove searchText to see all projects")) : t.archivedStatus === "archived" ? (u.push("No archived projects"), u.push("Use archivedStatus: 'all' to also see active projects")) : t.archivedStatus === "all" ? (u.push("No projects created yet"), u.push(`Use ${Tt} to create a project`)) : (u.push("No active projects"), u.push("Use archivedStatus: 'all' to include archived projects"), u.push(`Use ${Tt} to create a project`))), M({
    subject: n,
    count: e.length,
    limit: t.searchText || t.archivedStatus === "all" ? void 0 : t.limit,
    nextCursor: o ?? void 0,
    filterHints: a,
    previewLines: p,
    zeroReasonHints: u
  });
}
const Vn = {
  taskId: s.string().optional().describe(
    "Find all reminders for a specific task. Returns both time-based and location reminders."
  ),
  reminderId: s.string().optional().describe("Get a specific time-based reminder (relative or absolute) by its ID."),
  locationReminderId: s.string().optional().describe("Get a specific location reminder by its ID.")
}, Jn = {
  reminders: s.array(st).describe("The found reminders (time-based and location)."),
  searchType: s.string().describe('The search type used: "task", "reminder", or "location_reminder".'),
  searchId: s.string().describe("The ID used for the search."),
  totalCount: s.number().describe("Total reminders in this response.")
}, Xn = {
  name: f.FIND_REMINDERS,
  description: "Find reminders by task ID (returns all reminder types), or get a specific reminder by its ID. Use reminderId for time-based reminders and locationReminderId for location reminders.",
  parameters: Vn,
  outputSchema: Jn,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { taskId: o, reminderId: r, locationReminderId: n } = e, a = [o, r, n].filter(Boolean);
    if (a.length === 0)
      throw new Error("One of taskId, reminderId, or locationReminderId must be provided.");
    if (a.length > 1)
      throw new Error(
        "Only one of taskId, reminderId, or locationReminderId can be provided at a time."
      );
    if (r) {
      const i = await t.getReminder(r), c = he(i);
      return {
        textContent: `Found ${i.type} reminder (id=${r})`,
        structuredContent: {
          reminders: [c],
          searchType: "reminder",
          searchId: r,
          totalCount: 1
        }
      };
    }
    if (n) {
      const i = await t.getLocationReminder(n), c = he(i);
      return {
        textContent: `Found location reminder (id=${n})`,
        structuredContent: {
          reminders: [c],
          searchType: "location_reminder",
          searchId: n,
          totalCount: 1
        }
      };
    }
    if (o) {
      const [i, c] = await Promise.all([
        L({
          apiMethod: (u) => t.getReminders(u),
          args: { taskId: o }
        }),
        L({
          apiMethod: (u) => t.getLocationReminders(u),
          args: { taskId: o }
        })
      ]), l = [...i, ...c].map(he);
      return {
        textContent: Zn(l, o),
        structuredContent: {
          reminders: l,
          searchType: "task",
          searchId: o,
          totalCount: l.length
        }
      };
    }
    throw new Error("One of taskId, reminderId, or locationReminderId must be provided.");
  }
};
function Zn(e, t) {
  if (e.length === 0)
    return `No reminders found for task ${t}`;
  const { timeBasedCount: o, locationCount: r } = Gt(e), n = [];
  if (o > 0) {
    const a = o > 1 ? "time-based reminders" : "time-based reminder";
    n.push(`${o} ${a}`);
  }
  if (r > 0) {
    const a = r > 1 ? "location reminders" : "location reminder";
    n.push(`${r} ${a}`);
  }
  return `Found ${n.join(" and ")} for task ${t}`;
}
const { ADD_SECTIONS: Qn } = f, ea = {
  projectId: s.string().min(1).describe(
    'The ID of the project to search sections in. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  searchText: s.string().optional().describe(
    'Search for a section by name (partial and case insensitive match). Supports wildcards (e.g. "work*" for prefix match). Use "\\*" for a literal asterisk. If omitted, all sections in the project are returned.'
  )
}, ta = {
  sections: s.array(ce).describe("The found sections."),
  totalCount: s.number().describe("The total number of sections found."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, sa = {
  name: f.FIND_SECTIONS,
  description: "Search for sections by name or other criteria in a project. When searching, uses server-side search to avoid fetching all sections.",
  parameters: ea,
  outputSchema: ta,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = await K({
      projectId: e.projectId,
      client: t
    });
    let r;
    e.searchText ? r = await No(t, e.searchText, o) : r = (await t.getSections({
      projectId: o
    })).results;
    const n = r.map(X);
    return {
      textContent: oa({
        sections: n,
        projectId: e.projectId,
        searchText: e.searchText
      }),
      structuredContent: {
        sections: n,
        totalCount: n.length,
        appliedFilters: e
      }
    };
  }
};
function oa({
  sections: e,
  projectId: t,
  searchText: o
}) {
  const r = [];
  o ? (r.push("Try broader search terms"), r.push("Check spelling"), r.push("Remove searchText to see all sections")) : (r.push("Project has no sections yet"), r.push(`Use ${Qn} to create sections`));
  const n = o ? `Sections in project ${t} matching "${o}"` : `Sections in project ${t}`, a = e.length > 0 ? e.map((i) => `    ${i.name} • id=${i.id}`).join(`
`) : void 0;
  return M({
    subject: n,
    count: e.length,
    previewLines: a,
    zeroReasonHints: r
  });
}
function ss(e) {
  return /* @__PURE__ */ new Date(`${e}T00:00:00`);
}
const ra = {
  startDate: s.string().regex(/^(\d{4}-\d{2}-\d{2}|today)$/).optional().describe("The start date to get the tasks for. Format: YYYY-MM-DD or 'today'."),
  overdueOption: s.enum(["overdue-only", "include-overdue", "exclude-overdue"]).optional().describe(
    "How to handle overdue tasks. 'overdue-only' to get only overdue tasks, 'include-overdue' to include overdue tasks along with tasks for the specified date(s), and 'exclude-overdue' to exclude overdue tasks. Default is 'include-overdue'."
  ),
  daysCount: s.number().int().min(1).max(30).default(1).describe(
    "The number of days to get the tasks for, starting from the start date. Default is 1 which means only tasks for the start date."
  ),
  limit: s.number().int().min(1).max(j.TASKS_MAX).default(j.TASKS_DEFAULT).describe("The maximum number of tasks to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of tasks (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  responsibleUser: s.string().optional().describe(
    "Filter tasks assigned to this user. User ID, name, or email. For personal queries (summaries, plans, reports), set to current user from user-info to exclude collaborators."
  ),
  responsibleUserFiltering: s.enum(Ft).optional().describe(
    "Filter when responsibleUser is omitted. 'assigned'=assigned to others; 'unassignedOrMe'=unassigned+mine; 'all'=everyone. Default: 'unassignedOrMe'."
  ),
  ...at
}, na = {
  tasks: s.array(J).describe("The found tasks."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of tasks in this page."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, aa = {
  name: f.FIND_TASKS_BY_DATE,
  description: "Get tasks by date range. startDate='today' includes overdue items. Default responsibleUserFiltering='unassignedOrMe' excludes others' tasks. Person-specific queries (summaries, plans, reports) require responsibleUser.",
  parameters: ra,
  outputSchema: na,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    if (!e.startDate && e.overdueOption !== "overdue-only")
      throw new Error(
        "Either startDate must be provided or overdueOption must be set to overdue-only"
      );
    const o = await Qe(t, e.responsibleUser), r = o?.userId, n = o?.email;
    let a = "";
    if (e.overdueOption === "overdue-only")
      a = "overdue";
    else if (e.startDate === "today")
      if (e.daysCount > 1) {
        const u = /* @__PURE__ */ new Date(), m = re(u), h = Fe(u, e.daysCount), k = ct(h, { representation: "date" }), b = `(due after: ${m} | due: ${m}) & due before: ${k}`;
        a = e.overdueOption === "exclude-overdue" ? b : `(${b} | overdue)`;
      } else
        a = e.overdueOption === "exclude-overdue" ? "today" : "(today | overdue)";
    else if (e.startDate) {
      const u = e.startDate, m = Fe(ss(u), e.daysCount), h = ct(m, { representation: "date" });
      a = `(due after: ${u} | due: ${u}) & due before: ${h}`;
    }
    const i = it(e.labels, e.labelsOperator);
    i.length > 0 && (a = ae(a, `(${i})`));
    const c = Ht({
      resolvedAssigneeId: r,
      assigneeEmail: n,
      responsibleUserFiltering: e.responsibleUserFiltering
    });
    a = ae(a, c);
    const { tasks: d, nextCursor: l } = await tt({
      client: t,
      query: a,
      cursor: e.cursor,
      limit: e.limit
    });
    return {
      textContent: ia({ tasks: d, args: e, nextCursor: l, assigneeEmail: n }),
      structuredContent: {
        tasks: d,
        nextCursor: l ?? void 0,
        totalCount: d.length,
        hasMore: !!l,
        appliedFilters: e
      }
    };
  }
};
function ia({
  tasks: e,
  args: t,
  nextCursor: o,
  assigneeEmail: r
}) {
  const n = [];
  if (t.overdueOption === "overdue-only")
    n.push("overdue tasks only");
  else if (t.startDate === "today") {
    const c = t.overdueOption === "exclude-overdue" ? "" : " + overdue tasks";
    n.push(
      `today${c}${t.daysCount > 1 ? ` + ${t.daysCount - 1} more days` : ""}`
    );
  } else if (t.startDate) {
    const c = t.daysCount > 1 ? ` to ${re(Fe(ss(t.startDate), t.daysCount))}` : "";
    n.push(`${t.startDate}${c}`);
  }
  if (t.labels && t.labels.length > 0) {
    const c = t.labels.map((d) => `@${d}`).join(t.labelsOperator === "and" ? " & " : " | ");
    n.push(`labels: ${c}`);
  }
  if (t.responsibleUser) {
    const c = r || t.responsibleUser;
    n.push(`assigned to: ${c}`);
  }
  let a = "";
  if (t.overdueOption === "overdue-only" ? a = "Overdue tasks" : t.startDate === "today" ? a = t.overdueOption === "exclude-overdue" ? "Today's tasks" : "Today's tasks + overdue" : t.startDate ? a = `Tasks for ${t.startDate}` : a = "Tasks", t.responsibleUser) {
    const c = r || t.responsibleUser;
    a += ` assigned to ${c}`;
  }
  const i = [];
  if (e.length === 0)
    if (t.overdueOption === "overdue-only")
      i.push("Great job! No overdue tasks");
    else if (t.startDate === "today") {
      const c = t.overdueOption === "exclude-overdue" ? "" : " or overdue";
      i.push(`Great job! No tasks for today${c}`);
    } else
      i.push("Expand date range with larger 'daysCount'"), i.push("Check today's tasks with startDate='today'");
  return M({
    subject: a,
    count: e.length,
    limit: t.limit,
    nextCursor: o ?? void 0,
    filterHints: n,
    previewLines: De(e, Math.min(e.length, t.limit)),
    zeroReasonHints: i
  });
}
const ca = 300 * 1e3;
class da {
  constructor() {
    this.cache = null;
  }
  async getFilters(t) {
    if (this.cache && Date.now() - this.cache.timestamp < ca)
      return this.cache.filters;
    const r = ((await t.sync({
      resourceTypes: ["filters"],
      syncToken: "*"
    })).filters ?? []).filter((n) => !n.isDeleted);
    return this.cache = { filters: r, timestamp: Date.now() }, r;
  }
  /**
   * Resolve a filter name or ID to a filter ID, name, and query.
   *
   * Resolution order:
   * 1. Exact ID match
   * 2. Exact case-insensitive name match
   * 3. Unique partial case-insensitive name match
   * 4. Multiple partial matches → throw ambiguous error
   * 5. No match → throw not-found error
   */
  async resolveFilter(t, o) {
    const r = o.trim();
    if (!r)
      throw new Error("Filter reference cannot be empty");
    const n = await this.getFilters(t), a = n.find((p) => p.id === r);
    if (a)
      return { filterId: a.id, filterName: a.name, filterQuery: a.query };
    const i = r.toLowerCase(), c = n.find((p) => p.name.toLowerCase() === i);
    if (c)
      return {
        filterId: c.id,
        filterName: c.name,
        filterQuery: c.query
      };
    const d = n.filter((p) => p.name.toLowerCase().includes(i)), l = d.length === 1 ? d[0] : void 0;
    if (l)
      return {
        filterId: l.id,
        filterName: l.name,
        filterQuery: l.query
      };
    if (d.length > 1) {
      const p = d.slice(0, 5).map((u) => `  - "${u.name}" (id: ${u.id})`).join(`
`);
      throw new Error(
        `Ambiguous filter reference "${r}". Multiple filters match:
${p}` + (d.length > 5 ? `
  ... and ${d.length - 5} more` : "")
      );
    }
    throw new Error(`Filter "${r}" not found.`);
  }
  /**
   * Clear the filter cache — useful for testing.
   */
  clearCache() {
    this.cache = null;
  }
}
const la = new da(), { FIND_COMPLETED_TASKS: wt, ADD_TASKS: vt } = f, ua = {
  searchText: s.string().optional().describe("The text to search for in tasks."),
  projectId: s.string().optional().describe(
    'Find tasks in this project. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: s.string().optional().describe("Find tasks in this section."),
  parentId: s.string().optional().describe("Find subtasks of this parent task."),
  responsibleUser: s.string().optional().describe("Find tasks assigned to this user. Can be a user ID, name, or email address."),
  responsibleUserFiltering: s.enum(Ft).optional().describe(
    'How to filter by responsible user when responsibleUser is not provided. "assigned" = only tasks assigned to others; "unassignedOrMe" = only unassigned tasks or tasks assigned to me; "all" = all tasks regardless of assignment. Default value will be `unassignedOrMe`.'
  ),
  limit: s.number().int().min(1).max(j.TASKS_MAX).default(j.TASKS_DEFAULT).describe("The maximum number of tasks to return."),
  cursor: s.string().optional().describe(
    "The cursor to get the next page of tasks (cursor is obtained from the previous call to this tool, with the same parameters)."
  ),
  filter: s.string().optional().describe(
    'A raw Todoist filter query string (e.g. "today", "p1", "##Work", "(today | overdue) & p1"). Combined with other filters using AND. Cannot be used with projectId, sectionId, parentId, or filterIdOrName.'
  ),
  filterIdOrName: s.string().optional().describe(
    "The ID or name of a saved Todoist filter. The filter's query will be fetched and used to find tasks. Cannot be used with the `filter` parameter, projectId, sectionId, or parentId."
  ),
  ...at
}, pa = {
  tasks: s.array(J).describe("The found tasks."),
  nextCursor: s.string().optional().describe("Cursor for the next page of results."),
  totalCount: s.number().describe("The total number of tasks in this page."),
  hasMore: s.boolean().describe("Whether there are more results available."),
  appliedFilters: s.record(s.string(), s.unknown()).describe("The filters that were applied to the search.")
}, ma = {
  name: f.FIND_TASKS,
  description: "Find tasks by text search, project/section/parent container, responsible user, labels, a raw Todoist filter string, or a saved filter by ID or name (filterIdOrName). At least one filter must be provided.",
  parameters: ua,
  outputSchema: pa,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const {
      searchText: o,
      projectId: r,
      sectionId: n,
      parentId: a,
      responsibleUser: i,
      responsibleUserFiltering: c,
      limit: d,
      cursor: l,
      labels: p,
      labelsOperator: u,
      filter: m,
      filterIdOrName: h
    } = e, k = await t.getUser(), b = p && p.length > 0;
    if (!o && !r && !n && !a && !i && !b && !m && !h)
      throw new Error(
        "At least one filter must be provided: searchText, projectId, sectionId, parentId, responsibleUser, labels, filter, or filterIdOrName"
      );
    if (m && h)
      throw new Error(
        "The `filter` and `filterIdOrName` parameters cannot be used together. Provide only one."
      );
    if ((m || h) && (r || n || a))
      throw new Error(
        'The `filter`/`filterIdOrName` parameter cannot be combined with projectId, sectionId, or parentId. Use filter syntax instead (e.g. "##ProjectName").'
      );
    let C = m;
    h && (C = (await la.resolveFilter(t, h)).filterQuery);
    const I = await Qe(t, i), x = I?.userId, w = I?.email;
    if (r || n || a) {
      const A = {
        limit: d,
        cursor: l ?? null
      };
      r && (A.projectId = await K({ projectId: r, user: k })), n && (A.sectionId = n), a && (A.parentId = a);
      const { results: F, nextCursor: B } = await t.getTasks(A), Ae = F.map(_);
      let H = o ? Ae.filter(
        (Z) => Z.content.toLowerCase().includes(o.toLowerCase()) || Z.description?.toLowerCase().includes(o.toLowerCase())
      ) : Ae;
      return H = Eo({
        tasks: H,
        resolvedAssigneeId: x,
        currentUserId: k.id,
        responsibleUserFiltering: c
      }), p && p.length > 0 && (H = u === "and" ? H.filter(
        (Z) => p.every((xe) => Z.labels.includes(xe))
      ) : H.filter(
        (Z) => p.some((xe) => Z.labels.includes(xe))
      )), {
        textContent: Pe({
          tasks: H,
          args: e,
          nextCursor: B,
          isContainerSearch: !0,
          assigneeEmail: w
        }),
        structuredContent: {
          tasks: H,
          nextCursor: B ?? void 0,
          totalCount: H.length,
          hasMore: !!B,
          appliedFilters: e
        }
      };
    }
    if (x && !o && !b && !C) {
      const { results: A, nextCursor: F } = await t.getTasksByFilter({
        query: `assigned to: ${w}`,
        lang: "en",
        limit: d,
        cursor: l ?? null
      }), B = A.map(_);
      return {
        textContent: Pe({
          tasks: B,
          args: e,
          nextCursor: F,
          isContainerSearch: !1,
          assigneeEmail: w
        }),
        structuredContent: {
          tasks: B,
          nextCursor: F ?? void 0,
          totalCount: B.length,
          hasMore: !!F,
          appliedFilters: e
        }
      };
    }
    let y = C ? `(${C})` : "";
    o && (y = ae(y, `search: ${o}`));
    const D = it(p, u);
    if (y = ae(y, D), !(h && !i && !c)) {
      const A = Ht({
        resolvedAssigneeId: x,
        assigneeEmail: w,
        responsibleUserFiltering: c
      });
      y = ae(y, A);
    }
    const { tasks: v, nextCursor: $ } = await tt({
      client: t,
      query: y,
      cursor: e.cursor,
      limit: e.limit
    });
    return {
      textContent: Pe({
        tasks: v,
        args: e,
        nextCursor: $,
        isContainerSearch: !1,
        assigneeEmail: w
      }),
      structuredContent: {
        tasks: v,
        nextCursor: $ ?? void 0,
        totalCount: v.length,
        hasMore: !!$,
        appliedFilters: e
      }
    };
  }
};
function ha(e) {
  if (e.projectId) {
    const t = [
      e.searchText ? "No tasks in project match search" : "Project has no tasks yet"
    ];
    return e.searchText || t.push(`Use ${vt} to create tasks`), t;
  }
  if (e.sectionId) {
    const t = [e.searchText ? "No tasks in section match search" : "Section is empty"];
    return e.searchText || t.push("Tasks may be in other sections of the project"), t;
  }
  if (e.parentId) {
    const t = [e.searchText ? "No subtasks match search" : "No subtasks created yet"];
    return e.searchText || t.push(`Use ${vt} with parentId to add subtasks`), t;
  }
  return [];
}
function Pe({
  tasks: e,
  args: t,
  nextCursor: o,
  isContainerSearch: r,
  assigneeEmail: n
}) {
  let a = "Tasks";
  const i = [], c = [];
  if (r) {
    if (t.projectId ? (a = "Tasks in project", i.push(`in project ${t.projectId}`)) : t.sectionId ? (a = "Tasks in section", i.push(`in section ${t.sectionId}`)) : t.parentId ? (a = "Subtasks", i.push(`subtasks of ${t.parentId}`)) : a = "Tasks", t.searchText && (a += ` matching "${t.searchText}"`, i.push(`containing "${t.searchText}"`)), t.responsibleUser) {
      const d = n || t.responsibleUser;
      a += ` assigned to ${d}`, i.push(`assigned to ${d}`);
    }
    if (t.labels && t.labels.length > 0) {
      const d = t.labels.map((l) => `@${l}`).join(t.labelsOperator === "and" ? " & " : " | ");
      i.push(`labels: ${d}`);
    }
    e.length === 0 && c.push(...ha(t));
  } else {
    const d = n || t.responsibleUser, l = [];
    if (t.filter && l.push(`filter: ${t.filter}`), t.searchText && l.push(`"${t.searchText}"`), t.responsibleUser && l.push(`assigned to ${d}`), t.labels && t.labels.length > 0) {
      const p = t.labels.map((u) => `@${u}`).join(t.labelsOperator === "and" ? " & " : " | ");
      l.push(`with labels: ${p}`);
    }
    if (t.filter && !t.searchText && !t.responsibleUser && !t.labels?.length ? (a = `Tasks matching filter: ${t.filter}`, i.push(`filter: ${t.filter}`)) : t.searchText ? (a = `Search results for ${l.join(" ")}`, i.push(`matching "${t.searchText}"`), t.filter && i.push(`filter: ${t.filter}`)) : t.responsibleUser && (!t.labels || t.labels.length === 0) ? (a = `Tasks assigned to ${d}`, t.filter && i.push(`filter: ${t.filter}`)) : t.labels && t.labels.length > 0 && !t.responsibleUser ? (a = `Tasks with labels: ${t.labels.map((u) => `@${u}`).join(t.labelsOperator === "and" ? " & " : " | ")}`, t.filter && i.push(`filter: ${t.filter}`)) : a = `Tasks ${l.join(" ")}`, t.responsibleUser && i.push(`assigned to ${d}`), t.labels && t.labels.length > 0) {
      const p = t.labels.map((u) => `@${u}`).join(t.labelsOperator === "and" ? " & " : " | ");
      i.push(`labels: ${p}`);
    }
    if (e.length === 0) {
      if (t.responsibleUser) {
        const p = n || t.responsibleUser;
        c.push(`No tasks assigned to ${p}`), c.push("Check if the user name is correct"), c.push(`Check completed tasks with ${wt}`);
      }
      t.searchText && (c.push("Try broader search terms"), c.push("Verify spelling and try partial words"), t.responsibleUser || c.push(`Check completed tasks with ${wt}`));
    }
  }
  return M({
    subject: a,
    count: e.length,
    limit: t.limit,
    nextCursor: o ?? void 0,
    filterHints: i,
    previewLines: De(e, Math.min(e.length, t.limit)),
    zeroReasonHints: c
  });
}
const fa = {
  projectId: s.string().min(1).optional().describe(
    "Optional project ID. If provided, shows detailed overview of that project. If omitted, shows overview of all projects."
  )
}, os = s.lazy(
  () => s.object({
    id: s.string().describe("The project ID."),
    name: s.string().describe("The project name."),
    parentId: s.string().optional().describe("The parent project ID (for sub-projects, personal projects only)."),
    folderId: s.string().optional().describe("The folder ID this project belongs to (workspace projects only)."),
    childOrder: s.number().describe("The ordering index among siblings."),
    sections: s.array(
      s.object({
        id: s.string(),
        name: s.string()
      })
    ),
    children: s.array(os).describe("Nested child projects.")
  })
), ba = {
  type: s.enum(["account_overview", "project_overview"]).describe("The type of overview returned."),
  totalProjects: s.number().optional().describe("Total number of projects (account overview only)."),
  totalTasks: s.number().optional().describe("Total number of tasks."),
  totalSections: s.number().optional().describe("Total number of sections (project overview only)."),
  tasksWithoutSection: s.number().optional().describe("Number of tasks not in any section (project overview only)."),
  projectInfo: s.object({
    id: s.string(),
    name: s.string(),
    isShared: s.boolean(),
    isFavorite: s.boolean()
  }).optional().describe("Project information (project overview only)."),
  // Additional fields that exist in structured outputs
  hasNestedProjects: s.boolean().optional().describe("Whether account has nested projects (account overview only)."),
  inbox: s.object({
    id: s.string().describe("The inbox project ID."),
    name: s.string().describe("The inbox project name."),
    sections: s.array(ce).describe("Sections in the inbox project.")
  }).nullable().optional().describe("Inbox information (account overview only)."),
  projects: s.array(s.lazy(() => os)).optional().describe(
    "List of projects with hierarchy, folders, and ordering (account overview only)."
  ),
  project: s.object({
    id: s.string().describe("The project ID."),
    name: s.string().describe("The project name.")
  }).optional().describe("Project details (project overview only)."),
  sections: s.array(ce).optional().describe("List of sections (project overview only)."),
  tasks: s.array(s.any()).optional().describe("List of tasks (project overview only)."),
  stats: s.object({
    totalTasks: s.number(),
    totalSections: s.number(),
    tasksWithoutSection: s.number()
  }).optional().describe("Statistics object (project overview only).")
};
function ga(e) {
  const t = {};
  for (const n of e)
    t[n.id] = {
      ...n,
      children: [],
      childOrder: n.childOrder ?? 0
    };
  const o = [];
  for (const n of e) {
    const a = t[n.id];
    if (a)
      if (ie(n) && n.parentId) {
        const i = t[n.parentId];
        i ? i.children.push(a) : o.push(a);
      } else
        o.push(a);
  }
  function r(n) {
    n.sort((a, i) => a.childOrder - i.childOrder);
    for (const a of n)
      r(a.children);
  }
  return r(o), o;
}
async function ya(e, t) {
  const o = {};
  return await Promise.all(
    t.map(async (r) => {
      const { results: n } = await e.getSections({ projectId: r });
      o[r] = n;
    })
  ), o;
}
function rs(e, t, o = "") {
  const r = [];
  r.push(`${o}- Project: ${e.name} (id=${e.id})`);
  const n = t[e.id] || [];
  for (const a of n)
    r.push(`${o}  - Section: ${a.name} (id=${a.id})`);
  for (const a of e.children)
    r.push(...rs(a, t, `${o}  `));
  return r;
}
function It(e) {
  const t = {};
  for (const r of e)
    t[r.id] = { ...r, children: [] };
  const o = [];
  for (const r of e) {
    const n = t[r.id];
    if (!n) continue;
    if (!r.parentId) {
      o.push(n);
      continue;
    }
    const a = t[r.parentId];
    a ? a.children.push(n) : o.push(n);
  }
  return o;
}
function ze(e, t = "") {
  const o = [];
  for (const r of e) {
    const n = `id=${r.id}`, a = r.dueDate ? `; due=${r.dueDate}` : "", i = `; content=${r.content}`;
    o.push(`${t}- ${n}${a}${i}`), r.children.length > 0 && o.push(...ze(r.children, `${t}  `));
  }
  return o;
}
function ns(e, t) {
  return {
    id: e.id,
    name: e.name,
    parentId: ie(e) ? e.parentId ?? void 0 : void 0,
    folderId: ke(e) ? e.folderId ?? void 0 : void 0,
    childOrder: e.childOrder,
    sections: (t[e.id] || []).map(X),
    children: e.children.map(
      (o) => ns(o, t)
    )
  };
}
async function ka(e, t) {
  let o = [], r;
  do {
    const { results: n, nextCursor: a } = await e.getTasks({
      projectId: t,
      limit: j.TASKS_BATCH_SIZE,
      cursor: r ?? void 0
    });
    o = o.concat(n.map(_)), r = a ?? void 0;
  } while (r);
  return o;
}
async function Ta(e, t) {
  const { results: o } = await e.getSections({ projectId: t });
  return o;
}
async function wa(e) {
  const { results: t } = await e.getProjects({}), o = t.find((u) => ie(u) && u.inboxProject === !0), r = t.filter((u) => !ie(u) || u.inboxProject !== !0), n = ga(r), a = t.map((u) => u.id), i = await ya(e, a), c = ["# Personal Projects", ""];
  if (o) {
    c.push(`- Inbox Project: ${o.name} (id=${o.id})`);
    for (const u of i[o.id] || [])
      c.push(`  - Section: ${u.name} (id=${u.id})`);
  }
  if (n.length)
    for (const u of n)
      c.push(...rs(u, i));
  else
    c.push("_No projects found._");
  c.push("");
  const d = n.some((u) => u.children.length > 0);
  d && c.push(
    "_Note: Indentation indicates that a project is a sub-project of the one above it. This allows for organizing projects hierarchically, with parent projects containing related sub-projects._",
    ""
  );
  const l = c.join(`
`), p = {
    type: "account_overview",
    inbox: o ? {
      id: o.id,
      name: o.name,
      sections: (i[o.id] || []).map(X)
    } : null,
    projects: n.map(
      (u) => ns(u, i)
    ),
    totalProjects: t.length,
    totalSections: a.reduce(
      (u, m) => u + (i[m]?.length || 0),
      0
    ),
    hasNestedProjects: d
  };
  return { textContent: l, structuredContent: p };
}
async function va(e, t) {
  const [o, r, n] = await Promise.all([
    e.getProject(t),
    Ta(e, t),
    ka(e, t)
  ]), a = {};
  for (const p of r)
    a[p.id] = [];
  const i = [];
  for (const p of n)
    (p.sectionId ? a[p.sectionId] ?? i : i).push(p);
  const c = [`# ${o.name}`];
  if (i.length > 0) {
    c.push("");
    const p = It(i);
    c.push(...ze(p));
  }
  for (const p of r) {
    c.push(""), c.push(`## ${p.name}`);
    const u = a[p.id];
    if (!u?.length)
      continue;
    const m = It(u);
    c.push(...ze(m));
  }
  const d = c.join(`
`), l = {
    type: "project_overview",
    project: {
      id: o.id,
      name: o.name
    },
    sections: r.map(X),
    tasks: n.map((p) => ({
      ...p,
      children: []
      // Tasks already include hierarchical info via parentId
    })),
    stats: {
      totalTasks: n.length,
      totalSections: r.length,
      tasksWithoutSection: i.length
    }
  };
  return { textContent: d, structuredContent: l };
}
const Ia = {
  name: f.GET_OVERVIEW,
  description: "Get a Markdown overview. If no projectId is provided, shows all projects with hierarchy and sections (useful for navigation). If projectId is provided, shows detailed overview of that specific project including all tasks grouped by sections.",
  parameters: fa,
  outputSchema: ba,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = e.projectId ? await va(t, e.projectId) : await wa(t);
    return {
      textContent: o.textContent,
      structuredContent: o.structuredContent
    };
  }
}, ja = {}, oe = s.object({
  count: s.number().describe("Number of consecutive periods in this streak."),
  start: s.string().describe("Start date of the streak."),
  end: s.string().describe("End date of the streak.")
}), jt = s.object({
  id: s.string().describe("Project ID."),
  completed: s.number().describe("Number of tasks completed in this project.")
}), Sa = {
  completedCount: s.number().describe("Total number of completed tasks (all-time)."),
  daysItems: s.array(
    s.object({
      date: s.string().describe("Date string (YYYY-MM-DD)."),
      totalCompleted: s.number().describe("Total tasks completed on this day."),
      items: s.array(jt).describe("Per-project completion breakdown for this day.")
    })
  ).describe("Daily completion breakdown (most recent days)."),
  weekItems: s.array(
    s.object({
      from: s.string().describe("Start date of the week."),
      to: s.string().describe("End date of the week."),
      totalCompleted: s.number().describe("Total tasks completed in this week."),
      items: s.array(jt).describe("Per-project completion breakdown for this week.")
    })
  ).describe("Weekly completion breakdown (most recent weeks)."),
  goals: s.object({
    dailyGoal: s.number().describe("Daily task completion goal."),
    weeklyGoal: s.number().describe("Weekly task completion goal."),
    currentDailyStreak: oe.describe("Current daily goal streak."),
    currentWeeklyStreak: oe.describe("Current weekly goal streak."),
    lastDailyStreak: oe.describe("Previous daily goal streak."),
    lastWeeklyStreak: oe.describe("Previous weekly goal streak."),
    maxDailyStreak: oe.describe("Best daily goal streak ever."),
    maxWeeklyStreak: oe.describe("Best weekly goal streak ever."),
    vacationMode: s.boolean().describe("Whether vacation mode is enabled."),
    karmaDisabled: s.boolean().describe("Whether karma tracking is disabled.")
  }).describe("Goal and streak information."),
  karma: s.number().describe("Current karma score."),
  karmaTrend: s.string().describe('Karma trend direction (e.g., "up" or "down").'),
  karmaLastUpdate: s.number().describe("Timestamp of the last karma update."),
  karmaGraphData: s.array(
    s.object({
      date: s.string().describe("Date of the karma data point."),
      karmaAvg: s.number().describe("Average karma for this date.")
    })
  ).describe("Historical karma data points for graphing."),
  karmaUpdateReasons: s.array(
    s.object({
      time: s.string().describe("Timestamp of the karma change."),
      newKarma: s.number().describe("New karma value after the change."),
      positiveKarma: s.number().describe("Positive karma earned."),
      negativeKarma: s.number().describe("Negative karma incurred."),
      positiveKarmaReasons: s.array(s.any()).describe("Reasons for positive karma."),
      negativeKarmaReasons: s.array(s.any()).describe("Reasons for negative karma.")
    })
  ).describe("Recent karma change events with reasons."),
  projectColors: s.record(s.string(), s.string()).describe("Map of project ID to color key.")
}, Ca = {
  name: f.GET_PRODUCTIVITY_STATS,
  description: "Get comprehensive productivity statistics including daily/weekly completion breakdowns, goal streaks (current, last, max), karma score and trends, and historical karma data. Useful for productivity analysis and tracking goal progress.",
  parameters: ja,
  outputSchema: Sa,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const o = await t.getProductivityStats();
    return {
      textContent: $a(o),
      structuredContent: {
        completedCount: o.completedCount,
        daysItems: o.daysItems,
        weekItems: o.weekItems,
        goals: {
          dailyGoal: o.goals.dailyGoal,
          weeklyGoal: o.goals.weeklyGoal,
          currentDailyStreak: o.goals.currentDailyStreak,
          currentWeeklyStreak: o.goals.currentWeeklyStreak,
          lastDailyStreak: o.goals.lastDailyStreak,
          lastWeeklyStreak: o.goals.lastWeeklyStreak,
          maxDailyStreak: o.goals.maxDailyStreak,
          maxWeeklyStreak: o.goals.maxWeeklyStreak,
          vacationMode: o.goals.vacationMode,
          karmaDisabled: o.goals.karmaDisabled
        },
        karma: o.karma,
        karmaTrend: o.karmaTrend,
        karmaLastUpdate: o.karmaLastUpdate,
        karmaGraphData: o.karmaGraphData,
        karmaUpdateReasons: o.karmaUpdateReasons,
        projectColors: o.projectColors
      }
    };
  }
};
function $a(e) {
  const t = [
    "# Productivity Statistics",
    "",
    `**Total Completed Tasks:** ${e.completedCount.toLocaleString()}`,
    `**Karma:** ${e.karma.toLocaleString()} (${e.karmaTrend})`,
    "",
    "## Goals & Streaks",
    `**Daily Goal:** ${e.goals.dailyGoal} tasks/day`,
    `**Weekly Goal:** ${e.goals.weeklyGoal} tasks/week`,
    `**Current Daily Streak:** ${e.goals.currentDailyStreak.count} days (${e.goals.currentDailyStreak.start} to ${e.goals.currentDailyStreak.end})`,
    `**Current Weekly Streak:** ${e.goals.currentWeeklyStreak.count} weeks (${e.goals.currentWeeklyStreak.start} to ${e.goals.currentWeeklyStreak.end})`,
    `**Best Daily Streak:** ${e.goals.maxDailyStreak.count} days`,
    `**Best Weekly Streak:** ${e.goals.maxWeeklyStreak.count} weeks`
  ];
  if (e.daysItems.length > 0) {
    t.push("", "## Recent Daily Completions");
    const o = e.daysItems.slice(0, 7);
    for (const r of o)
      t.push(`  ${r.date}: ${r.totalCompleted} tasks`);
  }
  if (e.weekItems.length > 0) {
    t.push("", "## Recent Weekly Completions");
    const o = e.weekItems.slice(0, 4);
    for (const r of o)
      t.push(`  ${r.from} to ${r.to}: ${r.totalCompleted} tasks`);
  }
  return t.join(`
`);
}
const Da = {
  projectId: s.string().min(1).describe("The ID of the project to get activity stats for."),
  weeks: s.number().int().min(1).max(12).optional().describe("Number of weeks of activity data to retrieve (1-12, default 2)."),
  includeWeeklyCounts: s.boolean().optional().describe("Include weekly rollup counts alongside daily counts.")
}, Aa = {
  projectId: s.string().describe("The project ID."),
  dayItems: s.array(
    s.object({
      date: s.string().describe("Date in ISO format."),
      totalCount: s.number().describe("Number of tasks completed on this day.")
    })
  ).describe("Daily task completion counts."),
  weekItems: s.array(
    s.object({
      fromDate: s.string().describe("Start date of the week."),
      toDate: s.string().describe("End date of the week."),
      totalCount: s.number().describe("Number of tasks completed in this week.")
    })
  ).nullable().describe("Weekly completion rollups. Only included when includeWeeklyCounts is true.")
};
function xa(e, t) {
  const o = [`# Activity Stats: Project ${e}`, ""];
  if (t.dayItems.length > 0) {
    o.push("## Daily Activity");
    for (const r of t.dayItems)
      o.push(`- ${r.date}: ${r.totalCount} completed`);
  } else
    o.push("No daily activity data available.");
  if (t.weekItems && t.weekItems.length > 0) {
    o.push(""), o.push("## Weekly Activity");
    for (const r of t.weekItems)
      o.push(`- ${r.fromDate} to ${r.toDate}: ${r.totalCount} completed`);
  }
  return o.join(`
`);
}
const Ea = {
  name: f.GET_PROJECT_ACTIVITY_STATS,
  description: "Get daily and optional weekly task completion counts for a project over a configurable time window (1-12 weeks). Useful for identifying completion trends and patterns.",
  parameters: Da,
  outputSchema: Aa,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { projectId: o, weeks: r, includeWeeklyCounts: n } = e, a = await t.getProjectActivityStats(o, {
      weeks: r,
      includeWeeklyCounts: n
    });
    return {
      textContent: xa(o, a),
      structuredContent: {
        projectId: o,
        dayItems: a.dayItems,
        weekItems: a.weekItems ?? null
      }
    };
  }
}, Oa = {
  projectId: s.string().min(1).describe("The ID of the project to check health for."),
  includeContext: s.boolean().default(!1).describe(
    "Include detailed health context with project metrics and task-level data. May produce large output for projects with many tasks."
  )
}, Ua = s.object({
  taskId: s.string().describe("The ID of the task this recommendation is for."),
  recommendation: s.string().describe("The recommendation for this task.")
}), Pa = s.object({
  id: s.string().describe("The task ID."),
  content: s.string().describe("The task content/title."),
  priority: s.string().describe("The task priority (1-4)."),
  due: s.string().nullable().optional().describe("The due date string, if set."),
  deadline: s.string().nullable().optional().describe("The deadline date string, if set."),
  isCompleted: s.boolean().describe("Whether the task is completed."),
  labels: s.array(s.string()).describe("Labels applied to this task.")
}), _a = {
  projectId: s.string().describe("The project ID."),
  projectName: s.string().describe("The project name."),
  progress: s.object({
    completedCount: s.number().describe("Number of completed tasks."),
    activeCount: s.number().describe("Number of active (incomplete) tasks."),
    progressPercent: s.number().describe("Completion percentage (0-100).")
  }).describe("Project completion progress."),
  health: s.object({
    status: s.enum(Ve).describe("The overall health status of the project."),
    description: s.string().nullable().optional().describe("Detailed description of the health assessment."),
    descriptionSummary: s.string().nullable().optional().describe("Brief summary of the health assessment."),
    taskRecommendations: s.array(Ua).nullable().optional().describe("Specific recommendations for individual tasks."),
    isStale: s.boolean().describe("Whether the health data is stale and may need refreshing."),
    updateInProgress: s.boolean().describe("Whether a health analysis update is currently in progress."),
    updatedAt: s.string().nullable().optional().describe("When the health assessment was last updated.")
  }).describe("Project health assessment."),
  context: s.object({
    projectDescription: s.string().nullable().describe("The project description, if any."),
    projectMetrics: s.object({
      totalTasks: s.number().describe("Total number of tasks in the project."),
      completedTasks: s.number().describe("Number of completed tasks."),
      overdueTasks: s.number().describe("Number of overdue tasks."),
      tasksCreatedThisWeek: s.number().describe("Tasks created in the current week."),
      tasksCompletedThisWeek: s.number().describe("Tasks completed in the current week."),
      averageCompletionTime: s.number().nullable().describe("Average task completion time in days, if available.")
    }).describe("Aggregated project metrics."),
    tasks: s.array(Pa).describe("Tasks in the project.")
  }).optional().describe(
    "Detailed project context with metrics and task data. Only included when includeContext is true."
  )
};
async function Ra(e, t, o) {
  if (o) {
    const [a, i, c] = await Promise.all([
      e.getProjectProgress(t),
      e.getProjectHealth(t),
      e.getProjectHealthContext(t)
    ]);
    return { progress: a, health: i, context: c };
  }
  const [r, n] = await Promise.all([
    e.getProjectProgress(t),
    e.getProjectHealth(t)
  ]);
  return { progress: r, health: n };
}
function Na(e, { progress: t, health: o, context: r }) {
  const n = [`# Project Health: ${e}`, ""];
  if (n.push(`**Status:** ${o.status}`), o.isStale && n.push("**Note:** Health data is stale and may not reflect recent changes."), o.updateInProgress && n.push("**Note:** A health analysis update is currently in progress."), o.updatedAt && n.push(`**Last Updated:** ${o.updatedAt.toISOString()}`), n.push(""), n.push("## Progress"), n.push(
    `**Completed:** ${t.completedCount} | **Active:** ${t.activeCount} | **Progress:** ${t.progressPercent}%`
  ), o.description && (n.push(""), n.push("## Health Description"), n.push(o.description)), o.taskRecommendations && o.taskRecommendations.length > 0) {
    n.push(""), n.push("## Task Recommendations");
    for (const a of o.taskRecommendations)
      n.push(`- **Task ${a.taskId}**: ${a.recommendation}`);
  }
  if (r) {
    n.push(""), n.push("## Project Metrics");
    const a = r.projectMetrics;
    if (n.push(`- **Total Tasks:** ${a.totalTasks}`), n.push(`- **Completed:** ${a.completedTasks}`), n.push(`- **Overdue:** ${a.overdueTasks}`), n.push(`- **Created This Week:** ${a.tasksCreatedThisWeek}`), n.push(`- **Completed This Week:** ${a.tasksCompletedThisWeek}`), a.averageCompletionTime !== null && n.push(`- **Avg Completion Time:** ${a.averageCompletionTime} days`), r.tasks.length > 0) {
      n.push(""), n.push(`## Tasks (${r.tasks.length})`);
      for (const i of r.tasks) {
        const c = [`id=${i.id}`, i.content];
        i.due && c.push(`due=${i.due}`), i.deadline && c.push(`deadline=${i.deadline}`), i.isCompleted && c.push("(completed)"), n.push(`- ${c.join("; ")}`);
      }
    }
  }
  return n.join(`
`);
}
const La = {
  name: f.GET_PROJECT_HEALTH,
  description: "Get a comprehensive health assessment for a project including completion progress, health status (EXCELLENT, ON_TRACK, AT_RISK, CRITICAL), and optional detailed context with project metrics and task-level recommendations. Use includeContext=true for full detail including task data.",
  parameters: Oa,
  outputSchema: _a,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { projectId: o, includeContext: r } = e, n = await Ra(t, o, r), a = n.context?.projectName ?? `Project ${o}`, i = Na(a, n), c = n.context ? {
      projectDescription: n.context.projectDescription,
      projectMetrics: n.context.projectMetrics,
      tasks: n.context.tasks.map((d) => ({
        id: d.id,
        content: d.content,
        priority: d.priority,
        due: d.due ?? null,
        deadline: d.deadline ?? null,
        isCompleted: d.isCompleted,
        labels: d.labels
      }))
    } : void 0;
    return {
      textContent: i,
      structuredContent: {
        projectId: o,
        projectName: a,
        progress: {
          completedCount: n.progress.completedCount,
          activeCount: n.progress.activeCount,
          progressPercent: n.progress.progressPercent
        },
        health: {
          status: n.health.status,
          description: n.health.description ?? null,
          descriptionSummary: n.health.descriptionSummary ?? null,
          taskRecommendations: n.health.taskRecommendations ?? null,
          isStale: n.health.isStale,
          updateInProgress: n.health.updateInProgress,
          updatedAt: n.health.updatedAt?.toISOString() ?? null
        },
        context: c
      }
    };
  }
}, Ma = {
  workspaceIdOrName: s.string().min(1).describe(
    "The workspace ID or name. Supports exact ID, exact name match (case-insensitive), or unique partial name match."
  ),
  projectIds: s.array(s.string().min(1)).min(1).optional().describe("Optional list of project IDs to scope insights to specific projects.")
}, Fa = s.object({
  projectId: s.string().describe("The project ID."),
  health: s.object({
    status: s.enum(Ve).describe("The health status of the project."),
    isStale: s.boolean().describe("Whether the health data is stale."),
    updateInProgress: s.boolean().describe("Whether a health analysis update is in progress.")
  }).nullable().describe("Health data for this project, if available."),
  progress: s.object({
    completedCount: s.number().describe("Number of completed tasks."),
    activeCount: s.number().describe("Number of active tasks."),
    progressPercent: s.number().describe("Completion percentage (0-100).")
  }).nullable().describe("Progress data for this project, if available.")
}), Ha = {
  workspaceId: s.string().describe("The resolved workspace ID."),
  workspaceName: s.string().describe("The resolved workspace name."),
  folderId: s.string().nullable().describe("The folder ID, if applicable."),
  projectInsights: s.array(Fa).describe("Health and progress insights for each project in the workspace.")
}, Wa = {
  name: f.GET_WORKSPACE_INSIGHTS,
  description: "Get aggregated health and progress insights across all projects in a workspace. Accepts workspace name or ID, with optional project ID filtering. Useful for a cross-project health overview.",
  parameters: Ma,
  outputSchema: Ha,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute(e, t) {
    const { workspaceIdOrName: o, projectIds: r } = e, n = await Bt.resolveWorkspace(t, o), a = await t.getWorkspaceInsights(n.workspaceId, { projectIds: r }), i = a.projectInsights.map((d) => ({
      projectId: d.projectId,
      health: d.health ? {
        status: d.health.status,
        isStale: d.health.isStale,
        updateInProgress: d.health.updateInProgress
      } : null,
      progress: d.progress ? {
        completedCount: d.progress.completedCount,
        activeCount: d.progress.activeCount,
        progressPercent: d.progress.progressPercent
      } : null
    })), c = [
      `# Workspace Insights: ${n.workspaceName}`,
      "",
      `**Projects:** ${i.length}`,
      ""
    ];
    for (const d of i) {
      const l = d.health?.status ?? "N/A", p = d.progress ? `${d.progress.progressPercent}%` : "N/A";
      c.push(`- Project ${d.projectId}: status=${l}, progress=${p}`);
    }
    return {
      textContent: c.join(`
`),
      structuredContent: {
        workspaceId: n.workspaceId,
        workspaceName: n.workspaceName,
        folderId: a.folderId,
        projectInsights: i
      }
    };
  }
}, St = 50, Ga = {
  goalId: s.string().min(1).describe("The ID of the goal."),
  taskIds: s.array(s.string().min(1)).min(1).max(St).describe(`The IDs of the tasks to link or unlink (max ${St}).`),
  action: s.enum(["link", "unlink"]).describe("Whether to link or unlink the tasks.")
}, Ba = {
  processed: s.array(s.string()).describe("The IDs of successfully processed tasks."),
  failures: s.array(be).describe("Failed operations with error details."),
  totalRequested: s.number().describe("The total number of tasks requested."),
  successCount: s.number().describe("The number of successfully processed tasks."),
  failureCount: s.number().describe("The number of failed operations.")
}, Ya = {
  name: f.LINK_GOAL_TASKS,
  description: "Link or unlink tasks to/from a goal.",
  parameters: Ga,
  outputSchema: Ba,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = await Promise.allSettled(
      e.taskIds.map(async (c) => (e.action === "link" ? await t.linkTaskToGoal({ goalId: e.goalId, taskId: c }) : await t.unlinkTaskFromGoal({ goalId: e.goalId, taskId: c }), c))
    ), r = [], n = [];
    o.forEach((c, d) => {
      const l = e.taskIds[d];
      if (c.status === "fulfilled")
        r.push(l);
      else {
        const p = c.reason instanceof Error ? c.reason.message : "Unknown error";
        n.push({ item: l, error: p });
      }
    });
    const a = e.action === "link" ? "Linked tasks to goal" : "Unlinked tasks from goal";
    return {
      textContent: ge({
        action: a,
        success: r.length,
        total: e.taskIds.length,
        successItems: r,
        failures: n
      }),
      structuredContent: {
        processed: r,
        failures: n,
        totalRequested: e.taskIds.length,
        successCount: r.length,
        failureCount: n.length
      }
    };
  }
}, qa = {}, za = {
  id: s.string().describe("The unique identifier for the workspace."),
  name: s.string().describe("The name of the workspace."),
  plan: s.enum(Is).describe("The workspace plan type."),
  role: s.enum(vs).optional().describe("The user's role in the workspace, if available."),
  isLinkSharingEnabled: s.boolean().describe("Whether link sharing is enabled for the workspace."),
  isGuestAllowed: s.boolean().describe("Whether guests are allowed in the workspace."),
  createdAt: s.string().optional().describe("The ISO 8601 timestamp when the workspace was created."),
  creatorId: s.string().describe("The ID of the user who created the workspace.")
}, Ka = {
  type: s.literal("workspaces").describe("The type of the response."),
  workspaces: s.array(s.object(za)).describe("List of workspaces."),
  count: s.number().describe("The total number of workspaces.")
};
async function Va(e) {
  const o = (await e.getWorkspaces()).map((i) => ({
    id: i.id,
    name: i.name,
    plan: i.plan,
    role: i.role,
    isLinkSharingEnabled: i.isLinkSharingEnabled,
    isGuestAllowed: i.isGuestAllowed,
    createdAt: i.createdAt?.toISOString(),
    creatorId: i.creatorId
  })), r = ["# Workspaces", ""];
  if (o.length === 0)
    r.push("No workspaces found.");
  else {
    r.push(
      `Found ${o.length} workspace${o.length === 1 ? "" : "s"}:`,
      ""
    );
    for (const i of o)
      r.push(`## ${i.name}`), r.push(`- **ID:** ${i.id}`), r.push(`- **Plan:** ${i.plan}`), i.role && r.push(`- **Your Role:** ${i.role}`), r.push(
        `- **Link Sharing:** ${i.isLinkSharingEnabled ? "Enabled" : "Disabled"}`
      ), r.push(`- **Guests Allowed:** ${i.isGuestAllowed ? "Yes" : "No"}`), i.createdAt && r.push(`- **Created:** ${i.createdAt}`), r.push(`- **Creator ID:** ${i.creatorId}`), r.push("");
  }
  const n = r.join(`
`), a = {
    type: "workspaces",
    workspaces: o,
    count: o.length
  };
  return { textContent: n, structuredContent: a };
}
const Ja = {
  name: f.LIST_WORKSPACES,
  description: "Get all workspaces for the authenticated user. Returns workspace details including ID, name, plan type (STARTER/BUSINESS), user role (ADMIN/MEMBER/GUEST), link sharing settings, guest permissions, creation date, and creator ID.",
  parameters: qa,
  outputSchema: Ka,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = await Va(t);
    return {
      textContent: o.textContent,
      structuredContent: o.structuredContent
    };
  }
}, { FIND_TASKS: Xa, FIND_PROJECT_COLLABORATORS: Ct, UPDATE_TASKS: Za } = f, Qa = 50, ei = {
  operation: s.enum(["assign", "unassign", "reassign"]).describe("The assignment operation to perform."),
  taskIds: s.array(s.string()).min(1).max(Qa).describe("The IDs of the tasks to operate on (max 50)."),
  responsibleUser: s.string().optional().describe(
    'The user to assign tasks to. Can be "me" (assigns to current user), a user ID, name, or email. Required for assign and reassign operations.'
  ),
  fromAssigneeUser: s.string().optional().describe(
    "For reassign operations: the current assignee to reassign from. Can be user ID, name, or email. Optional - if not provided, reassigns from any current assignee."
  ),
  dryRun: s.boolean().optional().default(!1).describe("If true, validates operations without executing them.")
}, ti = {
  results: s.array(
    s.object({
      taskId: s.string().describe("The ID of the task."),
      success: s.boolean().describe("Whether the operation was successful."),
      error: s.string().optional().describe("Error message if the operation failed."),
      originalAssigneeId: s.string().optional().describe("The original assignee ID before the operation."),
      newAssigneeId: s.string().optional().describe("The new assignee ID after the operation.")
    })
  ).describe("Results of the assignment operations."),
  summary: s.object({
    total: s.number().describe("Total number of tasks processed."),
    succeeded: s.number().describe("Number of successful operations."),
    failed: s.number().describe("Number of failed operations."),
    dryRun: s.boolean().describe("Whether this was a dry run.")
  }).optional().describe("Summary of the operation.")
}, si = {
  name: f.MANAGE_ASSIGNMENTS,
  description: "Bulk assignment operations for multiple tasks. Supports assign, unassign, and reassign operations with atomic rollback on failures.",
  parameters: ei,
  outputSchema: ti,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { operation: o, taskIds: r, responsibleUser: n, fromAssigneeUser: a, dryRun: i } = e;
    if ((o === "assign" || o === "reassign") && !n)
      throw new Error(`${o} operation requires responsibleUser parameter`);
    const c = await Promise.allSettled(
      r.map(async (w) => {
        try {
          return await t.getTask(w);
        } catch {
          throw new Error(`Task ${w} not found or not accessible`);
        }
      })
    ), d = [], l = [];
    for (let w = 0; w < c.length; w++) {
      const y = c[w];
      y && y.status === "fulfilled" ? d.push(y.value) : y && y.status === "rejected" ? l.push({
        taskId: r[w] || "invalid-task-id",
        success: !1,
        error: y.reason?.message || "Task not accessible"
      }) : l.push({
        taskId: r[w] || "invalid-task-id",
        success: !1,
        error: "Task not accessible"
      });
    }
    if (d.length === 0) {
      const w = l.map((y) => `"${y.taskId}": ${y.error}`).join("; ");
      throw new Error(`All ${l.length} task(s) failed: ${w}`);
    }
    let p;
    o === "reassign" && a && (p = (await G.resolveUser(t, a))?.userId || a);
    const u = [];
    for (const w of d)
      o === "reassign" && p && w.responsibleUid !== p || u.push({
        taskId: w.id,
        projectId: w.projectId,
        responsibleUid: n || ""
        // Will be validated appropriately
      });
    if (o === "unassign") {
      if (i) {
        const v = d.map((S) => ({
          taskId: S.id,
          success: !0,
          originalAssigneeId: S.responsibleUid ?? void 0,
          newAssigneeId: void 0
        }));
        return {
          textContent: _e({
            operation: o,
            results: v,
            dryRun: !0
          }),
          structuredContent: {
            operation: o,
            results: v,
            totalRequested: r.length,
            successful: v.length,
            failed: l.length,
            dryRun: !0
          }
        };
      }
      const w = d.map(async (v) => {
        try {
          return await t.updateTask(v.id, { assigneeId: null }), {
            taskId: v.id,
            success: !0,
            originalAssigneeId: v.responsibleUid ?? void 0,
            newAssigneeId: void 0
          };
        } catch ($) {
          return {
            taskId: v.id,
            success: !1,
            error: $ instanceof Error ? $.message : "Update failed",
            originalAssigneeId: v.responsibleUid ?? void 0
          };
        }
      }), y = await Promise.all(w), D = [...y, ...l];
      if (D.length > 0 && D.every((v) => !v.success)) {
        const v = D.map(($) => `"${$.taskId}": ${$.error}`).join("; ");
        throw new Error(`All ${D.length} unassign operation(s) failed: ${v}`);
      }
      return {
        textContent: _e({
          operation: o,
          results: D,
          dryRun: !1
        }),
        structuredContent: {
          operation: o,
          results: D,
          totalRequested: r.length,
          successful: y.filter((v) => v.success).length,
          failed: D.filter((v) => !v.success).length,
          dryRun: !1
        }
      };
    }
    const m = await rt.validateBulkAssignment(
      t,
      u
    ), h = [], k = [];
    for (let w = 0; w < u.length; w++) {
      const y = u[w], D = m[w];
      y && D?.isValid ? h.push({ assignment: y, validation: D }) : y?.taskId && k.push({
        taskId: y.taskId,
        success: !1,
        error: D?.error?.message || "Validation failed"
      });
    }
    async function b(w, y) {
      const D = w.filter(
        (v) => v.assignment != null && v.validation != null
      );
      if (!y)
        return D.map(({ assignment: v, validation: $ }) => {
          const S = d.find((A) => A.id === v.taskId);
          if (!v.taskId || !$.resolvedUser?.userId)
            throw new Error(
              "Invalid assignment or validation data - this should not happen"
            );
          return {
            taskId: v.taskId,
            success: !0,
            originalAssigneeId: S?.responsibleUid ?? void 0,
            newAssigneeId: $.resolvedUser.userId
          };
        });
      const R = D.map(
        async ({ assignment: v, validation: $ }) => {
          const S = d.find((A) => A.id === v.taskId);
          if (!v.taskId || !$.resolvedUser?.userId)
            return {
              taskId: v.taskId || "unknown-task",
              success: !1,
              error: "Invalid assignment data - missing task ID or resolved user",
              originalAssigneeId: S?.responsibleUid ?? void 0
            };
          try {
            return await t.updateTask(v.taskId, {
              assigneeId: $.resolvedUser.userId
            }), {
              taskId: v.taskId,
              success: !0,
              originalAssigneeId: S?.responsibleUid ?? void 0,
              newAssigneeId: $.resolvedUser.userId
            };
          } catch (A) {
            return {
              taskId: v.taskId,
              success: !1,
              error: A instanceof Error ? A.message : "Update failed",
              originalAssigneeId: S?.responsibleUid ?? void 0
            };
          }
        }
      );
      return Promise.all(R);
    }
    const C = await b(h, !i), I = [...C, ...k, ...l];
    if (I.length > 0 && I.every((w) => !w.success)) {
      const w = I.map((y) => `"${y.taskId}": ${y.error}`).join("; ");
      throw new Error(`All ${I.length} ${o} operation(s) failed: ${w}`);
    }
    return {
      textContent: _e({
        operation: o,
        results: I,
        dryRun: i
      }),
      structuredContent: {
        operation: o,
        results: I,
        totalRequested: r.length,
        successful: C.filter((w) => w.success).length,
        failed: I.filter((w) => !w.success).length,
        dryRun: i
      }
    };
  }
};
function _e({
  operation: e,
  results: t,
  dryRun: o
}) {
  const r = t.filter((d) => d.success), n = t.filter((d) => !d.success), a = o ? "would be" : "were", i = {
    assign: "assigned",
    unassign: "unassigned",
    reassign: "reassigned"
  }[e];
  let c = `**${o ? "Dry Run: " : ""}Bulk ${e} operation**

`;
  if (r.length > 0) {
    c += `**${r.length} task${r.length === 1 ? "" : "s"} ${a} successfully ${i}**
`;
    const d = r.slice(0, 5);
    for (const l of d) {
      let p = "";
      e === "unassign" ? p = " (unassigned from previous assignee)" : l.newAssigneeId && (p = ` → ${l.newAssigneeId}`), c += `  • Task ${l.taskId}${p}
`;
    }
    r.length > 5 && (c += `  • ... and ${r.length - 5} more
`), c += `
`;
  }
  if (n.length > 0) {
    c += `**${n.length} task${n.length === 1 ? "" : "s"} failed**
`;
    const d = n.slice(0, 5);
    for (const l of d)
      c += `  • Task ${l.taskId}: ${l.error}
`;
    n.length > 5 && (c += `  • ... and ${n.length - 5} more failures
`), c += `
`;
  }
  return !o && r.length > 0 ? (c += `**Next steps:**
`, c += `• Use ${Xa} with responsibleUser to see ${e === "unassign" ? "unassigned" : "newly assigned"} tasks
`, c += `• Use ${Za} for individual assignment changes
`, n.length > 0 && (c += `• Check failed tasks and use ${Ct} to verify collaborator access
`)) : o ? (c += `**To execute:**
`, c += `• Remove dryRun parameter and run again to execute changes
`, r.length > 0 && (c += `• ${r.length} task${r.length === 1 ? "" : "s"} ready for ${e} operation
`), n.length > 0 && (c += `• Fix ${n.length} validation error${n.length === 1 ? "" : "s"} before executing
`)) : r.length === 0 && (c += `**Suggestions:**
`, c += `• Use ${Ct} to find valid assignees
`, c += `• Check task IDs and assignee permissions
`, c += `• Use dryRun=true to validate before executing
`), c;
}
const oi = {
  action: s.enum(["archive", "unarchive"]).describe("The action to perform on the project."),
  projectId: s.string().min(1).describe("The ID of the project.")
}, ri = {
  project: ue.describe("The updated project."),
  success: s.boolean().describe("Whether the action was successful.")
}, ni = {
  name: f.PROJECT_MANAGEMENT,
  description: "Archive or unarchive a project by its ID.",
  parameters: oi,
  outputSchema: ri,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = e.action === "archive" ? await t.archiveProject(e.projectId) : await t.unarchiveProject(e.projectId), r = V(o);
    return {
      textContent: `${e.action === "archive" ? "Archived" : "Unarchived"} project: ${r.name} (id=${r.id})`,
      structuredContent: {
        project: r,
        success: !0
      }
    };
  }
}, ai = {
  action: s.enum(["move-to-workspace", "move-to-personal"]).describe("The action to perform on the project."),
  projectId: s.string().min(1).describe("The ID of the project to move."),
  workspaceId: s.string().min(1).optional().describe("The target workspace ID. Required when action is move-to-workspace."),
  folderId: s.string().min(1).optional().describe("Optional target folder ID within the workspace."),
  visibility: s.enum(["restricted", "team", "public"]).optional().describe(
    "Optional access visibility for the project in the workspace (restricted, team, or public)."
  )
}, ii = {
  project: ue.describe("The moved project."),
  success: s.boolean().describe("Whether the move was successful.")
}, ci = {
  name: f.PROJECT_MOVE,
  description: "Move a project between personal and workspace contexts.",
  parameters: ai,
  outputSchema: ii,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    let o;
    if (e.action === "move-to-workspace") {
      if (!e.workspaceId)
        throw new Error("workspaceId is required when action is move-to-workspace");
      const a = {
        projectId: e.projectId,
        workspaceId: e.workspaceId
      };
      e.folderId && (a.folderId = e.folderId), e.visibility && (a.access = { visibility: e.visibility }), o = await t.moveProjectToWorkspace(a);
    } else
      o = await t.moveProjectToPersonal({ projectId: e.projectId });
    const r = V(o);
    return {
      textContent: `${e.action === "move-to-workspace" ? "Moved to workspace" : "Moved to personal"}: ${r.name} (id=${r.id})`,
      structuredContent: {
        project: r,
        success: !0
      }
    };
  }
}, as = ["project", "section"], di = {
  type: s.enum(as).describe(
    'The type of entity to reorder. "project" reorders sibling projects within the same parent (and can move projects to a new parent). "section" reorders sections within the same project.'
  ),
  items: s.array(
    s.object({
      id: s.string().min(1).describe("The ID of the entity."),
      order: s.number().int().min(0).optional().describe(
        "The new position/order value for the entity. Lower values appear first."
      ),
      parentId: s.string().min(1).optional().describe(
        'Move a project to be a sub-project of this parent project ID, or use "root" to move to the top level. Only valid when type is "project".'
      )
    })
  ).min(1).describe(
    "The items to reorder or move. Each item must have at least order or parentId. Items with parentId will be moved first, then items with order will be reordered. All items being reordered should be siblings for predictable results."
  )
}, li = {
  type: s.enum(as).describe("The type of entity that was reordered/moved."),
  movedCount: s.number().describe("The number of entities moved to a new parent."),
  reorderedCount: s.number().describe("The number of entities reordered."),
  affectedIds: s.array(s.string()).describe("The IDs of all affected entities."),
  success: s.boolean().describe("Whether the operation was successful.")
}, ui = {
  name: f.REORDER_OBJECTS,
  description: 'Reorder sibling projects or sections, and optionally move projects to a new parent. For projects: set order to reorder siblings, and/or set parentId to move under a new parent (use "root" for top level). For sections: set order to reorder within a project.',
  parameters: di,
  outputSchema: li,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { type: o, items: r } = e, n = /* @__PURE__ */ new Set();
    for (const u of r) {
      if (n.has(u.id))
        throw new Error(`Duplicate item id=${u.id}. Each item must appear only once.`);
      if (n.add(u.id), u.order === void 0 && u.parentId === void 0)
        throw new Error(
          `Item id=${u.id} must have at least one of "order" or "parentId".`
        );
      if (u.parentId !== void 0 && o !== "project")
        throw new Error(
          `parentId is only supported when type is "project", but type is "${o}".`
        );
    }
    const a = [], i = r.filter((u) => u.parentId !== void 0);
    for (const u of i) {
      const m = u.parentId;
      a.push(
        q("project_move", {
          id: u.id,
          parentId: m === "root" ? null : m
        })
      );
    }
    const c = r.filter(
      (u) => u.order !== void 0
    );
    c.length > 0 && (o === "project" ? a.push(
      q("project_reorder", {
        projects: c.map((u) => ({
          id: u.id,
          childOrder: u.order
        }))
      })
    ) : a.push(
      q("section_reorder", {
        sections: c.map((u) => ({
          id: u.id,
          sectionOrder: u.order
        }))
      })
    ));
    try {
      await t.sync({ commands: a });
    } catch (u) {
      const m = u instanceof Error ? u.message : String(u);
      throw new Error(`Reorder failed: ${m}`);
    }
    const d = o === "project" ? "projects" : "sections", l = r.map((u) => u.id), p = [];
    return i.length > 0 && p.push(`moved ${i.length}`), c.length > 0 && p.push(`reordered ${c.length}`), {
      textContent: `${p.join(" and ")} ${d}: ${l.map((u) => `id=${u}`).join(", ")}`,
      structuredContent: {
        type: o,
        movedCount: i.length,
        reorderedCount: c.length,
        affectedIds: l,
        success: !0
      }
    };
  }
}, pi = s.object({
  id: s.string().min(1).describe("The ID of the task to reschedule."),
  date: s.string().min(1).describe(
    "The new date for the task. Use YYYY-MM-DD for date-only, or YYYY-MM-DDTHH:MM:SS for datetime. If date-only is provided and the task already has a specific time, the existing time is preserved."
  )
}), mi = {
  tasks: s.array(pi).min(1).describe("The tasks to reschedule with their new dates.")
}, hi = {
  tasks: s.array(J).describe("The rescheduled tasks."),
  totalCount: s.number().describe("The total number of tasks rescheduled."),
  rescheduledTaskIds: s.array(s.string()).describe("The IDs of the rescheduled tasks.")
}, fi = {
  name: f.RESCHEDULE_TASKS,
  description: "Reschedule tasks to new dates while preserving recurring schedules. Unlike update-tasks (which replaces the entire due string and can wipe recurrence), this tool changes only the date, keeping recurrence patterns intact. Use this when moving recurring tasks to a different date without altering their repeat pattern.",
  parameters: mi,
  outputSchema: hi,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { tasks: o } = e, n = (await Promise.all(
      o.map(async (d) => {
        const l = await t.getTask(d.id);
        return { input: d, task: l };
      })
    )).map(({ input: d, task: l }) => {
      if (!l.due)
        throw new Error(
          `Task "${l.content}" (${l.id}) has no due date. Rescheduling requires an existing due date.`
        );
      const p = bi(d.date, l.due);
      return q("item_update", {
        id: d.id,
        due: {
          date: p,
          string: l.due.string,
          isRecurring: l.due.isRecurring,
          timezone: l.due.timezone ?? void 0,
          lang: l.due.lang ?? void 0
        }
      });
    });
    try {
      await t.sync({ commands: n });
    } catch (d) {
      const l = d instanceof Error ? d.message : String(d);
      throw new Error(`Reschedule failed: ${l}`);
    }
    const a = await Promise.all(o.map((d) => t.getTask(d.id))), i = a.map(_);
    return {
      textContent: nt("Rescheduled", i, {
        showDetails: i.length <= 5
      }),
      structuredContent: {
        tasks: i,
        totalCount: i.length,
        rescheduledTaskIds: a.map((d) => d.id)
      }
    };
  }
};
function bi(e, t) {
  if (/^\d{4}-\d{2}-\d{2}$/.test(e) && t.datetime) {
    const r = t.datetime.substring(10);
    return e + r;
  }
  return e;
}
const gi = {
  query: s.string().min(1).describe("The search query string to find tasks and projects.")
}, yi = {
  results: s.array(
    s.object({
      id: s.string().describe("The ID of the result."),
      title: s.string().describe("The title of the result."),
      url: s.string().describe("The URL of the result.")
    })
  ).describe("The search results."),
  totalCount: s.number().describe("Total number of results found.")
}, ki = {
  name: f.SEARCH,
  description: "Search across tasks and projects in Todoist. Returns a list of relevant results with IDs, titles, and URLs.",
  parameters: gi,
  outputSchema: yi,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const { query: o } = e, [r, n] = await Promise.all([
      tt({
        client: t,
        query: `search: ${o}`,
        limit: j.TASKS_MAX,
        cursor: void 0
      }),
      Wt(t, o)
    ]), a = [];
    for (const i of r.tasks)
      a.push({
        id: `task:${i.id}`,
        title: i.content,
        url: Ut(i.id)
      });
    for (const i of n)
      a.push({
        id: `project:${i.id}`,
        title: i.name,
        url: Pt(i.id)
      });
    return {
      textContent: JSON.stringify({ results: a }),
      structuredContent: { results: a, totalCount: a.length }
    };
  }
}, Ti = {
  ids: s.array(s.string().min(1)).min(1).describe("The IDs of the tasks to uncomplete.")
}, wi = {
  uncompleted: s.array(s.string()).describe("The IDs of successfully uncompleted tasks."),
  failures: s.array(be).describe("Failed task uncompletion with error details."),
  totalRequested: s.number().describe("The total number of tasks requested to uncomplete."),
  successCount: s.number().describe("The number of successfully uncompleted tasks."),
  failureCount: s.number().describe("The number of failed task uncompletions.")
}, vi = {
  name: f.UNCOMPLETE_TASKS,
  description: "Uncomplete (reopen) one or more completed tasks by their IDs.",
  parameters: Ti,
  outputSchema: wi,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !1 },
  async execute(e, t) {
    const o = [], r = [];
    for (const a of e.ids)
      try {
        await t.reopenTask(a), o.push(a);
      } catch (i) {
        const c = i instanceof Error ? i.message : "Unknown error";
        r.push({
          item: a,
          error: c
        });
      }
    return {
      textContent: Ii({
        uncompleted: o,
        failures: r,
        args: e
      }),
      structuredContent: {
        uncompleted: o,
        failures: r,
        totalRequested: e.ids.length,
        successCount: o.length,
        failureCount: r.length
      }
    };
  }
};
function Ii({
  uncompleted: e,
  failures: t,
  args: o
}) {
  return ge({
    action: "Uncompleted tasks",
    success: e.length,
    total: o.ids.length,
    successItems: e,
    successLabel: "Reopened",
    failures: t
  });
}
const ji = s.object({
  id: s.string().min(1).describe("The ID of the comment to update."),
  content: s.string().min(1).describe("The new content for the comment.")
}), Si = {
  comments: s.array(ji).min(1).describe("The comments to update.")
}, Ci = {
  comments: s.array(je).describe("The updated comments."),
  totalCount: s.number().describe("The total number of comments updated."),
  updatedCommentIds: s.array(s.string()).describe("The IDs of the updated comments."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of comments updated.")
  }).describe("Summary of operations performed.")
}, $i = {
  name: f.UPDATE_COMMENTS,
  description: "Update multiple existing comments with new content.",
  parameters: Si,
  outputSchema: Ci,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { comments: o } = e, r = o.map(async (c) => await t.updateComment(c.id, { content: c.content })), a = (await Promise.all(r)).map(we);
    return {
      textContent: Di({
        comments: a
      }),
      structuredContent: {
        comments: a,
        totalCount: a.length,
        updatedCommentIds: a.map((c) => c.id),
        appliedOperations: {
          updateCount: a.length
        }
      }
    };
  }
};
function Di({ comments: e }) {
  const t = e.filter((a) => a.taskId).length, o = e.filter((a) => a.projectId).length, r = [];
  if (t > 0) {
    const a = t > 1 ? "comments" : "comment";
    r.push(`${t} task ${a}`);
  }
  if (o > 0) {
    const a = o > 1 ? "comments" : "comment";
    r.push(`${o} project ${a}`);
  }
  return r.length > 0 ? `Updated ${r.join(" and ")}` : "No comments updated";
}
const Ai = s.object({
  id: s.string().min(1).describe("The ID of the filter to update."),
  name: s.string().min(1).optional().describe("The new name of the filter."),
  query: s.string().min(1).optional().describe(
    'The new filter query string. Examples: "today & p1", "#Work & overdue", "@email & today".'
  ),
  color: de,
  isFavorite: s.boolean().optional().describe("Whether to mark the filter as a favorite.")
}), xi = {
  filters: s.array(Ai).min(1).describe("The filters to update.")
}, Ei = {
  filters: s.array(ot).describe("The updated filters."),
  totalCount: s.number().describe("The total number of filters updated."),
  updatedFilterIds: s.array(s.string()).describe("The IDs of the updated filters."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of filters actually updated."),
    skippedCount: s.number().describe("The number of filters skipped (no changes).")
  }).describe("Summary of operations performed.")
}, Oi = {
  name: f.UPDATE_FILTERS,
  description: "Update one or more existing personal filters with new values.",
  parameters: xi,
  outputSchema: Ei,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { filters: o } = e, r = [], a = o.map((m) => {
      const h = Pi(m);
      return h !== null ? { kind: "skipped", reason: h } : (r.push(m), { kind: "updated", filter: m });
    }).filter((m) => m.kind === "skipped").length;
    if (r.length === 0)
      return {
        textContent: `Updated 0 filters (${a} skipped - no changes)`,
        structuredContent: {
          filters: [],
          totalCount: 0,
          updatedFilterIds: [],
          appliedOperations: { updateCount: 0, skippedCount: a }
        }
      };
    const i = r.map((m) => {
      const { id: h, color: k, ...b } = m;
      return q("filter_update", {
        id: h,
        ...b,
        ...k !== void 0 ? { color: k } : {}
      });
    });
    await t.sync({ commands: i });
    const d = ((await t.sync({ resourceTypes: ["filters"], syncToken: "*" })).filters ?? []).filter((m) => !m.isDeleted), l = new Set(r.map((m) => m.id)), p = d.filter((m) => l.has(m.id)).map((m) => ({
      id: m.id,
      name: m.name,
      query: m.query,
      color: le.parse(m.color),
      isFavorite: m.isFavorite,
      itemOrder: m.itemOrder
    }));
    return {
      textContent: Ui({ filters: p, skippedCount: a }),
      structuredContent: {
        filters: p,
        totalCount: p.length,
        updatedFilterIds: p.map((m) => m.id),
        appliedOperations: {
          updateCount: p.length,
          skippedCount: a
        }
      }
    };
  }
};
function Ui({
  filters: e,
  skippedCount: t
}) {
  const o = e.length;
  let r = `Updated ${o} filter${o === 1 ? "" : "s"}`;
  if (t > 0 && (r += ` (${t} skipped - no changes)`), o > 0) {
    const n = e.map((a) => `• ${a.name} (id=${a.id})`).join(`
`);
    r += `:
${n}`;
  }
  return r;
}
function Pi({ id: e, ...t }) {
  return Object.values(t).every((r) => r === void 0) ? "no-fields" : null;
}
const ne = "remove", Ke = "unassign", $t = 25, _i = s.object({
  id: s.string().min(1).describe("The ID of the goal to update."),
  name: s.string().min(1).optional().describe("New goal name."),
  description: s.preprocess(
    (e) => e === null ? ne : e,
    s.string().describe(`New description. Use "${ne}" to clear.`)
  ).optional(),
  deadline: s.preprocess(
    (e) => e === null ? ne : e,
    s.string().describe(`New deadline (YYYY-MM-DD). Use "${ne}" to clear.`)
  ).optional(),
  responsibleUid: s.preprocess(
    (e) => e === null ? Ke : e,
    s.string().describe(`New responsible user ID. Use "${Ke}" to clear.`)
  ).optional()
}), Ri = ["no-fields", "no-valid-values"], Ni = {
  goals: s.array(_i).min(1).max($t).describe(`The array of goals to update (max ${$t}).`)
}, Li = s.object({
  id: s.string().describe("The ID of the goal that was skipped."),
  reason: s.enum(Ri).describe(
    '"no-fields" = only the id was supplied; "no-valid-values" = all updatable fields were undefined.'
  )
}), Mi = {
  goals: s.array(Se).describe("The updated goals."),
  totalCount: s.number().describe("The total number of goals updated."),
  updatedGoalIds: s.array(s.string()).describe("The IDs of the updated goals."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of goals actually updated."),
    skippedCount: s.number().describe("The number of goals skipped (no changes)."),
    skipped: s.array(Li).describe("Per-goal skip details (id + reason).")
  }).describe("Summary of operations performed.")
};
function Re(e, t) {
  return e === t ? null : e;
}
function Fi({ id: e, ...t }) {
  const o = Object.values(t);
  return o.length === 0 ? "no-fields" : o.every((r) => r === void 0) ? "no-valid-values" : null;
}
const Hi = {
  name: f.UPDATE_GOALS,
  description: "Update one or more goals by their IDs.",
  parameters: Ni,
  outputSchema: Mi,
  annotations: { readOnlyHint: !1, destructiveHint: !1, idempotentHint: !0 },
  async execute({ goals: e }, t) {
    const o = await Promise.all(
      e.map(async (l) => {
        const p = Fi(l);
        if (p !== null)
          return { kind: "skipped", id: l.id, reason: p };
        const { id: u, description: m, deadline: h, responsibleUid: k, ...b } = l, C = await t.updateGoal(u, {
          ...b,
          description: Re(m, ne),
          deadline: Re(h, ne),
          responsibleUid: Re(k, Ke)
        });
        return { kind: "updated", goal: ve(C) };
      })
    ), r = o.filter(
      (l) => l.kind === "updated"
    ).map((l) => l.goal), n = o.filter(
      (l) => l.kind === "skipped"
    ).map(({ id: l, reason: p }) => ({ id: l, reason: p })), a = r.length, i = r.map((l) => `• ${l.name} (id=${l.id})`).join(`
`), c = Wi(n);
    return {
      textContent: a > 0 ? `Updated ${a} goal${a === 1 ? "" : "s"}${c}:
${i}` : `Updated 0 goals${c}`,
      structuredContent: {
        goals: r,
        totalCount: a,
        updatedGoalIds: r.map((l) => l.id),
        appliedOperations: {
          updateCount: a,
          skippedCount: n.length,
          skipped: n
        }
      }
    };
  }
};
function Wi(e) {
  if (e.length === 0) return "";
  const t = /* @__PURE__ */ new Map();
  for (const { id: r, reason: n } of e) {
    const a = t.get(n) ?? [];
    a.push(r), t.set(n, a);
  }
  return ` (${Array.from(t.entries()).map(
    ([r, n]) => `${n.length} skipped — ${r} (${n.join(", ")})`
  ).join("; ")})`;
}
const Gi = s.object({
  labelType: s.literal("personal").describe("Update a personal label, identified by its ID."),
  id: s.string().min(1).describe("The ID of the personal label to update."),
  name: s.string().min(1).max(128).optional().describe("The new name of the label."),
  color: de,
  order: s.number().int().optional().describe("The new position of the label in the label list."),
  isFavorite: s.boolean().optional().describe("Whether to mark the label as a favorite.")
}), Bi = s.object({
  labelType: s.literal("shared").describe(
    "Rename a shared label across all tasks. Shared labels are identified by name; only renaming is supported (no color, order, or favorite)."
  ),
  name: s.string().min(1).describe("The current name of the shared label."),
  newName: s.string().min(1).describe("The new name for the shared label.")
}), Yi = s.discriminatedUnion("labelType", [
  Gi,
  Bi
]), qi = {
  labels: s.array(Yi).min(1).describe(
    'The labels to update. Use labelType="personal" with an ID to update a personal label, or labelType="shared" with name+newName to rename a shared label.'
  )
}, zi = s.object({
  name: s.string().describe("The previous name of the shared label."),
  newName: s.string().describe("The new name of the shared label.")
}), Ki = {
  updatedLabels: s.array(pe).describe("The updated personal labels."),
  renamedSharedLabels: s.array(zi).describe("The shared labels that were renamed."),
  totalCount: s.number().describe("The total number of successful operations (personal + shared)."),
  appliedOperations: s.object({
    updateCount: s.number().describe("Number of personal labels updated."),
    renameCount: s.number().describe("Number of shared labels renamed."),
    skippedCount: s.number().describe("Number of operations skipped (no changes or shared label not found).")
  }).describe("Summary of operations performed.")
}, Vi = {
  name: f.UPDATE_LABELS,
  description: "Update one or more existing labels. Personal labels (identified by ID) can have their name, color, order, and favorite flag updated. Shared labels (identified by name) can only be renamed.",
  parameters: qi,
  outputSchema: Ki,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { labels: o } = e, r = await Promise.all(
      o.map(async (p) => {
        if (p.labelType === "personal") {
          const m = Xi(p);
          if (m !== null) return { kind: "skipped", reason: m };
          const { id: h, labelType: k, ...b } = p;
          return { kind: "updated", label: await t.updateLabel(h, b) };
        }
        return await t.renameSharedLabel({
          name: p.name,
          newName: p.newName
        }) ? { kind: "renamed", name: p.name, newName: p.newName } : { kind: "skipped", reason: "not-found" };
      })
    ), n = r.filter((p) => p.kind === "updated").map((p) => pe.parse(p.label)), a = r.filter(
      (p) => p.kind === "renamed"
    ).map(({ name: p, newName: u }) => ({ name: p, newName: u })), i = r.filter((p) => p.kind === "skipped").length, c = r.filter(
      (p) => p.kind === "skipped" && p.reason === "no-fields"
    ).length, d = r.filter(
      (p) => p.kind === "skipped" && p.reason === "not-found"
    ).length;
    return {
      textContent: Ji({
        updatedLabels: n,
        renamedSharedLabels: a,
        skippedNoFields: c,
        skippedNotFound: d
      }),
      structuredContent: {
        updatedLabels: n,
        renamedSharedLabels: a,
        totalCount: n.length + a.length,
        appliedOperations: {
          updateCount: n.length,
          renameCount: a.length,
          skippedCount: i
        }
      }
    };
  }
};
function Ji({
  updatedLabels: e,
  renamedSharedLabels: t,
  skippedNoFields: o,
  skippedNotFound: r
}) {
  const n = e.length, a = t.length, i = n + a, c = [];
  c.push(
    `Updated ${i} label${i === 1 ? "" : "s"} (${n} personal, ${a} shared)`
  );
  const d = [];
  if (o > 0 && d.push(`${o} skipped - no changes`), r > 0 && d.push(`${r} skipped - shared label not found`), d.length > 0 && (c[0] += ` (${d.join(", ")})`), n > 0) {
    c.push("Personal:");
    for (const l of e) c.push(`• ${l.name} (id=${l.id})`);
  }
  if (a > 0) {
    c.push("Shared:");
    for (const l of t) c.push(`• ${l.name} → ${l.newName}`);
  }
  return c.join(`
`);
}
function Xi({
  id: e,
  labelType: t,
  ...o
}) {
  return Object.values(o).every((n) => n === void 0) ? "no-fields" : null;
}
const Zi = s.object({
  id: s.string().min(1).describe("The ID of the project to update."),
  name: s.string().min(1).optional().describe("The new name of the project."),
  isFavorite: s.boolean().optional().describe("Whether the project is a favorite."),
  viewStyle: s.enum(["list", "board", "calendar"]).optional().describe("The project view style."),
  description: s.preprocess(
    // `null` is the advertised clear value; the param schema stays a
    // plain string (Gemini forbids nullable schemas, not preprocessing),
    // so `null` is normalised to "" before the project wire clear.
    (e) => e === null ? "" : e,
    s.string().describe(
      "The description of the project (Markdown). Pass null (or an empty string) to clear it."
    )
  ).optional(),
  color: de
}), Qi = {
  projects: s.array(Zi).min(1).describe("The projects to update.")
}, ec = {
  projects: s.array(ue).describe("The updated projects."),
  totalCount: s.number().describe("The total number of projects updated."),
  updatedProjectIds: s.array(s.string()).describe("The IDs of the updated projects."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of projects actually updated."),
    skippedCount: s.number().describe("The number of projects skipped (no changes).")
  }).describe("Summary of operations performed.")
}, tc = {
  name: f.UPDATE_PROJECTS,
  description: "Update multiple existing projects with new values.",
  parameters: Qi,
  outputSchema: ec,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { projects: o } = e, r = await Promise.all(
      o.map(async (d) => {
        const l = oc(d);
        if (l !== null) return { kind: "skipped", reason: l };
        const { id: p, ...u } = d;
        return { kind: "updated", project: await t.updateProject(p, u) };
      })
    ), n = r.filter(
      (d) => d.kind === "updated"
    ).map((d) => V(d.project)), a = r.filter(
      (d) => d.kind === "skipped" && d.reason === "no-fields"
    ).length, i = r.filter(
      (d) => d.kind === "skipped" && d.reason === "no-valid-values"
    ).length;
    return {
      textContent: sc({
        projects: n,
        skippedNoFields: a,
        skippedNoValidValues: i
      }),
      structuredContent: {
        projects: n,
        totalCount: n.length,
        updatedProjectIds: n.map((d) => d.id),
        appliedOperations: {
          updateCount: n.length,
          skippedCount: a + i
        }
      }
    };
  }
};
function sc({
  projects: e,
  skippedNoFields: t,
  skippedNoValidValues: o
}) {
  const r = e.length, n = e.map((c) => `• ${c.name} (id=${c.id})`).join(`
`);
  let a = `Updated ${r} project${r === 1 ? "" : "s"}`;
  const i = [];
  return t > 0 && i.push(`${t} skipped - no changes`), o > 0 && i.push(`${o} skipped - no valid field values`), i.length > 0 && (a += ` (${i.join(", ")})`), r > 0 && (a += `:
${n}`), a;
}
function oc({ id: e, ...t }) {
  const o = Object.values(t);
  return o.length === 0 ? "no-fields" : o.every((r) => r === void 0) ? "no-valid-values" : null;
}
const rc = s.object({
  type: s.literal("relative"),
  id: s.string().min(1).describe("The ID of the relative reminder to update."),
  minuteOffset: s.number().int().min(0).optional().describe("New minute offset before task due time."),
  service: Ce.optional().describe('New delivery method: "email" or "push".'),
  isUrgent: $e
}), nc = s.object({
  type: s.literal("absolute"),
  id: s.string().min(1).describe("The ID of the absolute reminder to update."),
  due: Yt.optional().describe("New due date/time for the reminder."),
  service: Ce.optional().describe('New delivery method: "email" or "push".'),
  isUrgent: $e
}), ac = s.object({
  type: s.literal("location"),
  id: s.string().min(1).describe("The ID of the location reminder to update."),
  name: s.string().optional().describe("New location name."),
  locLat: s.string().optional().describe("New latitude."),
  locLong: s.string().optional().describe("New longitude."),
  locTrigger: qt.optional().describe(
    'New trigger condition: "on_enter" or "on_leave".'
  ),
  radius: s.number().int().optional().describe("New radius in meters.")
}), ic = s.discriminatedUnion("type", [
  rc,
  nc,
  ac
]), cc = {
  reminders: s.array(ic).min(1).max(Te).describe(
    `Array of reminders to update (max ${Te}). Each must include the reminder type and ID. Only include fields that need to change.`
  )
}, dc = {
  reminders: s.array(st).describe("The updated reminders."),
  totalCount: s.number().describe("Total reminders updated."),
  updatedReminderIds: s.array(s.string()).describe("IDs of updated reminders.")
}, lc = {
  name: f.UPDATE_REMINDERS,
  description: 'Update existing reminders. Each reminder must specify its type ("relative", "absolute", or "location") and ID. Only include fields that need to change.',
  parameters: cc,
  outputSchema: dc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !0 },
  async execute(e, t) {
    const { reminders: o } = e, r = o.map(async (d) => {
      switch (d.type) {
        case "relative": {
          const { id: l, type: p, ...u } = d;
          return await t.updateReminder(l, {
            reminderType: "relative",
            ...u
          });
        }
        case "absolute": {
          const { id: l, type: p, ...u } = d;
          return await t.updateReminder(l, {
            reminderType: "absolute",
            ...u
          });
        }
        case "location": {
          const { id: l, type: p, ...u } = d;
          return await t.updateLocationReminder(l, u);
        }
      }
    }), a = (await Promise.all(r)).map(he), i = a.length === 1 ? "reminder" : "reminders";
    return {
      textContent: `Updated ${a.length} ${i}`,
      structuredContent: {
        reminders: a,
        totalCount: a.length,
        updatedReminderIds: a.map((d) => d.id)
      }
    };
  }
}, uc = s.object({
  id: s.string().min(1).describe("The ID of the section to update."),
  name: s.string().min(1).optional().describe("The new name of the section."),
  description: s.preprocess(
    // `null` is the advertised clear value; the param schema stays a
    // plain string (Gemini forbids nullable schemas, not preprocessing),
    // so `null` is normalised to "" and then mapped to the section
    // wire clear (`null`) at execute.
    (e) => e === null ? "" : e,
    s.string().describe(
      "The description of the section (Markdown). Pass null (or an empty string) to clear it."
    )
  ).optional()
}).refine((e) => e.name !== void 0 || e.description !== void 0, {
  message: 'Provide at least one of "name" or "description" to update.'
}), pc = {
  sections: s.array(uc).min(1).describe("The sections to update.")
}, mc = {
  sections: s.array(ce).describe("The updated sections."),
  totalCount: s.number().describe("The total number of sections updated."),
  updatedSectionIds: s.array(s.string()).describe("The IDs of the updated sections.")
}, hc = {
  name: f.UPDATE_SECTIONS,
  description: "Update multiple existing sections with new values.",
  parameters: pc,
  outputSchema: mc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute({ sections: e }, t) {
    const o = await Promise.all(
      e.map(({ id: n, name: a, description: i }) => {
        const c = {
          ...a !== void 0 ? { name: a } : {},
          ...i !== void 0 ? { description: i === "" ? null : i } : {}
        };
        return t.updateSection(n, c);
      })
    );
    return {
      textContent: fc({
        sections: o
      }),
      structuredContent: {
        sections: o.map(X),
        totalCount: o.length,
        updatedSectionIds: o.map((n) => n.id)
      }
    };
  }
};
function fc({ sections: e }) {
  const t = e.length, o = e.map((n) => `• ${n.name} (id=${n.id}, projectId=${n.projectId})`).join(`
`);
  return `Updated ${t} section${t === 1 ? "" : "s"}:
${o}`;
}
const bc = s.object({
  id: s.string().min(1).describe("The ID of the task to update."),
  content: s.string().optional().describe(
    'The new task name/title. Should be concise and actionable (e.g., "Review PR #123", "Call dentist"). For longer content, use the description field instead. Supports Markdown.'
  ),
  description: s.string().optional().describe(
    "New additional details, notes, or context for the task. Use this for longer content rather than putting it in the task name. Supports Markdown."
  ),
  projectId: s.string().optional().describe(
    'The new project ID for the task. Project ID should be an ID string, or the text "inbox", for inbox tasks.'
  ),
  sectionId: s.string().optional().describe("The new section ID for the task."),
  parentId: s.string().optional().describe("The new parent task ID (for subtasks)."),
  order: s.number().optional().describe("The new order of the task within its parent/section."),
  priority: Ze.optional().describe(Xe),
  dueString: s.preprocess(
    // Keep accepting legacy null while exposing a Gemini-compatible string schema.
    (e) => e === null ? "remove" : e,
    s.string().describe(
      'The new due date for the task in natural language (e.g., "tomorrow at 5pm"). Use "remove" to clear the due date.'
    )
  ).optional(),
  deadlineDate: s.preprocess(
    // Keep accepting legacy null while exposing a Gemini-compatible string schema.
    (e) => e === null ? "remove" : e,
    s.string().describe(
      'The new deadline date for the task in ISO 8601 format (YYYY-MM-DD, e.g., "2025-12-31"). Deadlines are immovable constraints shown with a different indicator than due dates. Use "remove" to clear the deadline.'
    )
  ).optional(),
  duration: s.string().optional().describe(
    'The duration of the task. Use format: "2h" (hours), "90m" (minutes), "2h30m" (combined), or "1.5h" (decimal hours). Max 24h.'
  ),
  responsibleUser: s.string().optional().describe(
    'Change task assignment. Use "unassign" to remove assignment. Can be "me" (assigns to current user), a user ID, name, or email. User must be a project collaborator.'
  ),
  labels: s.array(s.string()).optional().describe("The new labels for the task. Replaces all existing labels."),
  isUncompletable: s.boolean().optional().describe(
    "Whether this task should be uncompletable (organizational header). Tasks with isUncompletable: true appear as organizational headers and cannot be completed."
  )
}), gc = ["remove", "no date"], yc = ["remove", "no date", "no deadline"], kc = "no date", Tc = {
  tasks: s.array(bc).min(1).describe("The tasks to update.")
}, wc = {
  tasks: s.array(J).describe("The updated tasks."),
  totalCount: s.number().describe("The total number of tasks updated."),
  updatedTaskIds: s.array(s.string()).describe("The IDs of the updated tasks."),
  appliedOperations: s.object({
    updateCount: s.number().describe("The number of tasks actually updated."),
    skippedCount: s.number().describe("The number of tasks skipped (no changes).")
  }).describe("Summary of operations performed.")
}, vc = {
  name: f.UPDATE_TASKS,
  description: "Update existing tasks including content, dates, priorities, and assignments.",
  parameters: Tc,
  outputSchema: wc,
  annotations: { readOnlyHint: !1, destructiveHint: !0, idempotentHint: !1 },
  async execute(e, t) {
    const { tasks: o } = e, r = o.map(async (c) => {
      if (!jc(c))
        return;
      const {
        id: d,
        projectId: l,
        sectionId: p,
        parentId: u,
        dueString: m,
        duration: h,
        responsibleUser: k,
        priority: b,
        labels: C,
        deadlineDate: I,
        ...x
      } = c, w = await K({
        projectId: l,
        client: t
      });
      let y = {
        ...x,
        ...C !== void 0 && { labels: C }
      };
      b && (y.priority = Mt(b));
      const D = Dt(
        m,
        gc,
        kc
      );
      D !== void 0 && (y = { ...y, dueString: D });
      const R = Dt(
        I,
        yc,
        null
      );
      if (R !== void 0 && (y = { ...y, deadlineDate: R }), h)
        try {
          const { minutes: S } = Lt(h);
          y = {
            ...y,
            duration: S,
            durationUnit: "minute"
          };
        } catch (S) {
          throw S instanceof U ? new Error(`Task ${d}: ${S.message}`) : S;
        }
      if (k !== void 0)
        if (k === null || k === "unassign")
          y = { ...y, assigneeId: null };
        else {
          const S = await rt.validateTaskUpdateAssignment(
            t,
            d,
            k
          );
          if (!S.isValid) {
            const A = S.error?.message || "Assignment validation failed", F = S.error?.suggestions?.join(". ") || "";
            throw new Error(
              `Task ${d}: ${A}${F ? `. ${F}` : ""}`
            );
          }
          y = { ...y, assigneeId: S.resolvedUser?.userId };
        }
      if (!w && !p && !u)
        return await t.updateTask(d, y);
      const v = Lo(d, w, p, u), $ = await t.moveTask(d, v);
      return Object.keys(y).length > 0 ? await t.updateTask(d, y) : $;
    }), n = (await Promise.all(r)).filter(
      (c) => c !== void 0
    ), a = n.map(_);
    return {
      textContent: Ic({
        tasks: a,
        args: e
      }),
      structuredContent: {
        tasks: a,
        totalCount: a.length,
        updatedTaskIds: n.map((c) => c.id),
        appliedOperations: {
          updateCount: a.length,
          skippedCount: o.length - a.length
        }
      }
    };
  }
};
function Ic({
  tasks: e,
  args: t
}) {
  const o = t.tasks.length, r = e.length, n = o - r;
  let a = "";
  return n > 0 && (a = ` (${n} skipped - no changes)`), nt("Updated", e, {
    context: a,
    showDetails: e.length <= 5
  });
}
function jc({ id: e, ...t }) {
  return Object.keys(t).length > 0;
}
function Dt(e, t, o) {
  if (e === void 0)
    return e;
  if (e === null)
    return o;
  const r = e.trim().toLowerCase();
  return t.includes(r) ? o : e;
}
const Sc = {}, Cc = {
  type: s.literal("user_info").describe("The type of the response."),
  userId: s.string().describe("The user ID."),
  fullName: s.string().describe("The full name of the user."),
  timezone: s.string().describe("The timezone of the user."),
  currentLocalTime: s.string().describe("The current local time of the user."),
  startDay: s.number().describe("The start day of the week (1 = Monday, 7 = Sunday)."),
  startDayName: s.string().describe("The name of the start day."),
  weekStartDate: s.string().describe("The start date of the current week (YYYY-MM-DD)."),
  weekEndDate: s.string().describe("The end date of the current week (YYYY-MM-DD)."),
  currentWeekNumber: s.number().describe("The current week number of the year."),
  completedToday: s.number().describe("The number of tasks completed today."),
  dailyGoal: s.number().describe("The daily goal for task completions."),
  weeklyGoal: s.number().describe("The weekly goal for task completions."),
  email: s.string().describe("The email address of the user."),
  plan: s.enum(["Todoist Free", "Todoist Pro", "Todoist Business"]).describe("The user plan.")
};
function $c(e) {
  return e.businessAccountId ? "Todoist Business" : e.isPremium ? "Todoist Pro" : "Todoist Free";
}
function Dc(e, t) {
  const r = ((e.getDay() || 7) - t + 7) % 7, n = new Date(e);
  return n.setDate(e.getDate() - r), n;
}
function Ac(e) {
  const t = new Date(e);
  return t.setDate(e.getDate() + 6), t;
}
function xc(e) {
  const t = new Date(e.getFullYear(), 0, 1), o = (e.getTime() - t.getTime()) / 864e5;
  return Math.ceil((o + t.getDay() + 1) / 7);
}
function Ec(e) {
  return ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"][e === 7 ? 0 : e] ?? "Unknown";
}
function Oc(e) {
  try {
    return new Intl.DateTimeFormat("en-US", { timeZone: e }), !0;
  } catch {
    return !1;
  }
}
function is(e) {
  return Oc(e) ? e : "UTC";
}
function Uc(e, t) {
  const o = is(t);
  return e.toLocaleString("en-US", {
    timeZone: o,
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hour12: !1
  });
}
async function Pc(e) {
  const t = await e.getUser(), o = t.tzInfo?.timezone ?? "UTC", r = is(o), n = /* @__PURE__ */ new Date(), a = Uc(n, r), i = t.startDay ?? 1, c = Ec(i), d = $c(t), l = new Date(n.toLocaleString("en-US", { timeZone: r })), p = Dc(l, i), u = Ac(p), m = xc(l), k = [
    "# User Information",
    "",
    `**User ID:** ${t.id}`,
    `**Full Name:** ${t.fullName}`,
    `**Email:** ${t.email}`,
    `**Timezone:** ${r}`,
    `**Current Local Time:** ${a}`,
    "",
    "## Week Settings",
    `**Week Start Day:** ${c} (${i})`,
    `**Current Week:** Week ${m}`,
    `**Week Start Date:** ${re(p)}`,
    `**Week End Date:** ${re(u)}`,
    "",
    "## Daily Progress",
    `**Completed Today:** ${t.completedToday}`,
    `**Daily Goal:** ${t.dailyGoal}`,
    `**Weekly Goal:** ${t.weeklyGoal}`,
    "",
    "## Account Info",
    `**Plan:** ${d}`
  ].join(`
`), b = {
    type: "user_info",
    userId: t.id,
    fullName: t.fullName,
    timezone: r,
    currentLocalTime: a,
    startDay: i,
    startDayName: c,
    weekStartDate: re(p),
    weekEndDate: re(u),
    currentWeekNumber: m,
    completedToday: t.completedToday,
    dailyGoal: t.dailyGoal,
    weeklyGoal: t.weeklyGoal,
    email: t.email,
    plan: d
  };
  return { textContent: k, structuredContent: b };
}
const _c = {
  name: f.USER_INFO,
  description: "Get comprehensive user information including user ID, full name, email, timezone with current local time, week start day preferences, current week dates, daily/weekly goal progress, and user plan (Free/Pro/Business).",
  parameters: Sc,
  outputSchema: Cc,
  annotations: { readOnlyHint: !0, destructiveHint: !1, idempotentHint: !0 },
  async execute(e, t) {
    const o = await Pc(t);
    return {
      textContent: o.textContent,
      structuredContent: o.structuredContent
    };
  }
}, At = 10 * 1024 * 1024, Rc = /* @__PURE__ */ new Set([
  "image/png",
  "image/jpeg",
  "image/gif",
  "image/webp",
  "image/svg+xml"
]), Nc = /* @__PURE__ */ new Set([
  "text/plain",
  "text/csv",
  "text/html",
  "text/markdown",
  "application/json",
  "application/xml",
  "text/xml"
]), Lc = {
  ".png": "image/png",
  ".jpg": "image/jpeg",
  ".jpeg": "image/jpeg",
  ".gif": "image/gif",
  ".webp": "image/webp",
  ".svg": "image/svg+xml",
  ".txt": "text/plain",
  ".csv": "text/csv",
  ".html": "text/html",
  ".md": "text/markdown",
  ".json": "application/json",
  ".xml": "application/xml",
  ".pdf": "application/pdf"
};
function Mc(e) {
  return Rc.has(e) ? "image" : Nc.has(e) || e.startsWith("text/") ? "text" : "binary";
}
function Fc(e) {
  return (e.split(";")[0] ?? e).trim().toLowerCase();
}
function Hc(e) {
  try {
    const t = new URL(e).pathname, o = t.lastIndexOf(".");
    if (o === -1) return;
    const r = t.slice(o).toLowerCase();
    return Lc[r];
  } catch {
    return;
  }
}
function Wc(e) {
  try {
    const t = new URL(e).pathname, o = t.lastIndexOf("/");
    return o === -1 ? void 0 : t.slice(o + 1) || void 0;
  } catch {
    return;
  }
}
const Gc = {
  fileUrl: s.url().describe(
    "The URL of the attachment file to view. Get this from the fileUrl field in a comment's fileAttachment."
  )
}, Bc = {
  name: f.VIEW_ATTACHMENT,
  description: "View a file attachment from a Todoist comment. Pass the fileUrl from a comment's fileAttachment field. Supports images (returned inline), text files (returned as text), and binary files like PDFs (returned as embedded resources).",
  parameters: Gc,
  annotations: {
    readOnlyHint: !0,
    destructiveHint: !1,
    idempotentHint: !0
  },
  async execute({ fileUrl: e }, t) {
    const o = await t.viewAttachment(e), r = o.headers["content-length"], n = r ? Number.parseInt(r, 10) : void 0, a = Wc(e), i = o.headers["content-type"], c = i ? Fc(i) : void 0, d = c && c !== "application/octet-stream" ? c : Hc(e) ?? c ?? "application/octet-stream";
    if (n && n > At)
      return {
        textContent: `Attachment "${a ?? e}" is too large to display inline (${(n / 1024 / 1024).toFixed(1)}MB, limit is 10MB). File type: ${d}`
      };
    const l = Buffer.from(await o.arrayBuffer()), p = l.byteLength;
    if (p > At)
      return {
        textContent: `Attachment "${a ?? e}" is too large to display inline (${(p / 1024 / 1024).toFixed(1)}MB, limit is 10MB). File type: ${d}`
      };
    const u = Mc(d), m = [];
    return u === "image" ? m.push({
      type: "image",
      data: l.toString("base64"),
      mimeType: d
    }) : u === "text" ? m.push({
      type: "text",
      text: l.toString("utf-8")
    }) : m.push({
      type: "resource",
      resource: {
        uri: e,
        mimeType: d,
        blob: l.toString("base64")
      }
    }), {
      textContent: `Attachment: ${a ?? "unknown"} (${d}, ${(p / 1024).toFixed(1)}KB)`,
      contentItems: m
    };
  }
}, Yc = `
## Todoist Task and Project Management Tools

You have access to comprehensive Todoist management tools for personal productivity and team collaboration. Use these tools to help users manage tasks, projects, sections, comments, and assignments effectively.

### Core Capabilities:
- Create, update, complete, and search tasks with rich metadata (priorities, due dates, durations, assignments)
- Manage projects and sections with flexible organization
- Handle comments and collaboration features
- Bulk assignment operations for team workflows
- Get overviews and insights about workload and progress

### Tool Usage Guidelines:

**Task Management:**
- **add-tasks**: Create tasks (max 25 per call) with content, description, priority (\`p1\`, \`p2\`, \`p3\`, \`p4\` strings only; \`p1\` highest and \`p4\` lowest/default; integers are not accepted), dueString (natural language like "tomorrow", "next Friday", "2024-12-25"), deadlineDate (ISO 8601 format like "2025-12-31" for immovable constraints), duration (formats like "2h", "90m", "2h30m"), and assignments to project collaborators
- **update-tasks**: Modify existing tasks - get task IDs from search results first, only include fields that need changes. Supports priority updates using \`p1\`/\`p2\`/\`p3\`/\`p4\` string values (\`p1\` highest, \`p4\` lowest/default; integers are not accepted), due date updates via dueString and due date removal via "dueString: remove", plus deadlineDate (ISO 8601 format like "2025-12-31") updates and removals via "deadlineDate: remove". **IMPORTANT: Do NOT use update-tasks to reschedule/move task dates — use reschedule-tasks instead.** update-tasks replaces the entire due string which destroys recurrence patterns on recurring tasks.
- **reschedule-tasks**: **Always use this tool when moving/rescheduling task due dates to a different date.** This tool preserves recurring schedules and existing time-of-day. Accepts YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS. Works for both recurring and non-recurring tasks. Do NOT use update-tasks for rescheduling.
- **complete-tasks**: Mark tasks as done using task IDs
- **uncomplete-tasks**: Reopen completed tasks using task IDs
- **find-tasks**: Search by text, project/section/parent container, responsible user, labels, a raw Todoist \`filter\` string (e.g. "today", "p1", "##Work", "(today | overdue) & p1"), or a saved filter by ID or name (\`filterIdOrName\`). Requires at least one search parameter. \`filter\`/\`filterIdOrName\` cannot be combined with projectId/sectionId/parentId, and \`filter\` and \`filterIdOrName\` are mutually exclusive.
- **find-tasks-by-date**: Get tasks by date range (startDate: YYYY-MM-DD or 'today' which includes overdue tasks) or specific day counts
- **find-completed-tasks**: View completed tasks by completion date or original due date; if since/until are omitted, defaults to the last 7 days (returns all collaborators unless filtered)

**Project & Organization:**
- **add-projects/update-projects/find-projects**: Manage project lifecycle with names, descriptions (Markdown), favorites, view styles (list/board/calendar), and workspace assignment for new projects (by name or ID). find-projects returns active projects by default; pass archivedStatus ('archived' or 'all') to include archived projects. Every returned project includes an isArchived field
- **project-management**: Archive or unarchive projects by ID
- To delete a project (active or archived), use **delete-object** with type "project". Note: workspace projects must be archived first; personal projects can be deleted regardless
- **project-move**: Move projects between personal and workspace contexts
- **add-sections/update-sections/find-sections**: Organize tasks within projects using sections
- **find-goals/add-goals/update-goals**: Manage goals (personal or workspace). Goals track progress via linked tasks
- **complete-goals**: Complete or uncomplete goals by ID
- **link-goal-tasks**: Link or unlink tasks to/from a goal
- **get-overview**: Get comprehensive Markdown overview of entire account or specific project with task hierarchies. Project data includes parentId (sub-projects), folderId (workspace folder membership), and childOrder (sibling ordering)
- **list-workspaces**: Get all workspaces for the user with details like plan type, role, and settings

**Reminders:**
- **add-reminders**: Create reminders for tasks. Three types: "relative" (minutes before due), "absolute" (specific date/time), or "location" (geofence-triggered). Each reminder must specify a taskId.
- **find-reminders**: Find reminders by task ID (returns both time-based and location reminders), or get a specific reminder by ID (use reminderId for time-based, locationReminderId for location-based).
- **update-reminders**: Update existing reminders. Must specify the reminder type ("relative", "absolute", or "location") and ID.
- Relative and absolute reminders support an **isUrgent** flag to mark a reminder as urgent.
- Reminders can be deleted using **delete-object** with type "reminder" (time-based) or "location_reminder" (location-based).

**Collaboration & Comments:**
- **add-comments/update-comments/find-comments**: Manage task and project discussions
- **view-attachment**: View file attachments from comments. Pass the fileUrl from a comment's fileAttachment. Returns images inline, text files as text, and binary files as embedded resources.
- **find-project-collaborators**: Look up Todoist users (collaborators, teammates) by name or email to get their user ID — use for "find/who is X" questions or any time you need to resolve a person's name to an ID. By default searches collaborators of every shared project the authenticated user can access (plus the authenticated user themselves). An empty result means the person is not a collaborator on any shared project, not that they do not exist. Pass projectId to scope to a single project
- **manage-assignments**: Bulk assign/unassign/reassign up to 50 tasks with atomic operations and dry-run validation

**Filters:**
- **find-filters**: List all personal filters or search by name; filters are saved task views using query syntax
- **add-filters**: Create personal filters with name, query (e.g. "today & p1"), color, and favorite flag
- **update-filters**: Modify existing filters' name, query, color, or favorite status

**Activity & Audit:**
- **find-activity**: Retrieve recent activity logs to monitor and audit changes. Shows events from all users by default; use initiatorId to filter by specific user. Filter by object type (task/project/comment), event type (added/updated/deleted/completed/uncompleted/archived/unarchived/shared/left), and specific objects (objectId, projectId, taskId). Useful for tracking who did what and when. Note: Date-based filtering is not supported.
- **get-productivity-stats**: Get comprehensive productivity statistics including daily/weekly completion breakdowns, goal streaks (current, last, max), karma score and trends, and historical karma data. No parameters required.

**Project Health & Insights:**
- **get-project-health**: Get comprehensive health assessment for a project including completion progress (completed/active counts, percentage), health status (EXCELLENT/ON_TRACK/AT_RISK/CRITICAL), description, and task-level recommendations. Use includeContext=true for detailed metrics (overdue tasks, weekly activity, avg completion time) and full task data. Health data may be stale — check isStale flag.
- **get-project-activity-stats**: Get daily and optional weekly activity statistics for a project over a configurable time window (1-12 weeks). Useful for identifying activity trends.
- **analyze-project-health**: Trigger a new health analysis for a project. Use when health data is stale. The analysis may take time — use get-project-health afterward to see updated results.
- **get-workspace-insights**: Get aggregated health and progress insights across all projects in a workspace. Accepts workspace name or ID, with optional project ID filtering.

**General Operations:**
- **delete-object**: Remove projects, sections, tasks, comments, labels, filters, reminders, or location reminders by type and ID. Deletes both active and archived projects (workspace projects must be archived first; use find-projects with archivedStatus to locate archived projects)
- **fetch-object**: Fetch a single task, project, comment, or section by its ID
- **reorder-objects**: Reorder sibling projects or sections, and optionally move projects to a new parent. For projects: set order to reorder siblings, and/or set parentId to move under a new parent (use "root" for top level). For sections: set order to reorder within a project
- **user-info**: Get user details including timezone, goals, and plan information

### Best Practices:

1. **Task Creation**: Write clear, actionable task titles. Use natural language for due dates ("tomorrow", "next Monday"). Set appropriate priorities and include detailed descriptions when needed.

2. **Search Strategy**: Use specific search queries combining multiple filters for precise results. When searching for tasks, start with broader queries and narrow down as needed.

3. **Assignments & user lookup**: Always validate project collaborators exist before assigning tasks. Use find-project-collaborators to verify user access. Also use find-project-collaborators (with just a searchTerm and no projectId) to resolve a user's ID whenever the user references a person by name or email — it searches collaborators of all shared projects you can access, plus yourself.

4. **Bulk Operations**: When working with multiple items, prefer bulk tools (complete-tasks, manage-assignments) over individual operations for better performance.

5. **Date Handling**: All dates respect user timezone settings. Use 'today' keyword for dynamic date filtering (includes overdue tasks). **When rescheduling/moving tasks to a different date, always use reschedule-tasks** — never update-tasks with dueString, as that destroys recurrence on recurring tasks.

6. **Labels**: Use label filtering with AND/OR operators for advanced task organization. Most search tools support labels parameter. Use **find-labels** to discover personal and shared labels — use label **names** (not IDs) when filtering tasks, and use label **IDs** only with **delete-object** and **update-labels** (for personal label updates). Use **add-labels** to create new personal labels. Use **update-labels** to rename or recolor personal labels (by ID), or to rename shared labels (by name) — note that shared labels support renaming only, not color/order/favorite changes.

7. **Pagination**: Large result sets use cursor-based pagination. Use limit parameter to control result size (default varies by tool).

8. **Error Handling**: All tools provide detailed error messages and next-step suggestions. Pay attention to validation feedback for corrective actions.

### Common Workflows:

- **Daily Planning**: Use find-tasks-by-date with 'today' and get-overview for project status
- **Team Assignment**: find-project-collaborators → add-tasks with responsibleUser → manage-assignments for bulk changes
- **User Lookup**: find-project-collaborators with just a searchTerm (no projectId) to resolve a name or email to a Todoist user ID across all shared-project collaborators you can access
- **Task Search**: find-tasks with multiple filters → update-tasks or complete-tasks based on results
- **Project Organization**: add-projects → add-sections → add-tasks with projectId and sectionId
- **Progress Reviews**: find-completed-tasks (defaults to last 7 days; optionally use explicit date ranges) → get-overview for project summaries
- **Activity Auditing**: find-activity with event/object filters to track changes, monitor team activity, or investigate specific actions
- **Productivity Analysis**: Use the productivity-analysis prompt for comprehensive analysis combining user-info, get-productivity-stats, and find-completed-tasks data into actionable insights
- **Project Health Reviews**: get-project-health → analyze-project-health if stale → get-project-health with includeContext=true for detailed metrics → get-workspace-insights for cross-project overview

Always provide clear, actionable task titles and descriptions. Use the overview tools to give users context about their workload and project status.
`;
function nd({
  todoistApiKey: e,
  baseUrl: t,
  features: o = []
}) {
  const r = new ds(
    { name: "todoist-mcp-server", version: Je },
    {
      capabilities: {
        tools: { listChanged: !0 },
        prompts: { listChanged: !0 }
      },
      instructions: Yc
    }
  ), n = to(e, { baseUrl: t }), a = {
    ...aa,
    _meta: {
      ui: {
        resourceUri: He
      }
    }
  };
  xs(r);
  const i = { server: r, client: n, features: o };
  return g({ tool: Wr, ...i }), g({ tool: en, ...i }), g({ tool: vi, ...i }), g({ tool: vc, ...i }), g({ tool: fi, ...i }), g({ tool: ma, ...i }), g({ tool: a, ...i }), g({ tool: xn, ...i }), g({ tool: wr, ...i }), g({ tool: tc, ...i }), g({ tool: zn, ...i }), g({ tool: ni, ...i }), g({ tool: ci, ...i }), g({ tool: Pr, ...i }), g({ tool: hc, ...i }), g({ tool: sa, ...i }), g({ tool: Pn, ...i }), g({ tool: dr, ...i }), g({ tool: Hi, ...i }), g({ tool: Xr, ...i }), g({ tool: Ya, ...i }), g({ tool: Jo, ...i }), g({ tool: wn, ...i }), g({ tool: $i, ...i }), g({ tool: Ar, ...i }), g({ tool: Xn, ...i }), g({ tool: lc, ...i }), g({ tool: Bc, ...i }), g({ tool: mr, ...i }), g({ tool: Vi, ...i }), g({ tool: Nn, ...i }), g({ tool: er, ...i }), g({ tool: nr, ...i }), g({ tool: Oi, ...i }), g({ tool: hn, ...i }), g({ tool: Ca, ...i }), g({ tool: La, ...i }), g({ tool: Ea, ...i }), g({ tool: Kr, ...i }), g({ tool: Wa, ...i }), g({ tool: Ia, ...i }), g({ tool: rn, ...i }), g({ tool: cn, ...i }), g({ tool: ui, ...i }), g({ tool: _c, ...i }), g({ tool: Hn, ...i }), g({ tool: si, ...i }), g({ tool: Ja, ...i }), g({ tool: ki, ...i }), g({ tool: un, ...i }), r.registerPrompt(
    me.name,
    {
      title: me.title,
      description: me.description,
      argsSchema: me.argsSchema
    },
    me.callback
  ), r;
}
export {
  Ya as A,
  Xr as B,
  Hi as C,
  dr as D,
  Pn as E,
  sa as F,
  hc as G,
  Pr as H,
  zn as I,
  tc as J,
  wr as K,
  fi as L,
  xn as M,
  aa as N,
  ma as O,
  vc as P,
  vi as Q,
  en as R,
  Wr as S,
  rd as T,
  to as U,
  qs as V,
  ao as W,
  Hn as a,
  Ia as b,
  Wa as c,
  rn as d,
  Kr as e,
  un as f,
  nd as g,
  Ea as h,
  La as i,
  Ca as j,
  hn as k,
  Ja as l,
  si as m,
  Oi as n,
  nr as o,
  er as p,
  Nn as q,
  ui as r,
  ki as s,
  Vi as t,
  _c as u,
  mr as v,
  Bc as w,
  wn as x,
  $i as y,
  Jo as z
};
